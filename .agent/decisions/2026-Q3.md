# Decisions archive — 2026 Q3

Archived from `.agent/STATE.md` on 2026-08-27 when that file passed the ~200-line limit in
CLAUDE.md §4.1. Append-only: these are the record of why things are as they are, and are never
rewritten. Current decisions stay in `STATE.md`; this holds the ones no longer actively consulted.

## Bootstrap and scaffolding (2026-07-27 to 2026-08-08)

Everything below predates the arrival of `product-specification/` on 2026-08-26. It covers repo
scaffolding, the first backend vertical slice, the first contract promotion, and CI activation.

- 2026-07-27 Scaffolding created (CLAUDE.md, .agent/, .claude/agents/, contracts/) — orchestrator owns these per §11; needed before any dispatch is possible.
- 2026-07-27 contracts/openapi.json deliberately NOT created as a placeholder — §3 says it is never written by hand, and a stub would make every §4.4 drift check meaningless. It appears at the first backend build.
- 2026-08-08 Git initialised (resolves Open question #2). Remote `origin` points at https://github.com/maxcotech/school-management-proj.git.
- 2026-08-08 Backend reference vertical slice implemented and its OpenAPI contract promoted for the first time — `contracts/openapi.json` + `CONTRACT.lock` now real, hash verified to match (see Contract). Integration tests verified against a hosted Neon Postgres rather than Testcontainers — accepted per backend/docs/ASSUMPTIONS.md §3.1; revisit if a team Docker/Postgres standard is set later.
- 2026-08-08 `.claude/settings.json` created: Edit/Write on `contracts/openapi.json` and `contracts/CONTRACT.lock` only are allowed without prompting, so a backend session running `generate-openapi.ps1 -Promote` isn't blocked. Nothing else under `contracts/**` or `.agent/**` is exempted — hand-edits there still prompt. This is a permission convenience, not a substitute for the §4.4 drift check.
- 2026-08-08 CI workflow activated: moved `backend/ci/github-actions-backend.yml` to `.github/workflows/backend-ci.yml` (root-level CI is orchestrator-owned per §11; the file was staged by backend-dev with an explicit note for the orchestrator to relocate it). Not yet pushed/verified against a live Actions run.

## Full text of 2026-08-26/27 decisions, condensed in STATE.md on 2026-08-27

STATE.md kept one-line summaries per CLAUDE.md §4.1; the verbose originals are preserved here.
Per-card detail — evidence, pasted gate output, reasoning — lives in each `tasks/TASK-####.md` Log.

- 2026-08-26 Product scope resolved (Open question 7). `product-specification/` revision 3.1 is the authoritative requirements source for every task card. Cards cite spec section numbers rather than restating requirements, so a spec revision does not silently invalidate a card.

- 2026-08-26 Package manager: **npm** (Open question 3). Already in use, lockfile committed, pnpm absent. No reason to churn.

- 2026-08-26 Client generator: **openapi-typescript, types only** (Open question 6). Rejected openapi-fetch / NSwag / Kiota because the frontend already owns a working axios transport with auth interceptors and a single-flight refresh seam (`src/lib/http/`); a runtime client would duplicate it and force a rewrite of that seam. `src/api/` holds generated types plus a thin typed request helper over that transport, which satisfies §7's "all server communication goes through `src/api/`" without running two HTTP stacks.

- 2026-08-26 Build sequencing departs from `index.md`'s suggested order in one place: the privilege register and authorisation middleware (TASK-0002) come **before** school settings, not after. Spec 9.2 requires every route to declare the privilege it needs and to fail registration at boot if it does not — building settings endpoints first would mean writing routes against an enforcement layer that does not exist, then retrofitting it.

- 2026-08-26 **Auth carrier: HttpOnly cookie session + CSRF token** (Open question 8), human sign-off per §5. Rejected the bearer-in-memory approach the frontend scaffold provisionally built: a token held in JS is readable by any XSS, and it dies on page reload, so it must either sign the user out on refresh or be persisted somewhere — which is exactly the exposure the in-memory choice was trying to avoid. The cookie carries the same opaque server-side token spec 9.1 mandates, so nothing about the backend token model changes. Cost is a CSRF token on every mutation and one coherent cookie/CORS configuration end to end; the frontend change is confined to `src/lib/auth/auth-session.ts` and `src/lib/http/http-client.ts` per `frontend/HANDOFF.md`.

- 2026-08-26 Spec conflicts `25-open-conflicts-to-resolve.md` items 1, 2 and 5 are resolved by the school; items 3, 6, 7, 8 and 9 have provisional resolutions the build follows as written. Item 4 (weekly Parent's Comment) is the only unresolved item that changes what gets built; it is sequenced late (weekly reports) and blocks nothing before it. No subagent may amend a provisional resolution — raise it in Open questions instead.

- 2026-08-26 TASK-0004 closed: contract pipeline wired. `openapi-typescript@7.13.0` (pinned exact) generates `frontend/src/api/schema.d.ts`; `frontend/src/api/client.ts` (`apiGet`/`apiPost`) is a hand-written typed helper over the existing `src/lib/http/` transport — no second HTTP stack, auth seam untouched. MSW defaults (`src/test/msw/handlers.ts`) are now derived from `contracts/openapi.json` via `src/test/msw/openapi-handlers.ts` instead of hand-written. A new Vitest boundary test (`src/test/http-boundary.test.ts`) fails on any raw `axios`/`fetch(` outside `src/lib/http/` — §4.4 check 3. Deviation: `openapi-typescript@7.13.0` declares `peerDependencies: { typescript: "^5.x" }` and this repo runs TypeScript 6.0.3, which a plain `npm ci` rejects with ERESOLVE. Resolved with a scoped `package.json` `overrides` entry (`{ "openapi-typescript": { "typescript": "$typescript" } }`), not a repo-wide `.npmrc` `legacy-peer-deps` flag — the orchestrator caught that the broader flag would silently let a genuine future peer conflict install broken instead of erroring. The override pins only openapi-typescript's own `typescript` resolution to this repo's `typescript` devDependency range; every other package's peers are still checked normally. Verified with a fully clean `npm install` (no lockfile, no node_modules, no `.npmrc`) — resolves without ERESOLVE — and `npm ci` from the resulting lockfile. Justification recorded in `frontend/CONVENTIONS.md` §1. Revisit (and drop the override) once openapi-typescript ships a TS 6-aware peer range upstream. `.oxlintrc.json` ignorePatterns changed from blanket `src/api/**` to `src/api/schema.d.ts` only (hand-written `client.ts` is now linted) plus `scripts/**` added (Node codegen scripts, not shipped/bundled code, same rationale as excluding `dist`/`coverage`).

- 2026-08-26 **Frontend dependency refresh accepted, reviewed after the fact.** Closing TASK-0004 the frontend agent regenerated `package-lock.json` from scratch rather than adding one devDependency surgically. That swept 73 resolved versions, added 21 packages and removed 7. Every bump is inside a caret range `package.json` already declared, so nothing violates a pin the team chose; all gates are green on the new tree (124/124, zero lint warnings, clean build, no drift) and `npm audit` reports 0 vulnerabilities where the old tree carried a `nanoid` advisory. Accepted on that basis — reverting to re-pin 73 packages would be churn against a healthier tree. What was wrong was the process, not the result: this was never in the card, and §8 requires one logical change per commit. It therefore lands as its OWN commit, separate from the codegen work, so a later bisect can tell a base-ui regression from a codegen bug.

- 2026-08-26 Frontend CI workflow written (`.github/workflows/frontend-ci.yml`), root-level CI being orchestrator-owned per §11. Mirrors backend-ci.yml's house style: path-filtered on `frontend/**` and `contracts/**`, `npm ci` never `npm install`, Node pinned to 22.21.0 to match the dev machine (raising it and the React Router 7 pin in HANDOFF.md decision 2 must be revisited together), and the contract drift check as its own final step so a stale generated client reads differently from a broken frontend. Neither workflow has been verified against a live Actions run yet.

- 2026-08-26 **TASK-0002 closed.** The 93-privilege register, scope resolution (spec 4.2.1's four rules), the effective-privilege-set decision (`PrivilegeDecision.IsAuthorized`), the escalation guard (`RoleScopeGuard`), and the boot-time privilege-declaration guard are all built and tested. Two deliberate design choices worth recording:
  - **No `admin_account`/`role`/`role_assignment` persistence was added**, contrary to the task card's fallback permission to add it "if unavoidable to make resolution testable." It was avoidable: scope resolution and the escalation rule are pure functions over ports (`IPupilArmOfRecordLookup`, `IResultSetArmLookup`) and plain data (`PrivilegeGrant`), fully unit-testable with fakes, and the HTTP-level 401/403/200 states are integration-tested against a test-only auth scheme + a controllable `IEffectivePrivilegeProvider`/`IAuthorizationAuditSink` — no database schema needed. This keeps TASK-0003's future `admin_account` entity from being pre-empted or duplicated.
  - `IEffectivePrivilegeProvider` (returns empty — deny-by-default), `IAuthorizationAuditSink` (logs, doesn't persist), `IPupilArmOfRecordLookup`/`IResultSetArmLookup` (throw — no route uses them yet) are registered SEAMS in `SchoolManagement.Infrastructure/Authorization/`, each documented as such, each with a `TODO(TASK-0002)` where persistence should eventually land. The real role/assignment module and the audit log module (spec 6.1.12) replace them.

- 2026-08-26 **`/api/v1/reference/whoami` changed from implicitly-protected to `.AllowAnonymous()`.** Before TASK-0002's boot-time privilege-declaration guard, this endpoint deliberately declared no authorisation metadata at all, relying purely on the deny-by-default fallback policy, to prove that policy protects a route nobody remembered to secure. The guard now makes exactly that pattern — protected-by-bare-fallback-only, no explicit declaration — a STARTUP failure for every route (spec 9.2), so the endpoint could no longer exist in its old form. Per the task card's own instruction for this exact collision ("mark it explicitly anonymous rather than inventing a privilege for it"), it is now anonymous; it still reports whatever identity a future auth mechanism attaches. The deny-by-default guarantee itself is now proven statically (`PrivilegeDeclarationGuardTests`, a route with neither declaration fails to register) and at runtime for anything unmapped (`AnUnknownRoute_Returns401NotFound_BecauseOfTheFallbackPolicy`, unchanged). **Worth a human glance** — it is a behaviour change on an existing, previously-tested endpoint, even though the response shape did not change.

- 2026-08-26 New reference-slice endpoint `GET /api/v1/reference/arms/{armId}/secure` (privilege `arm.view`, scope `ScopeParameterKind.Arm`) added to `ReferenceEndpoints.cs` purely as a worked example of `.RequirePrivilege(...)` and as the fixture the integration tests exercise for anonymous/wrong-privilege/out-of-scope/in-scope. Same category as `/whoami` and `/records` — reference scaffolding, not a product surface — and documented as such in its own XML doc comments.

- 2026-08-26 `backend/scripts/ci.ps1`'s coverage-threshold gate had a latent bug: `Get-Content $trx.FullName -Raw` (positional path) breaks when a `.trx` filename contains a literal `[1]` disambiguation suffix — PowerShell's provider resolves unbracketed `[...]` as a wildcard character class. TASK-0002 hit this once a fourth trx-producing test run existed. Fixed with `-LiteralPath`. Unrelated to authorisation; fixed because it blocked getting a clean gate run at all.

- 2026-08-26 TASK-0002 reviewed by the orchestrator against §6 and moved back to `review`, not closed. `ScopeResolver` implements spec 4.2.1's four rules exactly and refuses a client-supplied arm id for pupil-scoped routes as 9.2 requires; `PrivilegeDeclarationGuard` is deliberately STRICTER than the deny-by-default fallback — a route relying on the fallback alone still fails the guard, because the fallback is a runtime backstop, not a declaration. Both are the right calls. What blocks closure is evidence, not design: the 38 integration tests that cover anonymous / wrong-privilege / out-of-scope / in-scope were never executed. Accepting the card on unit and architecture tests alone would accept the exact failure mode `backend-ci.yml` was written to prevent, and which ASSUMPTIONS.md §4 shows already caught three real defects that no unit test could.

- 2026-08-26 **TASK-0006: frontend client regenerated against contract `d229f8042c8c826e0b561deb31139e1fb332217e72e7c6d79d2eb66ecd658a79`** (unchanged from the hash already recorded in `## Contract` above — TASK-0002's promoted contract, first consumed here). `frontend/src/api/schema.d.ts` regenerated via `npm run generate:api`; `npm run check:api-drift` clean. The pre-regeneration diff matched exactly the documented delta: new `GET /api/v1/reference/arms/{armId}/secure` path + `SecureArmResponse` schema + `GetSecureArm` operation (401/403/429), and a new 429 on `whoami`. Contract-derived MSW handlers (`src/test/msw/openapi-handlers.ts`) picked up the new operation with no hand edit — the derivation walks the committed contract directly, no per-operation code exists to update. `ApiError` normalisation in `src/lib/http/http-error.ts` already mapped `403 -> 'forbidden'` before this card (nothing wire-specific about 403 needed adding); the contract now documents explicitly what the transport already handled generically. `npm run verify` green: typecheck, oxlint zero warnings, 124/124 tests, build. No dependency added; `package.json`/`package-lock.json` untouched by this card (their existing diff in the tree predates it, from TASK-0004's already-recorded dependency refresh). One non-blocking finding for a future card: the new `SecureArmResponse` schema has no `example` in the contract (every other schema does), so its derived MSW default handler returns 200 with an empty body rather than a shaped one — fine today since no test exercises the endpoint, but whichever card first writes tests against `/reference/arms/{armId}/secure` will need a `server.use()` override or a backend-side `example` added to that schema. Status left `review`, not `done` — orchestrator closes per this repo's convention (see TASK-0004).

- 2026-08-26 **TASK-0006 reopened and closed again: frontend client regenerated against contract `228ae57fd80c22f7e0511a665efce5d4722bb3280c66f5e3f4c96fff53795a65`**, superseding the `d229f804…` hash the previous entry recorded. Cause: the backend re-promoted after fixing the missing `SecureArmResponse` example (see the `~~SecureArmResponse~~ FIXED` Known-drift entry above). Confirmed drift red first; the diff was exactly the fix implied — `SecureArmResponse` gained an object-level `example` (`{"armId": "0192f0c4-7c3e-7a1b-9f2d-3b8e5a6c1d40"}`) and `armId` gained a property-level `example` of the same UUID — nothing else in the generated `schema.d.ts` moved. Ran `npm run generate:api`; `npm run check:api-drift` clean afterward. `npm run verify` green: typecheck, oxlint zero warnings, 124/124 tests, build.
  Verified the specific finding from the first pass: the contract-derived MSW handler for `GET /api/v1/reference/arms/{armId}/secure` now resolves a real body. Traced `openapi-handlers.ts`'s `exampleFor()` against the new contract directly (it follows the response schema's `$ref` to `components.schemas.SecureArmResponse.example`) and confirmed it now returns `{"armId":"0192f0c4-7c3e-7a1b-9f2d-3b8e5a6c1d40"}` instead of `undefined` — no derivation defect; it was reading the right place all along, the example just wasn't there yet. No source-mismatch found.
  Same constraints held: no dependency added, `package.json`/`package-lock.json` untouched by this pass, `src/lib/auth/` untouched, no UI shipped. Status left `review` — orchestrator closes.

- 2026-08-26 **Weekly report Parent's Comment resolved** (`25-open-conflicts-to-resolve.md` item 4, the last open conflict that changed what gets built). Confirmed as option A with an explicit addition: the class teacher transcribes what the parent wrote on the paper copy, **and the field is OPTIONAL — never required**. A teacher may leave any day's Parent's Comment empty, and an empty comment must never block saving a day panel, submitting a weekly report, or publishing one. No validation rule, no completeness gate, no warning banner treats it as missing data, because on paper most days genuinely have no parent comment. Option B is explicitly NOT built: there is no parent-facing write endpoint, the portal stays read-only, and the anonymous-write moderation / length-limit / abuse questions the conflicts file raises therefore do not arise. If option B is ever wanted it needs its own module and its own sign-off, per that file.
  NOTE: `product-specification/25-open-conflicts-to-resolve.md` still records item 4 as "Still open" in both its item body and its Current status table. The spec files are the human's authoritative document and the orchestrator has not edited them; this ledger entry is the decision of record until they are reconciled.

- 2026-08-26 **TASK-0009 implemented, moved to `review`.** The eight document-property assertions
  (`EverySchema_HasAnExample` and its seven neighbours, exact list in the card) moved verbatim from
  `SchoolManagement.IntegrationTests/OpenApiContractTests.cs` (deleted — nothing else in that file needed
  a database) to a new `SchoolManagement.ArchitectureTests/OpenApiContractTests.cs`, reading
  `backend/artifacts/openapi/SchoolManagement.Api.json` directly via a new `OpenApiDocument` static
  loader instead of starting the app under `WebApplicationFactory`. `backend/scripts/ci.ps1` gained one
  new gate, "Generate OpenAPI document (no database)", between Format and Tests, so that artefact exists
  before `dotnet test` runs; the existing "OpenAPI contract drift" gate no longer regenerates it a second
  time, it reuses that same file — still exactly one call site of `generate-openapi.ps1` in the script.
  `.github/workflows/backend-ci.yml`'s skip-derivation guard (`total - passed - failed` per trx) needed no
  change — verified, not touched, still root-owned.
  Both proof criteria demonstrated live on this machine (no `POSTGRES_TEST_CONNECTION`, no Docker/Podman):
  the 8 tests ran and passed (0 skipped) reading the no-database artefact; with `SecureArmResponse`'s
  example temporarily removed from `OpenApiExamples.cs` and the document regenerated WITHOUT `-Promote`,
  `EverySchema_HasAnExample` failed with the expected message, then passed again once restored — the
  source file was diff-checked back to byte-identical before rebuilding. `-Promote` was never run while
  the example was missing. Committed contract hash confirmed unchanged at
  `228ae57fd80c22f7e0511a665efce5d4722bb3280c66f5e3f4c96fff53795a65` before, during, and after. Full
  `ci.ps1 -Configuration Release` run (no database) green apart from the pre-existing SSH.NET
  "Vulnerable dependencies" gate (TASK-0007's, untouched) — `ArchitectureTests` 24/24 passed (16
  pre-existing + these 8), `IntegrationTests` 0/30 all skipped as designed (was 38 before this card; the
  8 moved tests account for the difference, everything else there still genuinely needs a database and
  was checked individually, not moved by file), `UnitTests` 124/124 passed. Full command output and
  per-test names are in the card's Log. Left for the orchestrator to raise if wanted: making the
  remaining integration tests fail-rather-than-skip when no database is reachable — explicitly out of
  scope for this card, arguably TASK-0008's territory.

- 2026-08-26 ~~OpenAPI generation is not deterministic between incremental and clean builds.~~ (ORCHESTRATOR OVERSTATED THIS — the real trigger is narrower: a document modified outside the generation target. Ordinary pulls and branch switches self-heal. See TASK-0010's closing Log.) **FIXED
  by TASK-0010, moved to `review`.** Root cause found by reading
  `Microsoft.Extensions.ApiDescription.Server`'s own `.targets`: the `GenerateOpenApiDocuments` MSBuild
  target is `Inputs="$(TargetPath)" Outputs="$(_OpenApiDocumentsCache)"` — an up-to-date check between
  the compiled assembly and one bookkeeping file in `obj/` (`SchoolManagement.Api.OpenApiFiles.cache`).
  It never inspects the generated document itself, so if the document is modified by anything other
  than that target running — a hand-edit was the reproduced case — while the assembly and cache file
  stay as they were, the target is (correctly, by its own contract) skipped, and the modified document
  survives untouched while `generate-openapi.ps1` still prints `==> Generated:`.
  **Checked the honest caveat before sizing the fix**: a real edit to `OpenApiExamples.cs` (even a
  no-op comment) reliably self-heals a corrupted artefact on the next incremental run, because a real
  source change advances the assembly's timestamp past the cache file's and forces the target to rerun
  — so branch switches and normal source edits are not a trigger. `ci.ps1`'s two builds (plain Build
  gate, then the flagged Generate gate) do not interfere either — traced and confirmed live: the cache
  file doesn't exist yet when the first flagged build of a run starts, so that target always runs
  unconditionally the first time, every time. **The only trigger found was a document changed outside
  the generation target** — hand-edit reproduced exactly per TASK-0009's original repro table
  (25807 bytes / sha `6a5d51fe...` on corruption, `228ae57f...` restored); an interrupted build is a
  plausible sibling of the same class, not separately reproduced, and the fix covers it regardless.
  **Fix**: `backend/scripts/generate-openapi.ps1` now deletes both the generated document and
  `src/SchoolManagement.Api/obj/SchoolManagement.Api.OpenApiFiles.cache` before every `dotnet build`
  call. Deleting the cache file is what matters — it is the target's actual tracked `Outputs`, so
  removing it makes regeneration unconditional every run. Deleting the artefact alone was tested and
  proven insufficient first: the target still skips on the cache-file check alone, so no document gets
  written and the script's own `Test-Path` guard throws — which is what led to targeting the cache file
  instead. Verified fixed with the identical hand-edit-then-incremental-build sequence:
  now yields `228ae57f...` (matching the committed contract) even without touching `obj`/`bin`, and
  twice in a row to confirm it isn't a one-off. `-Promote` never run while verifying; committed contract
  hash unchanged throughout. Full findings, evidence and the `ci.ps1` gate output are in TASK-0010's Log.

---

## Archived from STATE.md `## Decisions` on 2026-08-27

Moved verbatim to keep STATE.md under the ~200-line cap in CLAUDE.md §4.1. Append-only —
these remain in force. The task card named in each entry holds the full reasoning.

- 2026-08-26 Product scope resolved (OQ7): `product-specification/` rev 3.1 is authoritative; cards cite spec sections rather than restating them, so a spec revision cannot silently invalidate a card.
- 2026-08-26 Package manager **npm** (OQ3) — already in use, lockfile committed, pnpm absent.
- 2026-08-26 Client generator **openapi-typescript, types only** (OQ6) — the frontend already owns a working axios transport with auth interceptors; a runtime client would duplicate it and force a rewrite of that seam. TASK-0004.
- 2026-08-26 Sequencing departs from `index.md` once: authorisation infrastructure (TASK-0002) precedes settings, because spec 9.2 makes an undeclared route a boot failure and settings endpoints would otherwise be written against an enforcement layer that does not exist.
- 2026-08-26 **Auth carrier: HttpOnly cookie + CSRF**, human sign-off per §5. Rejected bearer-in-memory: readable by any XSS, and dies on reload so it must either sign the user out or be persisted — the exposure it was avoiding. Token model unchanged (spec 9.1 opaque, hashed, rotated). TASK-0003.
- 2026-08-26 Spec conflicts items 1/2/5 resolved by the school; 3/6/7/8/9 built to their provisional resolutions. No subagent may amend one — raise it in Open questions.
- 2026-08-26 TASK-0004 closed: contract pipeline wired end to end — generate, drift-check, typed helper over the existing transport, contract-derived MSW handlers, HTTP-boundary test.
- 2026-08-26 Frontend dependency refresh accepted **after the fact**: closing TASK-0004 regenerated the lockfile wholesale, sweeping 73 versions beyond scope. Accepted — all inside declared caret ranges, gates green, `npm audit` 0 vulnerabilities where the old tree carried a high-severity `nanoid` advisory. Process was wrong, result was not; must land as its own commit (§8).
- 2026-08-26 Frontend CI workflow written (orchestrator-owned per §11): path-filtered, `npm ci` never `npm install`, Node pinned to 22.21.0 to match the dev machine, drift check as its own final step so a stale client reads differently from a broken frontend.
- 2026-08-26 `/api/v1/reference/whoami` made explicitly `.AllowAnonymous()` rather than inventing a privilege for it, per TASK-0002's own instruction. Response shape unchanged.
- 2026-08-26 New reference-slice endpoint `GET /api/v1/reference/arms/{armId}/secure` proves the privilege substrate end to end. Scaffolding, not product — deleted with the rest of the reference slice when real endpoints land.
- 2026-08-26 Fixed a latent `ci.ps1` coverage-gate bug found in passing (`-LiteralPath`), unrelated to authorisation but blocking any clean gate run.
- 2026-08-26 **TASK-0002 closed** on a verified live run: 178 tests, Skipped 0, coverage 87.00%/65.58%, drift clean. Closed with ONE gate knowingly red (SSH.NET, TASK-0007) as an explicit recorded exception, not an unnoticed one.
- 2026-08-26 TASK-0002 was first reported done twice on runs where its integration suite SKIPPED; run for real it failed on a defect already in the committed contract. **A skipped suite is not a passing suite**, and the coverage floor deliberately stands down when anything skipped — so a skipping run degrades two gates at once while looking green. TASK-0009 removed the checks that never needed a database from that blast radius.
- 2026-08-26 TASK-0006 closed twice — once per contract move (`d229f804`, then `228ae57f`). The client regenerates in one command; the drift gate caught staleness on its own both times.
- 2026-08-26 **Weekly Parent's Comment resolved** (conflict item 4, last one affecting what gets built): teacher transcribes, portal read-only, and the field is **optional — never required**. No validation, completeness gate or warning may treat an empty comment as missing data. Option B (parent-facing write) explicitly not built.

## Archived from STATE.md `## Decisions` on 2026-09-04 (token-efficiency restructure)

Full text of the 2026-08-27 entries. `STATE.md` keeps a one-line index pointing here.

- 2026-08-27 **TASK-0010 closed.** OpenAPI generation could report `==> Generated:` over a document it had not written: the MSBuild target tracks `obj/…OpenApiFiles.cache`, not the JSON. Fixed by deleting the cache. The orchestrator's original "incremental vs clean builds diverge" framing was **overstated** — ordinary pulls and branch switches self-heal; the only confirmed trigger is a document modified outside the target.
- 2026-08-27 TASK-0007 sent to `review`: SSH.NET High advisory cleared by Option 1 (`Testcontainers.PostgreSql` 4.13.0 → 4.14.0), first option tried. Vulnerable-dependency gate verified green (offline check, no database needed). Full `ci.ps1` run otherwise SKIPPED — no Docker, no `POSTGRES_TEST_CONNECTION` in that session — so integration tests (30 skipped) and the coverage floor are unverified this pass, per the standing lesson that a skipping run must not be reported as a pass. Needs the database re-run before closing.
- 2026-08-27 TASK-0008 sent to `review`. `backend/scripts/local-env.template.ps1` (committed) resolves `POSTGRES_TEST_CONNECTION` from four sources in order — already-set env var → `$env:GRAS_LOCAL_ENV_FILE` → `$HOME/.gras/pg-test.txt` (recommended, outside the repo) → an inline slot in the developer's own copy, left empty in the template per the `appsettings.Development.template.json` precedent (no realistic-looking fake credential) — then runs a gate (`-Gate`, default `ci.ps1`). `backend/scripts/local-env.ps1` is the real, git-ignored copy; it never embeds a credential as static text since the inline slot stays empty and it resolves via source 3. `git check-ignore -v` proved the real script is ignored (exact-path rule, no wildcard) and the template is not, per the card's own warning that this is the easiest thing to get wrong. `gitleaks` (8.30.1) installed via `winget`, no admin; `ci.ps1`'s Secret scan gate now actually executes. Demonstrated the template doesn't trip it and a filled-in copy (obviously-fake demo value, never staged) does. Full detail and gate output: TASK-0008's Log.
- 2026-08-27 **gitleaks running for real, for the first time anywhere (TASK-0008), surfaces 9 pre-existing non-secret findings** it had never actually been run against before (it was uninstalled locally, so `ci.ps1` always warned-and-skipped; CI installs it but had never been exercised against this history either as far as this session could verify). All 9 are outside TASK-0008's file scope — `backend/README.md`, `appsettings.Development.template.json`, `DesignTimeDbContextFactory.cs`, `backend/ci/github-actions-backend.yml`, `.agent/STATE.md`, `contracts/CONTRACT.lock` — and `gitleaks detect` scans git history by default, so editing current file content would not clear it regardless. All are documented placeholders or content hashes, not real secrets. Recorded in `backend/docs/ASSUMPTIONS.md` §3.9 with three options (targeted `.gitleaks.toml` allowlist entries / scope the scan to `--no-git` / accept as a permanent characteristic of a history-scanning tool), none decided here — needs an owning task card or explicit sign-off, the same shape as the SSH.NET drift entry above.
- 2026-08-27 **TASK-0007 and TASK-0008 both reviewed and both held at `blocked`, neither closed.** Their own deliverables are complete and were verified independently by the orchestrator (one-line dependency bump with the vuln scan re-run clean across seven projects; `git check-ignore -v` proving the real script ignored and the template not). What stops both is external: the test role lacks `CREATE` on schema `public` (Open question 10, needs a human `GRANT`), so the integration suite fails 30/30 instead of passing. §10 does not let a card close on a red gate, and the standing lesson here is that a suite which does not pass is not a pass — this time it fails loudly rather than skipping quietly, which is an improvement in visibility, not in evidence.
- 2026-08-27 **TASK-0011 created, not dispatched** — owns the nine secret-scan findings TASK-0008 revealed. All nine inspected by the orchestrator: five are placeholder connection strings (`YOUR_PASSWORD`, `...`, `<pw>`) tripping an over-broad rule that had never executed before today, four are the SHA-256 contract hash read as an API key. No real credential among them. Four of the nine exist only in git history, so no edit to current files can clear them. Awaiting a human call on whether narrowing the rule with stopwords is acceptable, since TASK-0008's own text calls an allowlist "a permanent hole".
- 2026-08-27 **TASK-0011 implemented, sent to `review`.** Human approved narrowing the rule (no baseline). While building the fix, found `postgres-connection-string-with-password`'s regex has one capture group `(host|server)` (meant only to accept both keywords) — with no `secretGroup` set, gitleaks uses "first non-empty capture group" as `finding.Secret`, so every finding's Secret was literally the word `"Host"`/`"Server"`, never the connection string. Since `stopwords` (and the default allowlist target) always check against `Secret`, a Secret-scoped `stopwords` list — the card's first-listed option — **could not have worked for this rule**; confirmed by reading gitleaks' own source, not assumed. Same reason the file's *pre-existing* global allowlist entries (`ci_user`, `ci_password`, `Username=<user>;Password=<pw>`, `Password=\.\.\.`, present since 2026-08-03) had never actually suppressed anything for this rule either. Fix used the card's other option instead: `[[allowlists]]` blocks with `targetRules` (Family A: `postgres-connection-string-with-password`, `regexTarget = "match"`, one regex per literal placeholder token; Family B: `generic-api-key`, `paths` anchored to `contracts/CONTRACT.lock` and `.agent/STATE.md`), both rule-scoped not path-scoped for A, chosen path-scope for B over forking a default rule we don't own. The file's one legacy `[allowlist]` table was converted to `[[allowlists]]` (identical behaviour) because gitleaks refuses to load both forms mixed and `targetRules` needs the plural form. Verified against a real leak: a realistic fake connection string (fake host, non-placeholder password) written to a scratch file outside the repo still trips the rule (`leaks found: 1`), then deleted; the real gate (`gitleaks detect --source . --config .gitleaks.toml --redact --no-banner`, from `backend/`) then reports `6 commits scanned` / `no leaks found` / exit 0. Full `./backend/scripts/local-env.ps1` run: Restore/Build/Format/OpenAPI-gen/Coverage/Vulnerable-deps/**Secret scan**/OpenAPI-drift all green (contract hash unchanged, `228ae57f...`); unit 124/124 and architecture 24/24 pass; integration 30/30 FAIL, every one `Npgsql 42501: permission denied for schema public` — Open question 10, external, unrelated to this change, not fixed here. Detail and full pasted output: TASK-0011's Log.
- 2026-08-27 **TASK-0007, TASK-0008 and TASK-0011 all closed on one verified live run.** `ALL GATES PASSED` with the integration suite genuinely executing — `Failed: 0, Passed: 30, Skipped: 0` — coverage enforced on a complete run (77.18% line / 58.61% branch), the secret scan actually scanning (`6 commits scanned` -> `no leaks found`), vulnerable-package scan clean across seven projects, and contract drift clean at `228ae57f`. All four §4.4 drift checks run by the orchestrator, including the two frontend-side ones `ci.ps1` does not cover (client drift: no drift; no raw `fetch`/`axios` outside the generated client). Held all three at `blocked` through three separate agent reports rather than closing any on a red Tests gate — the gate went green because a database privilege was granted, not because the bar moved.
- 2026-08-27 The gate suite is now honest end to end for the first time: before today the vulnerable-dependency gate was red (TASK-0007), the secret scan printed `PASS` while scanning nothing (TASK-0008 installed gitleaks; TASK-0011 fixed what that revealed), and the integration suite could skip silently while degrading the coverage floor with it (TASK-0008's `local-env.ps1` removes the re-typing that caused it). Three gates that could previously report green without evidence now cannot.
- 2026-08-27 **Test-database credential split confirmed deliberate** — a dedicated least-privilege role, not `neondb_owner`, now backs `POSTGRES_TEST_CONNECTION`. Closes the 2026-08-26 finding that the test credential WAS the staging credential. It cost one cycle to discover, because the new role lacked `CREATE` on schema `public` and the EF fixture runs migrations — worth knowing for the next environment: a test role needs DDL rights on its own database, and Postgres 15+ does not grant them by default.
- 2026-08-27 **TASK-0001 closed. Both scaffolds audited rule by rule and both materially exceed spec** — `.agent/AUDIT.md` rewritten from scratch, superseding the 2026-07-27 version written against empty directories. Run as two parallel read-only audits (no `reviewer` agent is configured in the roster, so both ran as `general-purpose` under a write-nothing brief). Backend §6: enforcement is mechanical rather than aspirational — 24 architecture tests, `AnalysisLevel latest-All` with warnings as errors, CA1848/CA1849/CA2016 promoted to errors, deny-by-default authorization with a boot-time route guard. Frontend §7: zero `any`, zero `@ts-ignore`, zero non-null `!`, and 35 of oxlint's 36 `jsx-a11y` rules firing as errors (measured, not assumed from the config comment). **Two blockers**, both verified by the orchestrator before carding: the problem schemas forbid the `errorCode`/`traceId` every error response carries and the defect has already reached the generated client (TASK-0012, blocks TASK-0003's frontend half); and `Idempotency-Key` appears nowhere despite §6 and spec 9.8.2 (TASK-0013, decide-now not fix-now). Eight should-fixes carded as TASK-0014 (frontend) and TASK-0015 (backend). Three ledger claims corrected as factually wrong, including "obj/ is tracked" — it is not, and never was.

## Closure notes and process lessons, moved out of STATE.md `## In flight` on 2026-09-04

**TASK-0002 closed 2026-08-26** after a verified live re-run: 178 tests, Skipped: 0, coverage
87.00% line / 65.58% branch, contract drift clean, hash matching the lock. Closed with one gate
knowingly red — the SSH.NET advisory, owned by TASK-0007 — recorded as an explicit exception.
**TASK-0006 is reopened**, not superseded: its purpose is "the client matches TASK-0002's
contract", and the reopen fix moved that contract from `d229f804` to `228ae57f`. One more
regeneration closes it.
**The lesson, kept because it cost real time:** TASK-0002 was reported done twice on runs where the
integration suite skipped. Run for real it failed, on a defect that had already reached the
committed contract. A skipped suite is not a passing suite, and the coverage floor is deliberately
not enforced when anything skipped — so a skipping run degrades two gates at once while looking
green. TASK-0009 moves the checks that never needed a database out of that blast radius.
Full sequence and the cards not yet written: `.agent/ROADMAP.md`.

## Resolved open questions, moved out of STATE.md `## Open questions` on 2026-09-04

All of these were answered and are no longer consulted. Live questions stay in `STATE.md`.

1. ~~Greenfield build?~~ RESOLVED 2026-08-26 — yes; spec supplied, both scaffolds stand.
2. ~~Initialise git?~~ RESOLVED 2026-08-08.
3. ~~Frontend package manager?~~ RESOLVED 2026-08-26 — npm.
4. ~~Docker unavailable?~~ RESOLVED 2026-08-08 — hosted Neon Postgres for integration tests.
6. ~~Client generator?~~ RESOLVED 2026-08-26 — openapi-typescript, types only.
7. ~~Product scope?~~ RESOLVED 2026-08-26 — `product-specification/`.
8. ~~Auth carrier?~~ RESOLVED 2026-08-26 — HttpOnly cookie session + CSRF token, human
   sign-off. See `## Auth decision`. TASK-0003 unblocked.
9. ~~Weekly report Parent's Comment?~~ RESOLVED 2026-08-26 by the human. See `## Decisions`.
10. **The integration-test role lacks `CREATE` on schema `public` in `schoolmanagement_tests`.**
    A dedicated least-privilege role now backs `POSTGRES_TEST_CONNECTION` — it is **not**
    `neondb_owner`, which if intentional resolves the shared staging/test credential concern
    recorded in `## Known drift` (2026-08-26). The EF fixture runs migrations, so it needs DDL
    rights on that database: all 30 integration tests now fail at collection-fixture init with
    `Npgsql 42501: permission denied for schema public` (Postgres 15+ no longer grants schema
    `public` CREATE to PUBLIC). Fix is one `GRANT` run by an owner role against
    `schoolmanagement_tests`; SQL prepared for the human outside the repo with the role name
    substituted. **Blocks TASK-0007's close and TASK-0008's verification.** Raised 2026-08-27.
    **RESOLVED 2026-08-27** — the human ran the grant against `schoolmanagement_tests`. Verified by
    a full `ci.ps1` run: integration suite `Failed: 0, Passed: 30, Skipped: 0`, coverage enforced on
    a complete run at 77.18% line / 58.61% branch, ALL GATES PASSED. Unblocked TASK-0007, TASK-0008
    and TASK-0011, all three closed on that run.

## Why CLAUDE.md §13 exists — rationale, measured 2026-09-04

§13 in the root CLAUDE.md is deliberately terse, because it is loaded by every agent session and
a section about saving tokens that costs 900 tokens per dispatch is self-defeating. The reasoning
behind each of its rules is here instead.

**The measurement that prompted it.** Before the restructure, the fixed cost of one `frontend-dev`
dispatch — loaded before it read a single line of application code — was about 25,000 tokens:
CLAUDE.md 18.5 KB, `STATE.md` 36.3 KB, `contracts/openapi.json` 25 KB read whole for a document
describing four endpoints, `frontend/CONVENTIONS.md` 13.9 KB, and a task card averaging 7 KB.
That cost was paid again in full on every re-dispatch after a failed gate. After the restructure
the same dispatch loads about 9,500 tokens.

**Why the old ledger cap failed.** §4.1 capped `STATE.md` at "~200 lines". The file sat at exactly
200 lines and reported green while carrying 36.3 KB, because `## Decisions` held 10.3 KB in 18
lines and `## Known drift` held 14.7 KB in 27 lines — an average of 550 bytes per nominally
one-line entry, with one line reaching 2,345 bytes. A line cap does not bound cost. The cap is
now on bytes, and the sections are indexes pointing at full text in `decisions/` and `drift/`.

**Why lazy loading rather than deletion.** Nothing was deleted in the 2026-09-04 restructure; all
19 drift entries, 11 decision entries, the closure narrative, the nine resolved open questions and
eight task-card logs were moved to archives and indexed. Quality is not protected by an agent
having read everything — it is protected by §4.4's mechanical drift checks, which are unchanged.
Small contexts are safe precisely because the gates, not an agent's memory, catch mistakes.

**Why a skipped suite is the most expensive defect on record.** TASK-0002 was reported done twice
on runs whose integration suite silently skipped; run for real it failed, on a defect that had
already reached the committed contract. Each false close cost a full re-dispatch of a Sonnet
session plus an orchestrator review. No file-size saving in this repository comes close. Hence the
rule, and hence TASK-0016, which makes `ci.ps1` exit non-zero on `Failed > 0` and on `Skipped > 0`.

**Why §6 and §7 moved but kept their numbers.** 142 references to "§6" and "§7" exist across 24
files. Renaming them was rejected as a large, low-value, error-prone diff. The bodies moved to
`.agent/spec/backend.md` and `.agent/spec/frontend.md`; §6 and §7 remain in CLAUDE.md as pointer
stubs, so every existing reference still resolves. `backend/AGENTS.md` and
`frontend/CONVENTIONS.md` were checked for duplication and found to be concrete recipes
implementing the specs rather than restatements of them, so both were left in place.

## 2026-09-04 — TASK-0012 and TASK-0017 closure notes

**TASK-0012 — problem-detail extension members.** The contract declared `additionalProperties: false`
on both problem schemas while every error response carried `errorCode` and `traceId`, so each
schema's own `example` was invalid against the schema illustrating it, and a five-member
`ProblemDetails` had already reached `frontend/src/api/schema.d.ts`. Both members are now declared
with the schemas left CLOSED — declaring them inside `additionalProperties: false` keeps the
closed-type discipline that is right for request bodies while making the two members clients are
told to depend on part of the contract. `UnmappedMemberHandling.Disallow` was left global; the fix
is a targeted `IOpenApiSchemaTransformer`.

**The `required` decision, corrected mid-card.** The orchestrator initially told the human both
members would be `required`, then read the code and found that false for `errorCode`: `traceId`
comes from the single central `CustomizeProblemDetails` hook (`Program.cs`), so it is on every
problem response including framework-produced ones, but `errorCode` is attached only in
`Http/ResultExtensions.cs` and `Http/GlobalExceptionHandler.cs` — a model-binding 400 or a 401 from
authentication middleware carries none. Marking it required would have declared something the API
does not always send, i.e. the same class of contract lie the card existed to remove. Shipped as
`traceId` required, `errorCode` optional with its absence documented in the schema description.
Consequence worth remembering at TASK-0003: every frontend error branch needs an
`errorCode`-absent fallback, because middleware 401s are exactly that case, and one of the two new
frontend tests pins it.

**A latent bug the card did not predict.** `frontend/src/lib/http/http-error.ts` held a
hand-duplicated `ProblemDetails` interface, and `ApiError.code` was sourced from `problem.type` —
the RFC type URN, not the error code — so any future branch on `.code` would have matched the wrong
field entirely. Now a type alias of the generated schema, with `ApiError.errorCode`/`traceId`. This
is the argument for §3's "one source of truth" in miniature: the duplicate drifted silently because
nothing compared it to the contract.

**Two new contract tests, one of them general.**
`EverySchemaExample_ValidatesAgainstItsOwnSchema` checks every schema in the document, not just the
two: under `additionalProperties: false` no example may carry an undeclared member, and every
required property must appear. `EveryOperation_DocumentsItsErrorResponses` had not caught this. The
new tests were proven real by running them against the pre-fix document and watching them fail.

**TASK-0017 — why it existed at all.** Not a security finding. TASK-0011 proved its narrowed
gitleaks rule still fired by quoting the fake connection string it had tested with into its own
card, its run log, and `ASSUMPTIONS.md`; committing that write-up re-armed the rule against its own
documentation. Full mechanics, the rejected alternatives, and the two silent failure modes are in
the `## Known drift` archive entry for 2026-09-04 and at the head of `backend/.gitleaksignore`.

**Escalation calibration, recorded because it changed how the orchestrator works.** The orchestrator
escalated the TASK-0017 mechanism choice to the human on the strength of the 2026-08-27 precedent
(the human signed off on narrowing the gitleaks rule). The human pushed back: the precedent was
about weakening a detection rule's *scope*, a real judgment call, whereas suppressing five
identified fake strings is mechanical. Standing line drawn: mechanical gate hygiene, lockfile and
fingerprint maintenance, and choices differing only in maintenance cost are decided by the
orchestrator and reported. Contract-breaking changes (§3), session/auth decisions (§5), and anything
widening what a detection rule cannot see still go to the human.

**An orchestrator error worth not repeating.** While editing TASK-0017's card, a `python -c` invoked
under double quotes let bash run its backticked segments as command substitution, one of which
executed the developer's out-of-repo credential file and echoed part of it into the session
transcript. Nothing reached any file — verified by grepping the card for host, pooler and password
patterns — and nothing was committed, but the credential is in that transcript. Reported to the
human immediately with rotation offered. Rule: heredoc with a quoted delimiter for any multi-line
content containing backticks; never `python -c "..."` with prose in it.

- 2026-09-04 **TASK-0016 closed — the gate suite now fails fast and fails honestly.** `ci.ps1` runs
  restore → format → build → generate-OpenAPI → unit+architecture tests → integration tests →
  coverage floor → vulnerable packages → gitleaks → contract drift, stopping at the first failure
  unless `-NoFailFast` is passed (which `.github/workflows/backend-ci.yml` now does, because CI
  wants the whole picture from one run). Two exit conditions added: `Failed > 0` and, unless
  `-AllowSkipped` is passed, `Skipped > 0`. A fixed `SUMMARY` block ends every run — one line per
  gate that ran, plus `Tests: total=/passed=/failed=/skipped=` and `Coverage: line=/branch=` — so
  §9 can be satisfied by pasting that block and nothing else. The trx parsing and the pass/fail
  verdict moved to `backend/scripts/lib/gate-summary.ps1`, dot-sourced by `ci.ps1` and driven
  directly by `backend/scripts/tests/gate-summary.tests.ps1` (19 assertions, dependency-free
  harness — this machine has only Pester 3.4.0 and no `pwsh`, so taking a Pester dependency was
  refused). This closes the 2026-08-27 drift entry: the coverage gate printed `PASS` at 60.48% on a
  run where 30 integration tests FAILED, and that exact run is now a fixture that asserts a
  non-zero exit. Verified by the orchestrator rather than on report: full Postgres run
  `total=180 passed=180 failed=0 skipped=0`, 75.57% line / 57.60% branch, ten gates PASS, exit 0;
  no-database run exits 1 at the coverage gate with the three gates after it never running; the
  same run with `-AllowSkipped` exits 0. Frontend `verify` already held the ordering and `&&`
  fail-fast property, so it was deliberately NOT rewritten — the deliverable there is
  `frontend/src/test/gate-scripts.test.ts`, which pins the exact chain, asserts `check:api-drift`
  stays outside it, and observes real short-circuiting in a child process (129 tests, was 126).
- 2026-09-04 **Two defects in TASK-0016's own work were hidden by its test fixtures, and both were
  caught in review rather than by the tests.** First: the skip message named the trx FILE
  (`HP_DESKTOP-GPLBVV9_2026-09-04_14_12_56.trx` — machine, user, timestamp), not the suite, so the
  "print which suite skipped" criterion was unmet; the self-test asserted it and passed anyway
  because the fixtures were named `IntegrationTests.trx` and `UnitTests.trx`, filenames VSTest
  never produces. Fixed by deriving the suite from `TestRun.TestDefinitions.UnitTest[0].storage`
  and renaming the fixtures to realistic `<user>_<machine>_<timestamp>` form. Second, found by
  backend-dev while fixing the first: **`ci.ps1 -AllowSkipped` never worked at all.**
  `gate-summary.ps1` declared its own `param([switch]$AllowSkipped)` for script mode, and
  dot-sourcing a script binds its `param()` block in the CALLER's scope — so
  `. lib/gate-summary.ps1` silently reset `ci.ps1`'s `-AllowSkipped` to `$false` on every run,
  whatever the user passed. Fixed by removing the `param()` block and hand-parsing `$args` in
  script mode only. The lesson is the card's own thesis turned on itself: a fixture chosen for
  readability made a failing criterion look met, which is the same shape as a green result that
  stopped meaning anything. Fixtures must reproduce the real artefact's shape, names included.

## 2026-09-04 — TASK-0014 closure note

- 2026-09-04 **TASK-0014 CLOSED** — the five §7 audit should-fixes (S4-S7 plus the test-count
  correction), all in code or docs the first feature card copies.
  **S4, the reason the card existed:** `src/features/README.md` and `src/api/README.md` both
  documented a route around the generated client — the features README's worked example
  hand-typed `IStudentListItem` against a hardcoded `/api/v1/Students`, and the api README
  sanctioned the plain `@/lib/http` verb helpers "for anything not yet worth a typed wrapper".
  Both rewritten so `apiGet`/`apiPost` is the only sanctioned route; the general permission is
  replaced by a specific instruction (add `apiPut`/`apiPatch`/`apiDelete` to `client.ts` first,
  since only GET and POST are wired). The worked example now uses the one path the contract
  actually declares, `/api/v1/reference/records`, with types indexed off
  `components['schemas']` so they cannot drift — and it flags that the `Reference` tag is test
  scaffolding, not a product tag, so it is not mistaken for a real feature folder.
  **S5:** `lint` is `oxlint --max-warnings=0`, making the `suspicious` category and five named
  rules enforceable rather than aspirational. Zero diagnostics surfaced, matching TASK-0001's
  measurement — no mass-fix.
  **S6:** `httpClient` no longer re-exported from `src/lib/http/index.ts`, and
  `http-boundary.test.ts` gained a case failing on a `httpClient` import from outside
  `src/lib/http/` — enforced, not merely tidied.
  **S7:** the catch-all route's element is wrapped in `ErrorBoundary`, making
  `app-router.tsx`'s own doc comment true rather than false.
  **Test count:** HANDOFF.md claimed 120, STATE.md 129; the real number is **130 across 15
  files**, the extra one being this card's own new boundary case. Both corrected.
  **Orchestrator verification** went past the report on one point, because README code is not
  typechecked and this card exists to stop a template teaching something wrong: the rewritten
  example's `apiGet(path, params, options?)` / `apiPost(path, body, options?)` shape matches
  `src/api/client.ts:59,79` exactly, and `SampleRecordDto`, `CreateSampleRecordCommand` and
  `PagedResultOfSampleRecordDto` all exist in `schema.d.ts` (:325, :105, :204). Gates re-run by
  the orchestrator rather than accepted on report, since the agent's paste was reformatted
  rather than verbatim: verify green with 130/130 and 0 skipped, api-drift clean.
  **Accepted limitation, deliberately not widened:** the new boundary regex matches named
  imports only. `http-client.ts` exports `httpClient` as a named export only, so a default or
  namespace import is a form the codebase cannot currently produce. Recorded, not carded.
  S8 (`src/shared/`) untouched — Open question 11 still needs a human call.

## 2026-09-04 — TASK-0013 closure note

- 2026-09-04 **TASK-0013 CLOSED — idempotency deferred with a binding record, not a shrug.**
  Option B, human sign-off the same day: do not build the mechanism, land a record that will
  actually fire. Two files: `backend/docs/ASSUMPTIONS.md` §2.14 (new, marked DEVIATION in the
  §2.2 style) and a `TODO(TASK-0013)` in `ReferenceEndpoints.cs` — the class `<remarks>`, plus a
  `//` note in the `MapCreateSampleRecord` handler body because that method is what gets copied.
  Zero behaviour change; no filter, no middleware, no key store, no package, no migration.
  **The actual deliverable was closing a teaching gap, not a functional one.** `ReferenceEndpoints`
  is the declared copy-from template and its doc comment enumerated "the same five things every
  endpoint below demonstrates" without idempotency; every future POST copied from it inherited the
  omission silently. The record deliberately does NOT add idempotency as a sixth item on that list,
  because it is not yet true of anything in the file — it is a labelled TODO instead, which is the
  honest shape. Owner `backend-dev`; orchestrator accountable for firing the trigger at dispatch.
  Trigger written so it cannot be read narrowly: the first card implementing **any** retry-duplicable
  mutation builds the mechanism first, §9.8.2's four named operations being examples rather than an
  exhaustive list. Drift entry with the full reasoning: `drift/2026-Q3.md`, 2026-09-04 entry.
  **Contract deliberately did not move** — `b287da03…` byte-identical before and after, which was an
  acceptance criterion rather than a nicety: XML doc comments flow into the generated OpenAPI
  document automatically in .NET 10 (`ASSUMPTIONS.md` §5), so a doc-comment TODO placed in an
  operation's `<summary>`/`<remarks>` or in a `.WithDescription(...)` would have silently rewritten
  the contract on a documentation-only card. Placement in the class-level `<remarks>` of an
  `IEndpointModule` and in a handler body avoids it. Verified twice — by `ci.ps1`'s drift gate and
  independently by `contract-guardian` running all four §4.4 checks. **Generalises: on this stack a
  comment can be contract content, so a "documentation-only" backend diff is not automatically
  contract-safe and must still be hash-checked.**
  Gates: all ten green live, `total=180 passed=180 failed=0 skipped=0`, line coverage 75.61%,
  integration suite 30/30 actually run. §4.4 checks 1-4 all PASS, `frontend/src/api/` and
  `contracts/` untouched in `git status`, no `GENERATED` header touched.

## 2026-09-04 — TASK-0015 closure note

- 2026-09-04 **TASK-0015 CLOSED — §6 audit S1-S3 (backend scaffold conformance), Phase 0b now
  complete.**
  **S1** (`/health/ready` anonymous, unthrottled, hits the database): resolved with a dedicated
  `RateLimitingOptions.HealthPolicyName` fixed-window policy, `RequireRateLimiting`'d onto
  `/health/ready` only — `/health/live` stays unthrottled since it runs no dependency checks and
  amplifies nothing. Numbers: `HealthPermitLimit=120`, `HealthWindowSeconds=60` (2 req/s
  sustained), assuming a 5-second probe-interval floor (tightest of the common defaults —
  Kubernetes' own default is 10s, its allowed minimum 1s; AWS ALB/NLB's minimum is 5s; Docker's
  default 30s) with up to 3 independent probers sharing one partition key (a load balancer, an
  orchestrator's kubelet, and an external uptime monitor behind the same NAT/egress). Worst case
  at that floor is 36 req/min; 120 leaves >3x headroom so no legitimate cadence can ever trip it,
  while still capping an anonymous DB-touching endpoint far below "unlimited". Per the 2026-09-04
  card log steer, this is the app-level limiter — no ingress claim made; Open question 5
  (deployment target) stays open and undecided. Raised in Development
  (`appsettings.Development.template.json`) and in the integration test fixture, mirroring the
  existing `PermitLimit` override pattern, to avoid test flakiness.
  **S2** (Api importing `SchoolManagement.Infrastructure.Persistence` inside the HTTP pipeline):
  new Application port `IPersistenceErrorTranslator`
  (`Abstractions/Persistence/IPersistenceErrorTranslator.cs`), implemented by
  `Infrastructure.Persistence.PersistenceErrorTranslator` (a thin adapter delegating to the
  existing static `PersistenceErrors.TryTranslate` — that method is unchanged and stays
  independently unit-testable). Registered as a singleton (stateless; `GlobalExceptionHandler`,
  its only consumer, is itself a framework-registered singleton). `GlobalExceptionHandler` now
  depends on the port, not the Infrastructure type.
  **S3** (no architecture test for Api→Infrastructure): added
  `DependencyDirectionTests.Api_DoesNotDependOnInfrastructureOutsideComposition`. Confirmed
  empirically (build + test, not assumed) that only ONE narrow exemption was needed beyond
  `Program.cs`: `StartupEnvironmentGuard`, an `IHostedService` that inspects Infrastructure's
  `DatabaseOptions` at `StartAsync` — before the host accepts traffic — to refuse an unsafe
  deployed configuration. Both exemptions are startup/composition-time wiring, never
  request-path code, which is the same category the rule exists to keep clean. (A third
  candidate, `HealthEndpoints.cs`'s `using SchoolManagement.Infrastructure;` for
  `InfrastructureDependencyInjection.ReadinessTag`, needed no exemption at all: `ReadinessTag` is
  a C# `const`, so the compiler inlines its value at the call site and the compiled assembly
  carries no metadata reference to `InfrastructureDependencyInjection` — verified by running the
  test before adding any exemption and observing only `StartupEnvironmentGuard` reported.)
  **Deviation from the card's literal wording, flagged for visibility, since ACCEPTED:** the
  2026-09-04 dispatch said "whatever narrow exemption `Program.cs` genuinely needs", anticipating
  the carve-out would be Program.cs alone. The pre-existing `StartupEnvironmentGuard` dependency
  was not named in the card or in AUDIT.md's S2 finding. Extending the exemption to it (rather
  than also porting `DatabaseOptions` behind an abstraction, which is not in the card's acceptance
  criteria) was a backend-dev judgement call to unblock the test without scope creep. **Ruled
  ACCEPTED on reopen (below)** — orchestrator agreed an `IHostedService` reading `DatabaseOptions`
  at `StartAsync` is startup composition, not request-path code, and recorded a `## Known drift`
  entry with a trigger so a third exemption has to argue for itself.
  **S3 (stale artefact) — WIDENED ON REOPEN, see below for the corrected version.** Originally
  landed comparing the artefact's `LastWriteTimeUtc` against the newest `.cs` file under
  `src/SchoolManagement.Api` only, per the card's literal wording at the time.
  **Contract deliberately did not move** — `b287da03…` byte-identical before and after, verified
  both by direct `sha256sum` comparison and by `ci.ps1`'s own drift gate ("The committed contract
  matches the code"). The `/health/*` endpoints are `.ExcludeFromDescription()`'d and the new/
  changed doc comments (`HealthEndpoints.cs`, `RateLimitingOptions.cs`) sit on types that never
  reach the document, unlike TASK-0013's lesson about endpoint-level XML comments.
  Gates (initial close): all ten green live via `scripts/local-env.ps1` (DB resolved from
  `$HOME/.gras/pg-test.txt`), `total=181 passed=181 failed=0 skipped=0`, line coverage 75.86% /
  branch 57.60% (60% floor; the 87.00%/77.18% attribution question is TASK-0002/TASK-0009
  territory and was not touched). Diff: 10 files changed + 2 new
  (`IPersistenceErrorTranslator.cs`, `PersistenceErrorTranslator.cs`), 171 insertions / 6
  deletions — under the ~400-line guidance. Out-of-scope items (TASK-0012/0013 territory, the
  coverage number, the recorded nice-to-haves) left untouched, as instructed.

  **REOPENED same day — the staleness check was narrower than the defect it targets.** The
  Api-only scan was a faithful implementation of the card's own wording, but the card's wording
  was itself too narrow: the OpenAPI document is also shaped by types in
  `SchoolManagement.Application` — `ReferenceEndpoints.cs` returns `PingResponse`,
  `PagedResult<SampleRecordDto>` and `CreateSampleRecordResponse`, all declared there — so editing
  one of those changed the document while the Api-only check stayed green. That is the "stale
  passes silently" case the criterion exists to close, still open on a live path. Narrowing
  attributed to the card, not to the implementation; card corrected.
  **Fix:** `FindNewestApiSourceFile` renamed to `FindNewestBackendSourceFile` and widened from
  `src/SchoolManagement.Api` to all of `backend/src` (all four projects) — same `bin`/`obj`
  exclusion, same relative-path-and-both-timestamps failure message, same null-safe behaviour when
  the directory is absent. `OpenApiDocument`'s class `<remarks>` and the method doc comment
  rewritten to match: the old "that project is what generate-openapi.ps1 starts" justification is
  gone, replaced with the cross-project example above plus a new paragraph recording the accepted
  trade-off — a wider scan is slightly more likely to hit filesystem timestamp jitter on a fresh
  checkout, accepted because it fails loudly and by name rather than passing silently, and cannot
  fire at all inside `ci.ps1`.
  **Verified empirically:** touched
  `src/SchoolManagement.Application/Reference/Ping/PingQuery.cs`, confirmed the architecture tests
  failed with the message naming that exact file and timestamp, regenerated, confirmed all 27
  architecture tests passed again. Contract hash reconfirmed `b287da03…` byte-identical.
  Gates (refix): all ten green live via `scripts/local-env.ps1`, `total=181 passed=181 failed=0
  skipped=0`, line coverage 75.86% / branch 57.60%. Nothing else in the accepted diff touched.

  **REOPENED AGAIN same day, narrowly — criterion 1 (the health rate limit) landed with no test,
  and a pre-existing fixture comment made a false coverage claim.** `.RequireRateLimiting
  (RateLimitingOptions.HealthPolicyName)` in `HealthEndpoints.cs` was the one new production
  behaviour this card introduced and nothing asserted it — deleting the line left the whole suite
  green, which is exactly the "documented rather than enforced" defect family this card exists to
  eliminate. Separately, `ApiTestFixture.cs:124-125` said "the rate limiter itself is verified by a
  dedicated test that sets its own limit" — a grep for `429`/`TooManyRequests`/
  `Status429TooManyRequests` outside `bin`/`obj` found no such test anywhere in the backend. Both
  pre-existing, not introduced by the S1 implementation; worse than a plain gap because the comment
  tells the next reader to stop looking (same family as TASK-0016's fixtures, TASK-0010's document
  target).
  **Fix — three items, nothing else:**
  (1) New `tests/SchoolManagement.IntegrationTests/HealthRateLimitTests.cs`:
  `Ready_IsRejectedOnceTheHealthPolicyLimitIsExceeded` builds its own client via
  `Fixture.WithWebHostBuilder(...)`, layering `HealthPermitLimit=2` on the shared fixture's
  already-booted database connection (no new container, no new migration run — avoids driving
  10000+ real round trips against the shared Neon instance to reach the raised shared limit),
  sends three requests to `/health/ready`, asserts the third is `429`.
  (2) `Live_StaysUnthrottled_EvenUnderAHealthPolicyTightEnoughToRejectReady` — same technique,
  `HealthPermitLimit=1`, five requests to `/health/live`, all asserted `200` — cheap enough (one
  more `[Fact]`, same file) to include per the card's "if it costs only a few lines" steer.
  (3) `ApiTestFixture.cs:124-125` comment rewritten to name what is actually verified
  (`HealthRateLimitTests`, health policy only) and state plainly that the default and sensitive
  policies have no coverage — that gap is recorded as drift separately, TASK-0003 as the trigger
  (login is the sensitive policy's first real user).
  **Verified the new test can actually fail, not just pass — same standard as the prior reopen:**
  commented out `.RequireRateLimiting(RateLimitingOptions.HealthPolicyName)`, rebuilt, reran —
  `Ready_...` failed with `third.StatusCode should be TooManyRequests but was OK`; `Live_...` still
  passed (correctly — it was never gated on that line). Restored the line, rebuilt, reran — both
  passed; `git diff --stat` on `HealthEndpoints.cs` matches the prior round's diff exactly (17
  lines), confirming no residue from the round-trip.
  **Out of scope honoured:** no test for the default or sensitive policies; no other fixture
  refactor.
  Contract reconfirmed `b287da03…` byte-identical. Gates all ten green live,
  `total=183 passed=183 failed=0 skipped=0` (181 + 2 new), line coverage 75.16% / branch 57.60%.

## 2026-09-05 — Open question 11 RESOLVED by human decision: follow §7

**The question**, open since TASK-0001 (2026-08-27): the frontend scaffold deviates from §7 in three
places, all called deliberate in `frontend/HANDOFF.md` — a document written 2026-08-03, **before the
product specification landed**, so the deviations were chosen without knowing what would be built.
(a) §7 puts screens under `src/features/<feature>/`; the scaffold has a top-level `src/screens/` and
no `src/shared/`. (b) `react-hook-form` plus a zod resolver is mandated and absent. (c) Playwright is
mandated for auth, the primary create path and one failure path, and is absent. Each becomes binding
the moment a screen, a form or a flow exists — i.e. at TASK-0003's frontend half.

**Decision (human, 2026-09-05): follow §7's mandates, with the proviso "as long as it will lead to a
better file and codebase structure."** All three deviations are retired. `HANDOFF.md`'s reasoning is
superseded, not overruled on its own terms: it was correct for a design-system-first scaffold with no
product spec, and stopped being correct when the spec landed.

**How the proviso is applied.** It is a guard against churn, not a licence to re-open the decision per
case. Read as: adopt the §7 structure for everything new, and move existing code only where the move
buys something. Concretely —
- `src/features/<feature>/` is the home for every new screen. `src/features/` is currently empty and
  reserved, so TASK-0003's screens land there with nothing to migrate.
- `src/shared/` is created when the first genuinely shared thing needs it, and existing
  `src/components/`, `src/lib/`, `src/stores/` are NOT bulk-renamed into it on their own. A rename of
  a working, 130-test scaffold buys structure on paper and costs a large reviewable diff — that is
  exactly what the proviso excludes. The one thing that must not happen is a THIRD home for shared
  code appearing alongside them.
- `src/screens/scaffold-status/` is deleted when the first real screen lands, per its own drift entry,
  which removes `src/screens/` rather than migrating it.
- `react-hook-form` + `@hookform/resolvers` (zod) are added at the first form — TASK-0003's login.
  Dependency additions justified in that card per §8: spec-mandated by §7.
- Playwright is added at the first flow — TASK-0003's auth flow is the exact case §7 names first.

**Consequences.** TASK-0003's frontend half is now larger than a §4.3 "adapt callers" step: it
introduces a directory convention, two dependencies, and an E2E harness alongside the first real
screen. It splits. The `src/shared/` drift entry (2026-08-27) is resolved; the `src/screens/`
question inside it is answered.

**What this does NOT decide:** the bearer-token auth in `frontend/src/lib/auth/` is still known-wrong
against the cookie sign-off and is still TASK-0003's to fix — that was never part of question 11.

## 2026-09-05 — approved contract deltas moved to their own archive

Contract deltas are a distinct artefact class from decisions and will recur on every card that
touches the boundary. Appending 30 KB of endpoint shapes into this file per card would make it
unreadable, so approved deltas now live in `.agent/decisions/2026-Q3-contract-deltas.md`, one
entry per card, indexed from the card itself. First entry: TASK-0003.

## 2026-09-05 — TASK-0003 closure note (full text, condensed in STATE.md on 2026-09-05)

**TASK-0003 CLOSED.** Six `/api/v1/auth/*` endpoints, contract `3b518bd9…`, 245/245 gates green.
BREAKING only in removing `/reference/whoami` (zero callers). Rulings embedded in the
implementation: Super Admin is a flag bypass (spec 6.1.7 over 4.2.2); `423` is returned only when
the password is correct, so a locked account does not leak its own existence to a wrong guess.

**Reopened four times, always the same shape: the feature worked and its test passed while the
RULE went unenforced.** Landed as one ~1443-line diff instead of the three seams the card
envisaged. The second-pass review that size bought found three HIGH defects that 237 passing
tests were blind to. The lesson is not "write more tests" — it is that a test asserting the
feature does what it does cannot detect that the feature is not doing what the rule requires.

## 2026-09-05 — TASK-0022 closure note

**Discovered by raising PR #9**, the first time `backend-ci` ran against the branch. Job
`quality-gates` failed at "Self-test the gate-summary trx parser" in 7s, cascading into two
further errors that were symptoms, not causes.

**Three stacked defects, each hidden by the one in front of it:**

1. `scripts/tests/gate-summary.tests.ps1:79` invoked `& powershell.exe`. CI runs the step with
   `shell: pwsh` on `ubuntu-latest`, where that binary does not exist; `$ErrorActionPreference =
   'Stop'` turned it into a terminating error. Fixed by resolving `(Get-Process -Id $PID).Path` —
   the host the suite is itself running under. `$IsWindows`/`$IsCoreCLR` are unusable here: they
   are undefined in Windows PowerShell 5.1 and `Set-StrictMode -Version Latest` throws on an
   undefined variable. The same hardcode was fixed in `local-env.tests.ps1:107` (latent — that
   suite is not wired into any workflow).
2. `scripts/lib/gate-summary.ps1:80` derived the suite name via
   `[System.IO.Path]::GetFileNameWithoutExtension($storage)`, and every fixture carried a Windows
   `storage` path. On Unix, .NET recognises only `/` as a separator — a backslash is an ordinary
   filename character — so this would have returned the entire mangled path once defect 1 was
   fixed. Now splits on `/` and `\` explicitly. A new `skipped-unix/` fixture carries a real
   `/home/runner/...` path, and an assertion pins both shapes to the same derived suite name.
3. **`backend/.gitignore:18` (`*.trx`) meant NO fixture had ever been committed** — not
   `skipped-unix/`, and not the pre-existing `happy/`, `failed/` or `skipped/`. `git ls-files`
   returned only `fake-gate.ps1`. On a fresh `actions/checkout` those four directories do not
   exist, so **this self-test has never been capable of passing in CI**; defect 1 merely surfaced
   first and masked it. Fixed with a scoped negation, `!scripts/tests/fixtures/**/*.trx`, chosen
   over `git add -f` so future fixtures are included automatically and the intent is documented
   where the exclusion lives.

**Verification (performed by the orchestrator, not the subagent — see the dispatch note below).**
Mutation test: `Get-SuiteName` reverted to pre-fix behaviour → 3 of 25 assertions failed, exit 1;
restored → byte-exact against the index, 25/25, exit 0. `.gitignore` scoping proved by cases
rather than by reading — `artifacts/coverage/*.trx`, `TestResults/*.trx` and
`tests/**/TestResults/*.trx` all still ignored, fixtures re-included. Full gates: ten PASS,
`total=245 passed=245 failed=0 skipped=0`, line 77.93% / branch 59.08%, contract drift gate green
and the hash unmoved.

**Weakness found BY the mutation run, and worth acting on.** Under the mutant, the two
process-mode assertions at `gate-summary.tests.ps1:101` and `:108` still PASSED: they use
`-match 'schoolmanagement\.integrationtests'`, which matches as a substring of the fully mangled
path. Only the three in-process `-eq` assertions caught the break. Those two read as though they
verify correct suite derivation and would not fail if it were wrong. This is the standing
2026-09-04 lesson recurring inside the very suite that exists to enforce it: a check that cannot
be shown to fail is not a check, and a substring match on a long string is close to unfalsifiable.

**Dispatch note — a process failure worth recording.** The `backend-dev` dispatch stalled three
consecutive times, each returning a "waiting for…" string in place of the report its card
required, and it missed defect 3 entirely. The orchestrator caught defect 3 in review, and
performed the breakage proof and the gate run itself. Two lessons: the §9 rule that a subagent
reporting success without pasting gate output has not finished did its job — there was no output
to accept, so nothing was accepted; and reviewing the DIFF rather than the REPORT is what found
the untracked fixtures, since no report existed to be misled by.

**The through-line.** Third instance of the TASK-0011 pattern — a gate that had never actually
executed was reporting PASS. This one lived in CI for a week without running, and only raising a
PR revealed it. The gates were well written; the failure was again in whether they ran at all.

## 2026-09-05 — TASK-0023, 0024, 0025 closure notes, and the standing lesson they share

All three came out of PR #9, the first time either workflow ran against this branch. Read together
they are one finding, which is why the standing lesson is recorded here rather than three times.

### The standing lesson — inputs that live outside the repository

**Four defects in one day, all the same shape: something required to run the gates was not in the
repository, so "green locally" and "green in CI" were never the same claim.**

1. TASK-0022 — the gate self-test's `.trx` fixtures were `*.trx`-ignored and had never been
   committed. The suite could not run on a fresh checkout.
2. TASK-0023 — `frontend/.env` is gitignored, and `env-values.ts` validates at module scope, so
   all 15 test files died during collection on a runner. The suite had never run in CI.
3. TASK-0024 (a) — `scripts/local-env.ps1` is untracked and per-developer; this machine's copy
   predates TASK-0018 and still carries the bug that card fixed in the template.
4. TASK-0024 (b) — two tool VERSIONS were unpinned in opposite directions: `global.json`'s
   `rollForward: latestFeature` let CI take a newer SDK feature band than any developer had, and
   CI pinned gitleaks 8.28.0 while the developer ran 8.30.1.

**The rule that follows:** a gate is only as trustworthy as the reproducibility of its inputs.
Before trusting a green gate, ask what it reads that is not committed and not version-pinned.
`frontend-ci.yml` already had the right instinct — it pins Node "so CI cannot pass on a runtime
nobody runs locally" — and the backend violated exactly that principle in both directions.

Corollary on measurement: the gitleaks split was *measured*, not deduced — same repo, same 21
commits, same `.gitleaks.toml`, 8.30.1 reports `no leaks found` and 8.28.0 reports 15. Two tool
versions disagreeing about whether a repository contains secrets is not a difference of opinion;
it means the gate's verdict depended on which machine ran it.

### TASK-0023 — frontend test environment

`vite.config.ts`'s `test` block now declares an explicit `env` with all six `VITE_*` variables, so
vitest depends on no ambient file. Proved by moving `frontend/.env` aside: 15/15 files, 130/130
tests green with `Test-Path .env = False`. The case asserting "loads the real .env without
throwing" was the defect stated as a test and was reframed, not deleted; its real assertions stand.

Related gap CONFIRMED and deliberately not fixed: `vite build` does not execute `env-values.ts` —
it inlines `import.meta.env.VITE_*` as `undefined` — so with no `.env` the build SUCCEEDS while
emitting a bundle that throws on boot. The Build gate goes green on something unusable. The
workflow now copies `.env.example` (explicitly non-secret) for build/preview/E2E; the unit tests
deliberately do not rely on that copy. **A build-time env check is unowned and uncarded.**

### TASK-0024 — backend toolchain pinning

`rollForward` `latestFeature` → `latestPatch`: feature bands, not patches, carry new analyzer
versions, so this pins CI and local to the same analyzer set by construction while still
tolerating a security patch within the band. CI's gitleaks bumped 8.28.0 → 8.30.1 to match local,
and a `dotnet --version` step now prints the resolved SDK so the next divergence is read rather
than deduced.

Both analyzer findings were FIXED, not suppressed, and one was a genuine bug:

- **CA2025** (`PrivilegeAuthorizationTests.SendAsAsync`) — a non-async method held
  `using var request = new HttpRequestMessage(...)` and returned `_client.SendAsync(request, …)`'s
  Task directly. `Dispose()` therefore ran the moment `SendAsync` RETURNED ITS TASK, not when the
  send completed, so `HttpClient` could be reading a disposed request. Made `async`/`await`ed.
- **CA1873** (`RequestLoggingBehavior`, cancellation branch) — the `[LoggerMessage]` source
  generator only skips INTERNAL formatting when the level is disabled; a computed argument at the
  call site is still evaluated unconditionally. Guarded with `IsEnabled`.

Honest limits of the verification, recorded because they matter: this machine has only SDK
10.0.100, so the newer feature band that CI had drifted to could not be installed. The pin is
verified by SDK-resolution semantics, NOT by reproducing the failures and watching them disappear
— which is a weaker standard than this project's own "break it and watch it go red". The
`dotnet test` "argument <dll> is invalid" error likewise could not be reproduced locally.

### TASK-0025 — the client regeneration TASK-0003 skipped

§4.3's handoff is backend promotes → frontend regenerates. TASK-0003 promoted `3b518bd9…` and
closed with the second half never run, and **nothing recorded it** until 2026-09-05. The committed
`schema.d.ts` still declared `/reference/whoami`, removed as a BREAKING change, and none of the six
`/auth/*` paths. `check:api-drift` was correctly failing the whole time.

**The process defect is not the stale file; it is that a half-finished §4.3 handoff could close a
card.** §10's checklist covers "contract regenerated and lockfile current" — which was true on the
backend side — without asking whether the CONSUMER caught up.
