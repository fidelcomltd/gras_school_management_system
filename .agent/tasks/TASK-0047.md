# TASK-0047 — Regenerate the frontend client for role assignments

Owner: frontend-dev
Depends on: TASK-0030 (closed 2026-09-08 — the contract move this card consumes)
Contract impact: none. This card consumes a committed contract; it must not change one.
Status: done

> **Sixth run of one recurring card.** TASK-0029, 0033, 0037, 0040, 0044 were the same work
> against earlier contract moves. **[TASK-0044.md](TASK-0044.md) and [TASK-0040.md](TASK-0040.md)
> are the template — follow them exactly**; this card records only what differs. Every prior
> dispatch wrote **zero** new lines in `client.ts`/`client-types.ts`; expect the same.

## Goal

`frontend/src/api/schema.d.ts` matches the committed contract again, §4.4 check 2 goes green, and
all three role-assignment operations are callable through the existing typed wrapper.

## Contract delta

**None.** Already committed at sha256
`c3cb88ff74b931ba58057a11b781fbad58d72d1778ff91584dbfe712f661a4c9` (38 paths). **Recompute that
hash yourself against the committed file and confirm it against `contracts/CONTRACT.lock` before
you start** — if it does not match, stop and report rather than regenerating against a moving
target. Drift was confirmed RED by the orchestrator running `check:api-drift`, not inferred from
the hash moving.

Three new operations:

| Operation | Route | Headers | Response |
|---|---|---|---|
| `ListRoleAssignments` | `GET /api/v1/admins/{id}/assignments` | none | 200 — **bare `RoleAssignmentDto[]`** |
| `CreateRoleAssignment` | `POST /api/v1/admins/{id}/assignments` | `X-CSRF-Token` **required**, `Idempotency-Key` **required** | 201 |
| `RevokeRoleAssignment` | `DELETE /api/v1/assignments/{id}` | `X-CSRF-Token` **required**, `Idempotency-Key` **optional** | 204 |

New schemas: `RoleAssignmentDto`, `CreateRoleAssignmentCommand`, `RoleAssignmentStatus`
(`Active` | `Revoked`), `ScopeType` (`SchoolWide` | `ArmList`).

## Acceptance criteria

- [x] `npm run generate:api` re-run against the committed contract; `schema.d.ts` rewritten by the
      generator, never by hand. Report `git diff --stat`.
- [x] `npm run check:api-drift` → "No drift."
- [x] All three operations proven reachable through the existing generic wrapper. **Expect ZERO new
      lines in `client.ts`/`client-types.ts`**; justify any you add.
- [x] **Both negative typing directions proven** — an omitted required `Idempotency-Key` on
      `CreateRoleAssignment`, **and** an `idempotencyKey` rejected on `ListRoleAssignments`
      (declares no such header). Note that `RevokeRoleAssignment` accepts it *optionally*, so it
      proves neither direction — do not use it for either.
- [x] All three operations proven to reject an omitted required `id` path parameter.
- [x] §8 enum tolerance proven for **both** new enums — `RoleAssignmentStatus` and `ScopeType` —
      via `server.use(...)` overrides returning unrecognised members, asserting pass-through
      rather than a throw. **Write the override route as `:id`, never `{id}`** — MSW does not read
      the contract's `{id}` template as a parameter and an override bypasses `toMswRoute` in
      `openapi-handlers.ts`. TASK-0037 lost a run to exactly this, and every route on this card
      has a path parameter, so the trap applies to all of them this time.
- [x] Every `@ts-expect-error` verified load-bearing by TASK-0044's removal-probe method.
- [x] `npm run verify` green with counts pasted, `Skipped: 0`.
- [x] **Run the secret scan before reporting** — from the repo root:
      `gitleaks detect --source . --no-git --config backend/.gitleaks.toml --redact --no-banner`.
      `npm run verify` does NOT cover it, and TASK-0043 turned the backend gate red with a
      fixture that looked like a credential. Reuse the existing fixed UUID-shaped constants in
      `src/api/client-*.test.ts`; never a random high-entropy string, never an allowlist entry.
- [x] `npm run test:e2e` — re-run only if anything e2e-relevant changed; if you skip it, say which
      and why. Last known green: **8 passed / Skipped 0 (TASK-0043)** — corrected 2026-09-09;
      this card first said 4, copied from TASK-0040's then-current figure.

## Out of scope

- **Every screen.** No assignments UI, no role-grant form, no scope picker, no TanStack Query
  hooks, no `features/assignments` folder. Client seam only.
- `client.ts` / `client-types.ts` changes beyond a proven gap.
- Anything under `backend/**` or `contracts/**`. If the contract looks wrong, stop and report —
  §4.5 gives the backend the *how*.

## Notes for the implementing agent

Read `.agent/STATE.md` first (§4.1), then `frontend/src/api/README.md` and `CONVENTIONS.md`.

**The one new trap on this card.** `ListRoleAssignments`'s 200 body is an inline
`array` of `$ref: RoleAssignmentDto` with **no top-level `example`**. That is the exact shape that
cost TASK-0040 a debugging round on `ReorderLevels`: `exampleFor` in `openapi-handlers.ts` only
resolves an example off a `$ref`'d schema or an inline one that names one, so the contract-derived
default handler answers with an empty body instead of an array. Fix it the way TASK-0040 did —
an explicit `server.use(...)` override in that one test. Do **not** touch `openapi-handlers.ts`
and do **not** loosen the assertion. This is not contract drift; the contract owes no example here.

**Corrected 2026-09-09, after the implementing agent caught this card wrong.**
`RoleAssignmentDto.armIds` is **required and non-nullable** — a plain `array`, empty for a
`SchoolWide` grant, non-empty for `ArmList`. Only `CreateRoleAssignmentCommand.armIds`, the
*request* body, is nullable. Either way, do not add client-side logic that infers `scopeType` from
whether `armIds` is empty — the backend owns that (§4.5).

Keep each new test file under `CONVENTIONS.md` §3's 180-line cap; split by route group if needed,
as TASK-0044 did.

## Log

Full log: [logs/TASK-0047.log.md](logs/TASK-0047.log.md) — moved 2026-09-14. Card closed; final entry kept here:

- 2026-09-09 **CLOSED by orchestrator.** Diff reviewed directly, not taken on report. All five
  §4.4 checks verified GREEN by contract-guardian: backend→contract byte-identical (38 paths,
  342413 bytes), contract→client "No drift", no raw `fetch`/`axios` outside `src/api/` and
  ... full closure account (21 more lines) in [logs/TASK-0047.log.md](logs/TASK-0047.log.md).
