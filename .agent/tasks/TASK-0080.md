# TASK-0080 — Regenerate the typed client against `84b46211…`

Owner: frontend-dev
Depends on: TASK-0077 (done; contract promoted)
Contract impact: **none** — this card consumes a contract already promoted. It must not change one byte of
`contracts/**`.
Status: **done 2026-09-18** — closed on the orchestrator's gate run
Reads: `frontend/src/api/README.md`; `.agent/rules/wire.md`; `.agent/rules/gates.md` §1 (frontend row);
       `.agent/spec/frontend.md`.

## Goal

`src/api/schema.d.ts` matches the committed contract, so the result-rules settings screen card that
follows has a typed seam to build on.

## Contract delta

None. `jq` only what you need: the one new path is `/api/v1/settings/result-rules` (`GET` +
`PUT`); the five new schemas are `ResultRulesDto`, `UpdateResultRulesCommand`, `AnnualMethod`,
`PrimaryPositionScope`, `TieBreakRule`.

## Acceptance criteria

- [x] `npm run generate:api`, then `npm run check:api-drift` reports `No drift`, exit 0. No hand edit
      to `schema.d.ts` — regeneration must reproduce it byte-for-byte.
- [x] `npm run typecheck` and `npm run lint` exit 0. Pin and lockfile untouched
      (`openapi-typescript@7.13.0`, exact).
- [x] **No new wrapper code.** `apiPut` already exists (TASK-0079), and this surface is `GET` + `PUT`
      on one path, so the streak of zero-new-wrapper-code regenerations resumes. If you find yourself
      needing to add a verb helper, STOP — something is wrong with that finding.
- [x] Nothing consumes the result-rules types yet — no screens, no queries.

## Out of scope

The result-rules settings screen (its own card: §6.2.8 editor, the §6.2.9 reason dialogue, the
lock affordances). Any `backend/**` or `contracts/**` change.

## Notes for the implementing agent

- **Nearest shape:** TASK-0079 and TASK-0074 did exactly this regeneration; both are one-commit
  precedents. This one is smaller than either.
- The three new enums cross the wire **PascalCase** (`SimpleAverage`, `Arm`, `ExamThenCa`), not the
  snake_case that spec §6.2.8 uses in prose. That is deliberate and matches all 12 existing contract
  enums — see the TASK-0077 closure entry. Tolerate an unknown member per `rules/wire.md`; do not
  switch exhaustively over them without a default.
- **Drift that bites, and the screen card will hit it before you do:** a fresh database seeds
  `requireCorePass: true` with `coreSubjectIds` empty, which the PUT validator rejects — the default
  state is not round-trippable. Do not try to "fix" it in the client. → `drift/2026-Q3.md` 2026-09-18.
- **Known drift in the test family you are touching:** the `@ts-expect-error` type-level tests in
  `src/api/` fire real unawaited requests. If your card adds or edits one, prefer a non-executing form
  (`() => apiX(...)` or a type-only assertion). → `drift/2026-Q3.md` 2026-09-18.
- Do NOT run `npm run verify` or `npm run test:e2e` (`gates.md` §1).
- **Do not write to `.agent/**`.** Emit a `LEDGER ACCOUNT` section in your report instead. Do not commit.
- Estimate ≈ 60 lines, essentially all generated.

## Log

- 2026-09-18 Carded by the orchestrator at TASK-0077's promotion, which moved the contract to
  `84b46211…` (65 paths, 137 schemas).
- 2026-09-18 Implemented by `frontend-dev`: `schema.d.ts` regenerated (+353/-0, additive), nothing else touched,
  `client.ts` diff empty. Agent: drift No drift, typecheck and lint clean, `src/api` 134/134.
- 2026-09-18 **Closed by the orchestrator.** Scope verified (one file); `check:api-drift` → `No drift`, exit 0;
  `npm run verify` → 55 files, 381 tests, 0 failed, Skipped 0, build clean, exit 0. First dispatch under the
  permanent `LEDGER ACCOUNT` rule: no `.agent/**` writes.
