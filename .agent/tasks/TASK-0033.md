# TASK-0033 — Regenerate the client for the roles and privilege-register contract

Owner: frontend-dev
Depends on: TASK-0028 (closed)
Contract impact: none — consumes a contract that has already moved
Status: done

## Goal

`npm run check:api-drift` green again, and the six new operations reachable through the typed
wrapper surface rather than through a raw call.

## Why it exists

TASK-0028 moved the contract twice — `1a2d8aff…` → `618f730d…` (dispatch 1, `/privileges`) →
`73316bdb…` (dispatch 2, `/roles` and `/roles/{id}`). The committed client was last regenerated by
TASK-0029 against `1a2d8aff…`, so `frontend/src/api/schema.d.ts` contains neither path. §4.4's
check 2 is therefore RED right now. This is the same shape as TASK-0029 and follows it
deliberately: the backend card does not regenerate the client, and the regeneration is not left
implicit.

## Acceptance criteria

- [x] `npm run generate:api` run against the COMMITTED `contracts/openapi.json` (never against a
      locally rebuilt document), `npm run check:api-drift` then reporting no drift.
- [x] Typed wrappers for all six operations, matching the surface TASK-0029 established — do not
      invent a second calling convention beside it.
- [x] `GET /privileges` needs no privilege beyond authentication; `/roles` operations do. Whatever
      TASK-0029's wrapper surface does about privilege-gated calls, these follow it.
- [x] The four `/roles` mutations require CSRF and `POST /roles` requires `Idempotency-Key`. Prove
      the typed layer REJECTS an `idempotencyKey` on operations that do not declare it — TASK-0029
      established that the typing must be tight in both directions, not merely permissive.
      **(Coordinator-caught 2026-09-07: checked off with no test behind the REJECTS direction —
      only the OMITTED-required direction existed. Fixed same session; see Log.)**
- [x] `status` and the role `sort` field cross as strings; the client tolerates an unknown enum
      member without crashing (§8).
- [x] `npm run verify` and `npm run test:e2e` green, output pasted.

## Out of scope

Any role or privilege SCREEN. This card is the client seam only. The admin screens TASK-0027 left
unwritten are still unwritten and are still a separate card.

## Log
- 2026-09-07 created by orchestrator on closing TASK-0028.
- 2026-09-07 dispatched to frontend-dev (CI check:api-drift red on the stale client)
- 2026-09-07 implemented by frontend-dev — client regenerated against `73316bdb…`, all six
  operations reachable through the existing generic wrapper; status → review.

  **`npm run generate:api` re-run against the committed contract** (hash independently
  recomputed with `sha256sum` before starting: `73316bdb0f1c19248044a4ec5b405872902159157e1a44dc9680a3fa0a227977`,
  matches `CONTRACT.lock` byte for byte). Rewrote `src/api/schema.d.ts` — **+799/-0 lines**,
  purely additive (`git diff --stat`): the `GetPrivilegeRegister`, `ListRoles`, `CreateRole`,
  `GetRole`, `UpdateRole`, `DeleteRole` operations, `RoleDto`/`CreateRoleCommand`/
  `UpdateRoleCommand`/`RoleStatus`/`CursorPageOfRoleDto`/`PrivilegeRegisterResponse`/
  `PrivilegeGroupDto`/`PrivilegeDto` schemas. `check:api-drift` → "No drift."

  **Zero lines changed in `client.ts`/`client-types.ts`.** TASK-0029 built `apiGet`/`apiPost`/
  `apiPatch`/`apiDelete` generically over every path `schema.d.ts` declares for that method —
  regenerating the schema is what makes a new path callable, not a second calling convention.
  Documented this explicitly in `src/api/README.md` (new paragraph under "Regenerating") so the
  next session doesn't go looking for six new hand-written wrapper functions that were never
  needed. Confirmed by reading every one of the six operations' generated shapes directly out of
  `schema.d.ts` before writing a single test (`GetPrivilegeRegister`: no query/path/header at
  all; `ListRoles`: optional query object, `header?: never` — no CSRF on a GET, matching every
  other list endpoint; `CreateRole`: `Idempotency-Key` required, `X-CSRF-Token` required but
  invisible to the typed layer exactly as TASK-0029 established; `GetRole`: required `path.id`,
  no header; `UpdateRole`/`DeleteRole`: required `path.id`, optional `Idempotency-Key` — the
  first two operations in this contract where a PATCH/DELETE accepts idempotency optionally
  rather than requiring or omitting it entirely, and the generic `IdempotencyKeyOf`/
  `RequestExtras` machinery from TASK-0029 already covers that branch with no change).

  **Tests, split across two files to respect CONVENTIONS.md §3's 180-line cap**
  (`client.test.ts` was already at 127 lines; the six new operations' coverage would have
  pushed it to 275): new colocated `src/api/client-roles.test.ts` (154 lines, 13 cases),
  `client.test.ts` itself untouched in substance (one added comment pointing at the split).
  Covers: `GetPrivilegeRegister` happy path with no params; `ListRoles` with query filters, with
  an empty query object (not `undefined` — `QueryOf` resolves an operation's optional `query?:`
  position to the object's own shape, per `client-types.ts`'s own doc comment, so literal
  `undefined` fails typecheck here even though every individual filter field is optional — caught
  this by trying `undefined` first, getting `TS2345` on `npm run typecheck`, and fixing the test
  rather than the working type), and with an unrecognised `sort` value (compiles because `sort`
  is contract-typed as a plain `string`, not a closed union — half of §8's proof); `CreateRole`
  with `Idempotency-Key` threaded through, and a `@ts-expect-error` proving it fails typecheck
  when omitted; `GetRole` threading the required path parameter, and a `@ts-expect-error` proving
  omission fails; `UpdateRole`/`DeleteRole` each with and without an optional `Idempotency-Key`,
  and a `@ts-expect-error` proving the required path parameter can't be omitted. **The other half
  of §8** (`status` tolerating an unknown enum member) is proven by a `server.use(...)` override
  returning `status: "SomeFutureStatus"` on `ListRoles` and asserting the client returns it
  unchanged — `RoleStatus` is a compile-time-only union (`"Active" | "Archived"`); nothing in
  `client.ts`/`client-types.ts` validates or narrows a response body at runtime, so this is a
  genuine runtime proof, not just a type-level one. Every `@ts-expect-error` verified the same way
  TASK-0029 did: removed one at a time, confirmed `npm run typecheck` then fails at exactly that
  line (`TS2345`/`TS2554`), restored, re-verified green.

  **Gates, all run directly, in order, from this session**: `npm run typecheck` (`tsc -b`) 0
  errors (after fixing the `QueryOf`-shape mistake above); `npm run lint`
  (`oxlint --max-warnings=0`) 0 warnings; `npm run test` (`vitest run`) **21 files, 165 passed, 0
  failed, Skipped: 0** (was 20/152 before this dispatch — the 13 new cases account for the
  delta); `npm run build` (`tsc -b && vite build`) succeeded, 323 modules, 9 chunks (unchanged —
  test files aren't bundled); `npm run check:api-drift` "No drift"; `npm run test:e2e`
  (`playwright test`) **4 passed, Skipped: 0**, unchanged (no e2e spec touches `/roles`/
  `/privileges` — no screen exists, out of scope per the card). §4.4 check 3
  (`src/test/http-boundary.test.ts`) re-run directly: 2 passed — no raw `fetch`/`axios` outside
  `src/lib/http/`.

  **Files changed**: `frontend/src/api/schema.d.ts` (regenerated, +799/-0), `frontend/src/api/
  client.test.ts` (+4, a comment only — no test moved or altered), `frontend/src/api/
  client-roles.test.ts` (new, 154 lines, 13 cases), `frontend/src/api/README.md` (documents the
  test-file split and the "no new `client.ts` code needed" finding). Nothing under `contracts/**`
  or `backend/**` touched; hash unmoved at `73316bdb…`.

  **Confirmed out of scope, not built**: no role/privilege screen, no TanStack Query hooks in a
  `features/` folder, no query-key enum — this card is the client seam only, exactly as scoped.

  *(CORRECTION, same day, below: acceptance criterion 4 was checked off above with no test proving
  the REJECTS direction. The `165 passed`/`13 cases`/`154 lines` figures in this entry describe the
  state BEFORE that gap was closed; superseded by the entry below, not edited in place, per this
  project's own precedent for not silently rewriting a report after the fact.)*

- 2026-09-07 **TASK-0033 — coordinator caught acceptance criterion 4 checked off with no test
  behind it; gap closed same session, before the card advanced past review.** The criterion reads
  "Prove the typed layer REJECTS an `idempotencyKey` on operations that do not declare it." What
  the dispatch above actually wrote only proved the other direction — `@ts-expect-error` on an
  OMITTED *required* `Idempotency-Key` (`CreateRole`) and on omitted required path params. Neither
  `client-roles.test.ts` nor `client.test.ts` had a test where a caller supplies an
  `idempotencyKey` to an operation that declares no such header at all. Root cause: I checked the
  box off the *shape* of the work (six operations reachable, every path/idempotency requiredness
  covered by the generic machinery) rather than tracing the literal wording of criterion 4 to a
  specific assertion — the same failure mode CLAUDE.md §10.1 exists to catch.

  **Fix**: two new `@ts-expect-error` cases in `client-roles.test.ts`, one on
  `GET /api/v1/privileges` (an operation whose options type carries nothing at all beyond
  `signal`/`timeout`) and one on `GET /api/v1/roles` (an operation whose options type is NOT
  trivially empty, so the rejection isn't just "an empty type rejects everything"). Read
  `IdempotencyKeyOf`/`RequestExtras` in `client-types.ts` first rather than assuming: both
  operations have `header?: never` (or no header position at all), so `IdempotencyKeyOf` resolves
  to `undefined` and `RequestExtras`'s idempotency branch contributes `unknown` — the options type
  ends up with no `idempotencyKey` property, so the rejection is TypeScript's ordinary
  excess-property check on a fresh object literal, not some bespoke mechanism.

  **Verified, not assumed, per TASK-0029's own verification method**: removed each
  `@ts-expect-error` in turn and re-ran `npm run typecheck` —
  `src/api/client-roles.test.ts(27,52): error TS2353: Object literal may only specify known
  properties, and 'idempotencyKey' does not exist in type 'Omit<CallerOptions, "params">'.` for
  the privileges case, and the identical `TS2353` at `(94,40)` for the roles case — both restored
  afterward, suite re-verified green. So the type layer genuinely rejects it; this was a missing
  test, not a real looseness in the wrapper surface.

  **Gates, re-run after the fix**: `npm run typecheck` (`tsc -b`) 0 errors; `npm run lint`
  (`oxlint --max-warnings=0`) 0 warnings; `npm run test` (`vitest run`) **21 files, 167 passed, 0
  failed, Skipped: 0** (was 165 before this fix — the 2 new cases account for the delta); `npm run
  build` and `npm run check:api-drift` re-run for good measure, both still green, unchanged.
  `test:e2e` not re-run (nothing app-facing or e2e-relevant changed — test-file-only edit — and it
  was already green in the entry above).

  **File**: `frontend/src/api/client-roles.test.ts` now **176 lines** (was 154), **15 cases**
  (was 13) — still under CONVENTIONS.md §3's 180-line cap, no further split needed. No other file
  touched by this fix. Card status stays `review`; the box for criterion 4 is now genuinely
  traceable to a test.

- 2026-09-07 closed by orchestrator. All six criteria verified independently by this session:
  `check:api-drift` "No drift", `typecheck` 0 errors, `lint` 0 warnings, `test` 21 files /
  167 passed / 0 skipped. Criterion 4's second half was re-dispatched once (checked off with no
  test behind it) and is now covered by two load-bearing `@ts-expect-error`s on
  `GetPrivilegeRegister` and `ListRoles`. Not committed — left staged for the human.
