# TASK-0018 — Make local-env.ps1 pass switches through to the gate script

Owner: backend-dev
Depends on: TASK-0016
Contract impact: none
Status: done

## Goal

A developer can run the gate suite locally with any of `ci.ps1`'s switches. Today they cannot:
`./scripts/local-env.ps1 -GateArgs '-NoFailFast'` dies with

```
Cannot validate argument on parameter 'Configuration'. The argument "-NoFailFast" does not
belong to the set "Debug,Release" specified by the ValidateSet attribute.
```

because `$GateArgs` is a `[string[]]` splatted with `@GateArgs`, and splatting an ARRAY passes
positionally — so `-NoFailFast` binds to `ci.ps1`'s first positional parameter instead of being
read as a switch name. Pre-existing, but TASK-0016 made it bite: it added `-NoFailFast` and
`-AllowSkipped`, the two switches a local developer most needs, and `local-env.ps1` is the only
sanctioned way to run the suite with a resolved `POSTGRES_TEST_CONNECTION`.

Found by the orchestrator while independently verifying TASK-0016; the workaround used there was
to set the environment variable by hand and call `ci.ps1` directly, which defeats the point of
the wrapper.

## The file to change is the TEMPLATE, not the copy

`backend/scripts/local-env.ps1` is **not tracked** — `git ls-files backend/scripts/` lists only
`local-env.template.ps1`. The local file is a developer's generated, gitignored copy holding a real
credential, which is exactly why it is untracked.

The bug is in the tracked template, identically: `[string[]]$GateArgs = @()` at
`local-env.template.ps1:88`, splatted at `:138` as `& $scriptPath @GateArgs`. **Fix the template.**
A fix applied only to your own `local-env.ps1` would satisfy every criterion below and ship nothing
— the next developer regenerates from the template and gets the bug back. Say in your report which
file you edited.

## Contract delta

None. No file under `backend/src/**`, `frontend/src/**` or `contracts/**` changes.

## Acceptance criteria

- [x] `./scripts/local-env.ps1 -GateArgs '-NoFailFast'` reaches `ci.ps1` as the SWITCH
      `-NoFailFast`, not as a positional `-Configuration` value.
- [x] The same holds for `-AllowSkipped`, for two switches at once, and for a name/value pair
      such as `-Configuration Debug` — including the combination of both forms in one call.
- [x] A wrong switch name still fails loudly at `ci.ps1`'s own parameter binding. Do not swallow
      unknown arguments to make the happy path work.
- [x] The change lands in the tracked `backend/scripts/local-env.template.ps1`. A diff that touches
      only an untracked `local-env.ps1` is a non-delivery, not a fix.
- [x] Covered by the `backend/scripts/tests/` self-test harness TASK-0016 established, exercising
      the real argument path rather than asserting on a string.

## Out of scope

- Changing which gates run, their order, or any threshold.
- `ci.ps1`'s own parameters. They are correct; the wrapper is what mis-passes them.
- The credential-resolution logic (sources 1-4). It works and it is security-sensitive — read the
  header comment before touching anything near it, and prefer not to.
- Any CI workflow change. CI does not use this wrapper; it sets the environment variable itself.

## Notes for the implementing agent

Read the `.PARAMETER GateArgs` block and the invocation at the foot of
`backend/scripts/local-env.template.ps1` first. Splatting a HASHTABLE binds by name; splatting an array binds positionally — that asymmetry
is the whole bug. Whatever you choose, do not print or log the resolved connection string: the
header comment explains at length why this file never holds a realistic-looking credential, and
`.agent/drift/2026-Q3.md` (2026-09-04) records what happened last time a credential-shaped string
was committed into documentation.

## Log
- 2026-09-04 created by orchestrator, from a defect found while verifying TASK-0016.
- 2026-09-04 orchestrator amended before dispatch: the card sent the agent at the untracked
  `local-env.ps1`; the bug is in the tracked `local-env.template.ps1` and the fix belongs there.
  Noted too because commit `68a03fc` is titled as if this card were implemented — it only created
  the card, and `local-env.template.ps1` is unchanged since.
- 2026-09-04 dispatched to backend-dev (parallel with TASK-0014 under §4.3 — disjoint trees, no contract impact). STATE.md is orchestrator-written for this round; report the ledger line, do not append it.
- 2026-09-04 backend-dev implemented. Fixed the tracked `backend/scripts/local-env.template.ps1`
  only (verified `local-env.ps1` untracked and never opened/read, so its credential was never
  touched). Root cause confirmed empirically before coding: splatting a `[string[]]` ALWAYS binds
  positionally in PowerShell, regardless of whether the array came from `-GateArgs`, from
  `ValueFromRemainingArguments`, or even from re-splatting a manually-populated automatic `$args`
  — only a genuinely engine-populated `$args` (unbound tokens, no named parameter in between) or a
  Hashtable splat binds by name. Since the card's acceptance criteria require the explicit
  `-GateArgs` interface, added `ConvertTo-SplattableGateArguments` (new function in the template):
  walks the flat token list, reading each `-Name` as a switch (`$true`) unless immediately followed
  by a token that does not itself start with `-` (then that token is its value) — no reflection on
  the target script, so an unknown name passes through unresolved and the GATE SCRIPT's own binder
  rejects it. Invocation changed from `& $scriptPath @GateArgs` to splatting the converted
  hashtable. `.PARAMETER GateArgs` doc comment and four new `.EXAMPLE` blocks added, one per
  required argument form.
  Self-test: new `backend/scripts/tests/local-env.tests.ps1` (same hand-rolled harness style as
  TASK-0016's `gate-summary.tests.ps1`, since this machine has only Windows-bundled Pester 3.4.0)
  plus a new fixture `backend/scripts/tests/fixtures/fake-gate.ps1` mirroring ci.ps1's real
  parameter shape (`[ValidateSet]` string, ranged int, three switches) INCLUDING `[CmdletBinding()]`
  — omitting that attribute was tried first and made PowerShell silently drop an unknown named
  argument instead of erroring, which would have made the wrong-switch-name assertion pass for the
  wrong reason. The harness runs the real tracked template as a genuine child `powershell.exe`
  process via a PowerShell source string (not a raw `-File` argv list — that re-parses each
  `-Xxx`-shaped array element as a new top-level parameter before the template even runs, a
  different quirk from the one this task fixes, and breaks the two-switches/name-value/combined
  cases before they reach the code under test). 21/21 assertions passed, exit 0.
  All four required argument forms verified working (single switch, two switches, name/value pair,
  both forms combined); a wrong switch name (`-Bogus`) fails at the FIXTURE's own
  `[CmdletBinding()]` parameter binder (`NamedParameterNotFound`), not swallowed, not misreported —
  confirmed the fixture's body never ran (no `Configuration=` echo) so it is a rejected bind, not an
  accepted no-op. `ci.ps1` itself, its parameters, and the credential-resolution sources 1-4 were
  not touched. No credential-shaped literal was added anywhere (checked by grep and by eye); the
  self-test asserts no run ever prints a `Host=...Password=...`-shaped line.
  Left undone: did not run the full `ci.ps1` suite through the fixed wrapper (card states no
  ci.ps1-green criterion; the self-test harness is the specified evidence and doing a full gate run
  is expensive and out of scope for this card). Did not modify the untracked `local-env.ps1`.
- 2026-09-04 CLOSED by orchestrator. Self-test harness re-run independently rather than accepted
  on report: `powershell -File backend/scripts/tests/local-env.tests.ps1` → `PASSED: 21
  assertion(s).`, exit 0. Diff reviewed: 65 lines added to the TRACKED
  `local-env.template.ps1` and nothing else under `backend/**` — the template requirement held.
  The four scratch files the agent created mid-run (`fake-ci.ps1`, `fake-wrapper.ps1`,
  `fake-wrapper2.ps1`, `test-design.ps1`) were cleaned up before it finished; `git status` is
  clean of them. New tracked files: `tests/local-env.tests.ps1` and `tests/fixtures/fake-gate.ps1`.
  Two things the agent found that are worth more than the fix itself, both cases of a test that
  would have passed for the wrong reason:
  (a) the fixture initially lacked `[CmdletBinding()]`, and PowerShell SILENTLY DROPS unknown
      named arguments in that case — so the wrong-switch-name assertion would have gone green
      against a binder that never rejected anything. The fixture now mirrors `ci.ps1`'s real
      `[CmdletBinding()]`. This is exactly the TASK-0016 lesson (a fixture must reproduce the real
      artefact's shape) reappearing in a different disguise.
  (b) invoking the template via a raw `-File` argv list re-parses each `-Xxx`-shaped element as a
      new top-level parameter before the template runs — a separate quirk from the one fixed here.
      The harness therefore runs via `-Command`, matching what a contributor actually types.
  Accepted limitation, recorded not carded: a parameter VALUE that itself starts with `-` would be
  misread as the next switch name. No `ci.ps1` parameter can take such a value (a two-member
  ValidateSet string, a non-negative int, three switches), and the `.PARAMETER GateArgs` block
  states the rule. Revisit only if a gate parameter ever accepts a negative number or a
  dash-leading string.
  No `ci.ps1`-green criterion on this card, deliberately — the self-test harness is the specified
  evidence and a full suite run was out of scope. `ci.ps1` parameters, the credential-resolution
  logic and the CI workflows are all untouched, as required.
