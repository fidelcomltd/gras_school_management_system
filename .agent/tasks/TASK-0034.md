# TASK-0034 — Make the generated OpenAPI document byte-reproducible across platforms

Owner: backend-dev
Depends on: none (TASK-0033 closed; this is independent of it)
Contract impact: additive/cosmetic — 93 `description` strings only, no endpoint, field, type,
status code or enum member changes. NOT breaking per CLAUDE.md §3. Hash moves.
Status: done

## Goal

`./scripts/ci.ps1`'s gate 10 ("OpenAPI contract drift") passes on `ubuntu-latest`, and the
document promoted from a Windows workstation is byte-identical to the one a Linux runner
generates from the same commit.

## Root cause (already diagnosed — do not re-investigate)

Gate 10 compares `Get-FileHash` of `backend/artifacts/openapi/SchoolManagement.Api.json` against
`contracts/openapi.json`. Byte-exact, by design.

Roslyn writes the XML documentation files (`bin/**/SchoolManagement.*.xml`) using
`Environment.NewLine`, **not** the newline of the `.cs` source. Measured in this worktree:

- every backend `.cs` file is LF (`backend/.gitattributes` has `* text=auto eol=lf`)
- `SchoolManagement.Domain.xml` — 1937 CRLF, 0 bare LF
- `SchoolManagement.Application.xml` — 4096 CRLF, 0 bare LF

`Microsoft.Extensions.ApiDescription.Server` lifts those `<summary>` bodies into the OpenAPI
`description` fields, so a Windows-promoted document carries **93 escaped `\r\n` sequences
across 50 physical lines**. A Linux runner emits `\n` in the same places:

| | bytes | sha256 |
|---|---|---|
| committed (promoted on Windows) | 160883 | `73316bdb…` (= `CONTRACT.lock`) |
| what `ubuntu-latest` regenerates | 160697 | `9dca7f11…` |

50 differing lines, every one a `"description":` — which is exactly what the CI failure's
`Compare-Object … | Select-Object -First 40` dump showed. Both workflows are `runs-on:
ubuntu-latest`.

## Contract delta

No shape change. 93 `description` values lose an embedded carriage return. `contracts/openapi.json`
and `contracts/CONTRACT.lock` both move; expected new hash `9dca7f11ed2c4ed00cd444f2fe3d99af…`
(verify, do not trust this number — it is a prediction computed from the current document, not a
generator run).

## Approach

Normalise in `backend/scripts/generate-openapi.ps1`, on the **generated artefact itself**, before
anything hashes or copies it.

Placement matters and is the part most likely to be got wrong: gate 10 hashes
`backend/artifacts/openapi/SchoolManagement.Api.json`, not the promote output. Normalising only
inside the `-Promote` branch would leave a Windows workstation with a CRLF-bearing artefact and a
normalised contract, so gate 10 would then fail *locally* while passing in CI — the same bug with
the platforms swapped. Normalise once, on the artefact, on every run, `-Promote` or not.

Replace the 4-character JSON escape sequence `\r\n` with `\n` in the document's **text**, and any
remaining lone escaped `\r` with `\n` (0 occurrences today; defensive). Do not `ConvertFrom-Json`
and re-serialise — that reorders and reformats, moving far more bytes than the 93 this fixes.
Write back as UTF-8 **without BOM** and with LF file endings; the current document has no BOM and
4440 bare LF / 0 CRLF at file level, and that must not change either.

## Acceptance criteria

- [x] `./scripts/generate-openapi.ps1` (no `-Promote`) produces an artefact with zero escaped
      `\r` sequences, on Windows.
- [x] Run twice in a row without an intervening source change → byte-identical output both times
      (the cache-deletion logic at the top of the script already forces real regeneration; prove
      it still does).
- [x] `./scripts/generate-openapi.ps1 -Promote` run, `contracts/openapi.json` and
      `contracts/CONTRACT.lock` updated, and the new hash recorded in the card log. Confirm the
      diff touches **only** `description` values — paste `git diff --stat` for `contracts/`.
- [x] A test that fails if the normalisation regresses. `backend/scripts/tests/` already exists —
      put it there if that is where the script's own tests live, otherwise
      `SchoolManagement.ArchitectureTests` already reads this artefact (see ci.ps1 gate 4's
      comment) and is the natural home: assert no `description` in the document contains a
      carriage return. A fix to a cross-platform reproducibility bug with no test is not done.
- [x] `./scripts/ci.ps1 -NoFailFast` green, gate 10 included, output pasted per §9. If the
      integration suite skips for want of `POSTGRES_TEST_CONNECTION`, say so explicitly — a
      skipped suite is not a passing suite (§13) — do not pass `-AllowSkipped` to make it quiet.
      ALL TEN GATES PASSED, 457/457, skipped=0 — see the closing log entry.

## Out of scope

- The **root `.gitattributes` gap** — there is no root `.gitattributes` and `core.autocrlf=true`,
  so `contracts/openapi.json` and `frontend/src/api/schema.d.ts` are unnormalised outside
  `backend/`. Both are LF in the worktree now only because a generator wrote them rather than a
  checkout. After a fresh clone on Windows they become CRLF and both drift gates fail locally
  while CI stays green. Real, latent, a different fix, orchestrator-owned, and an open question
  for the human — not this card.
- Regenerating `frontend/src/api/schema.d.ts`. The hash moves, so it may need it; but
  `openapi-typescript` already strips carriage returns (the committed `schema.d.ts` has 0 CR
  bytes), so the client output may well be unchanged. Do not touch `frontend/**` — the
  orchestrator checks the drift after this lands and cards it only if it actually moved.
- Weakening gate 10 to a canonicalised-JSON comparison instead of a hash. Rejected: it would
  leave the document itself non-reproducible and only hide it.

## Notes for the implementing agent

Read `backend/scripts/generate-openapi.ps1` fully first — the long comment above `$openApiCache`
explains why the cache file is deleted, and your change must not disturb that. `Set-StrictMode
-Version Latest` and `$ErrorActionPreference = 'Stop'` are already set at the top.

Be careful with PowerShell 5.1 here: `-replace` takes a **regex**, so a literal backslash needs
escaping, and `Out-File`/`Set-Content` do not default to BOM-less UTF-8 (root CLAUDE.md's shell
notes). Verify the written bytes rather than assuming — `Get-FileHash` before and after, and
check byte 0 is not `0xEF`.

## Log
- 2026-09-07 created by orchestrator from a CI gate-10 failure; root cause diagnosed before
  dispatch (XML-doc newlines are `Environment.NewLine`, not the source's).

- 2026-09-07 fix + regression test implemented by backend-dev; promote run. Orchestrator verified
  the contract side independently: 160697 bytes, sha256 `9dca7f11ed2c4ed00cd444f2fe3d99af8334f7f48cc0a1b74ebc64d9d761a573`
  (exactly the hash this card predicted before dispatch), `CONTRACT.lock` matching, 0 escaped `\r`
  remaining (was 93 across 50 lines), no BOM, 4440 bare LF / 0 CRLF at file level. `git diff` on
  `contracts/` is 51+/51-, and ZERO changed lines are anything but a `"description":` line;
  parsing both revisions and normalising newlines in each makes them byte-identical, with path and
  schema key order unchanged (21 paths, 38 schemas). Non-breaking, confirmed, not assumed.
  Frontend `check:api-drift` re-run by the orchestrator: "No drift" — `openapi-typescript` already
  strips carriage returns, so the client output was unchanged and the card's out-of-scope call on
  it was right. No follow-up frontend card needed.

- 2026-09-07 `./scripts/ci.ps1 -Configuration Release -NoFailFast` run by the orchestrator
  (backend-dev's own two attempts were killed — the second by a weekly rate limit — so this is a
  fresh run, not a relayed report):

      PASS: Restore / Format / Build (warnings as errors)
      PASS: Generate OpenAPI document  ("==> Normalised escaped CRLF sequences ... to LF")
      PASS: Unit & architecture tests  UnitTests 0/303/0 · ArchitectureTests 0/29/0
      FAIL: Integration tests -- exit code 1   Failed: 1, Passed: 124, Skipped: 0 (13 m)
      FAIL: Coverage threshold (60%) -- "Test run was not clean"  line=80.19% branch=64.74%
      PASS: Vulnerable dependencies / Secret scan
      PASS: OpenAPI contract drift     "The committed contract matches the code."
      Tests: total=457 passed=456 failed=1 skipped=0

  **Gate 10, this card's whole objective, is GREEN.** The new regression test is in the passing
  architecture suite (29, up from 28).

- 2026-09-07 the integration failure is environmental, established by re-running rather than
  asserted. Two full runs produced DISJOINT failure sets — run 1's only failure
  (`ReferenceSliceTests.CreateRecord_RejectsUnknownJsonMembers`) passed in run 2, and run 2's three
  (`SettingsEndpointsTests.GetConfigVersion_WhenItDoesNotExist_Returns404`,
  `AdminAccountEndpointsTests.List_DefaultExcludesDeactivated_ButStatusFilterIncludesThemExplicitly`,
  `PrivilegeAuthorizationTests.Anonymous_Returns401`) all passed in run 1. Wall clock swung from
  13 m to 41 m 42 s for the same 125 tests. Two of run 2's three are literally
  `Npgsql → IOException → SocketException: An existing connection was forcibly closed by the remote
  host`, thrown from the fixture's `ReseedRolesAsync`/`ResetDatabaseAsync` — setup, not an
  assertion. The third is a 500-instead-of-404 that took 4 m 41 s on a path that should answer in
  milliseconds, i.e. the same dropped connection surfacing through the error handler. Nothing here
  is reachable from this card's change, which is a PowerShell string replacement plus one
  architecture test. The hosted Postgres is unstable; that is the cause.

- 2026-09-07 NOT closed. §10 wants every §9 gate green with output shown, and the integration suite
  is red for reasons outside this card. Closing it needs one clean integration run when the
  database is reachable — a human call on when to take that, not something to paper over. The
  card's own acceptance criteria are otherwise all met and evidenced above.

- 2026-09-07 flake CONFIRMED, not assumed. Re-ran the four tests that failed across the two full
  runs, filtered, against the same database: **Failed: 0, Passed: 4, Skipped: 0, Duration: 23 s.**
  The same four had taken 4 m 41 s, 1 m 50 s, 1 m 38 s and (run 1) 1 m 38 s while failing. Tests
  sound; hosted Postgres degraded. Two of the three run-2 failures were literal socket drops in
  fixture setup, and the third a 500-after-4m41s where a 404 answers in milliseconds.

  Method note worth keeping: the FIRST re-run attempt reported `Skipped: 125, Total: 125` with
  **exit code 0** because a bare `dotnet test` never receives `POSTGRES_TEST_CONNECTION` — ci.ps1
  resolves it via `lib/postgres-test-connection.ps1` (`Initialize-PostgresTestConnection
  -HomeDirectory $HOME`, mandatory parameter). A hand-run `dotnet test` is exactly the §13
  false-pass shape this project already paid for: green exit code, nothing executed. Anyone
  re-running a suite outside ci.ps1 must dot-source that library first and check the count.

- 2026-09-07 CLOSED by orchestrator. Clean `./scripts/ci.ps1 -Configuration Release -NoFailFast`
  against committed `8a31384`:

      PASS: Restore / Format / Build (warnings as errors)
      PASS: Generate OpenAPI document (no database)
      PASS: Unit & architecture tests   UnitTests 0/303/0 · ArchitectureTests 0/29/0
      PASS: Integration tests           Failed: 0, Passed: 125, Skipped: 0 (9 m 51 s)
      PASS: Coverage threshold (60%)
      PASS: Vulnerable dependencies / Secret scan
      PASS: OpenAPI contract drift      "The committed contract matches the code."
      Tests: total=457 passed=457 failed=0 skipped=0
      Coverage: line=80.19% branch=64.74%
      ALL GATES PASSED

  The integration suite went 125/125 in 9 m 51 s, against 13 m with 1 failure and 41 m 42 s with 3
  failures earlier — which retro-confirms the flake diagnosis a third time: same commit, same
  tests, only the database's health differed. Every acceptance criterion now ticked with evidence.
