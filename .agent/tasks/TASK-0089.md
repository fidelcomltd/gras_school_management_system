# TASK-0089 — Regenerate the typed client against `584a4a3c…`

Owner: frontend-dev
Depends on: TASK-0088 (contract promoted 2026-09-21, commit `11cce0f`)
Contract impact: **none**. This card consumes a contract that is already promoted, and must not change one byte of `contracts/**`.
Status: dispatched
Reads: `frontend/src/api/README.md`; `.agent/rules/wire.md`; `.agent/rules/gates.md` §1 (frontend row); `.agent/spec/frontend.md`.

## Goal

Make `src/api/schema.d.ts` match the committed contract, so frontend CI stays green on TASK-0088's PR and the future readiness
screen has typed operations.

## Contract delta

None. `jq` only what you need:
- 2 new paths: `GET /api/v1/arms/{armId}/readiness` (with a `termId` query) and `POST /api/v1/result-sets/{resultSetId}/submit`.
- 11 new schemas, including the string enum `MarkCompletionStatus` (`Complete` | `Partial` | `Empty`) and
  `ResultSetNotReadyProblemDetails`, the contract's first typed problem body on a 422.
- **No existing path or schema changed** (verified by `jq`), so no fixture or mock should need touching. If typecheck says otherwise, report it.

## Acceptance criteria

- [ ] `npm run generate:api`, then `npm run check:api-drift` reports `No drift` with exit 0. Do not hand-edit `schema.d.ts`.
- [ ] `npm run typecheck` and `npm run lint` exit 0. Leave the pin and the lockfile untouched (`openapi-typescript@7.13.0`).
- [ ] **No new wrapper code.** `apiGet` and `apiPost` already exist. If the typed 422 body cannot be reached through the existing
      error path, REPORT it; do not build a helper. The readiness screen card will own that.
- [ ] No screens, queries or hooks consume the new types yet.

## Out of scope

The readiness screen, submit button, any `backend/**` or `contracts/**` change.

## Notes for the implementing agent

- **Nearest shape:** TASK-0087 did exactly this one contract ago.
- Do NOT run `npm run verify` or e2e (`gates.md` §1). Never git push, pull, merge or rebase.
- **Do not write to `.agent/**`.** Put a `LEDGER ACCOUNT` in your report instead. Commit `schema.d.ts` on `task-0088` as
  `feat(client): regenerate against 584a4a3c` with `Refs: TASK-0089`.

## Log

- 2026-09-21 carded and dispatched by the orchestrator.
