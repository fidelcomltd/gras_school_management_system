# TASK-0004 — Wire the OpenAPI client generator and the typed API layer

Owner: frontend-dev
Depends on: —
Contract impact: none (consumes the committed contract; changes nothing on the wire)
Status: done

## Goal

Close the last gap in the contract pipeline. `contracts/openapi.json` has existed since
2026-08-08 and `frontend/src/api/` is still empty, so the frontend currently cannot call the
backend without guessing across the boundary — the one thing CLAUDE.md forbids outright. When
this lands, a backend contract change propagates to the frontend by running one script, and a
hand-edited client is a detectable blocker rather than an invisible one.

## Contract delta

None. This card consumes `contracts/openapi.json` exactly as committed. It must not require any
backend change. If the committed contract turns out to be unusable as-is, **stop and report** —
do not work around it in the client.

## Acceptance criteria

- [x] `openapi-typescript` added as a devDependency, pinned (no `latest`), lockfile committed.
      The choice is settled — see `STATE.md` Decisions 2026-08-26; do not substitute
      openapi-fetch, NSwag or Kiota. Pinned exact at `7.13.0` (no `^`).
- [x] An npm script generates `frontend/src/api/schema.d.ts` from `../contracts/openapi.json`.
      Generated output carries a `// GENERATED — DO NOT EDIT` header.
      `npm run generate:api` → `frontend/scripts/generate-api-schema.mjs`.
- [x] A second npm script verifies the generated output is current by regenerating to a temp
      path and diffing — non-empty diff exits non-zero. This is CLAUDE.md §4.4 check 2 and it
      must be runnable locally and in CI. `npm run check:api-drift` →
      `frontend/scripts/check-api-schema-drift.mjs`. Verified both directions (clean → exit 0;
      tampered file → exit 1 with a real `git diff --no-index` printed).
- [x] A thin typed request helper lives in `src/api/`, layered over the existing axios transport
      in `src/lib/http/`. It must **not** introduce a second HTTP stack, and must not bypass the
      existing interceptors, `ApiError` normalisation or the auth seam.
      `frontend/src/api/client.ts` — `apiGet`/`apiPost` call straight through to
      `getRequest`/`postRequest` from `@/lib/http`; no new axios instance, no auth code touched.
- [x] At least one real call is proved end to end against a `/api/v1/reference/*` path, typed
      from the generated schema, with the response type inferred rather than asserted.
      `frontend/src/api/client.test.ts` — three cases (ping, records list, records create)
      against the real transport + contract-derived MSW mock, no `as`/`!` on the results.
- [x] A lint rule or test fails on any raw `fetch(`/`axios` call to the API host outside
      `src/lib/http/` — CLAUDE.md §4.4 check 3, currently unenforced.
      `frontend/src/test/http-boundary.test.ts` (a test, not a lint rule — oxlint has no
      directory-scoped import-restriction rule available; a Vitest test was more reliable and
      gives a readable failure listing offending files).
- [x] MSW handlers in `src/test/msw/` are derived from the OpenAPI document rather than
      hand-written, so mocks cannot drift from the contract (CLAUDE.md §7 Testing).
      `frontend/src/test/msw/openapi-handlers.ts` builds one default handler per operation from
      `contracts/openapi.json`'s own `example` values; `handlers.ts` exports the result.
- [x] `npm run verify` green — typecheck, lint (zero warnings), 120+ tests, build. Output pasted.
      124/124 tests (120 prior + 4 new). See Log / orchestrator report for pasted output.
- [x] `frontend/src/api/README.md` updated to describe the pipeline as built rather than as
      proposed.

## Out of scope

- Any feature UI, screen, route or business logic. This card wires a pipeline; it ships no
  product surface.
- The frontend CI workflow — root-level CI is orchestrator-owned (§11). Report the exact
  commands CI should run and the orchestrator writes the workflow.
- Deleting `src/screens/scaffold-status/`. It goes when the first real screen lands, not here.
- Touching `src/lib/auth/`. The carrier is decided (HttpOnly cookie + CSRF) and the existing
  bearer implementation is therefore known-wrong code — but replacing it is TASK-0003, not this
  card. Layer over the transport as it stands; do not start the rewrite, and do not build
  anything that depends on the bearer seam surviving.
- Adding `react-hook-form` or any other dependency beyond the generator. Dependency additions
  need their own justification in a card (§8).

## Notes for the implementing agent

Read `.agent/STATE.md` first and append to it last (§4.1). Then `frontend/HANDOFF.md` — it is
the previous frontend session's own account of what exists and why, including why `src/api/` was
deliberately split from `src/lib/http/` (decision 3) and what `src/api/README.md` already
records about the intended pipeline.

The current contract surface is small — `/api/v1/reference/ping`, `/api/v1/reference/records`,
`/api/v1/reference/whoami`, `/health/live`, `/health/ready`. That is the point: prove the
pipeline on a trivial surface now, so that when the real endpoints arrive from TASK-0002 and
TASK-0005 the only new work is features.

Note `/api/v1/reference/whoami` will likely be absorbed or replaced by TASK-0003. Do not build
anything on it that would be expensive to unpick.

Watch out: this task and TASK-0002 may run concurrently. TASK-0002 regenerates the contract. If
the committed `contracts/openapi.json` hash changes under you mid-task, regenerate and re-run
the drift check before reporting — do not report against a stale document.

## Log

Full log: [logs/TASK-0004.log.md](logs/TASK-0004.log.md) — moved 2026-09-04 per §13. Card closed.
