# TASK-0042 — Academic structure screens: sessions & terms, levels & sections

Owner: frontend-dev
Depends on: TASK-0041 (shell, protected routing, feature-folder pattern). Client already current.
Contract impact: none. If a shape is missing, STOP and report it (§4.5) — do not shim it.
Status: **done** — closed 2026-09-14 by orchestrator. All 10 AC verified from the diff; gates green (350 tests, 9 e2e, Skipped 0). `decisions/2026-Q3.md`.

## Goal

An administrator can run the school year end to end from the back office: create a session and its
three terms, open and close a term, and manage the class levels and sections that everything else
hangs off — including reordering the progression chain.

## Scope

**1. Sessions & terms** (`features/sessions/`)
- List sessions (`GET /sessions`, cursor-paginated) with state.
- Create a session (`POST /sessions`, `Idempotency-Key` REQUIRED) — creates all three terms server-side.
- Session detail (`GET /sessions/{id}`) showing its three terms.
- Edit session (`PATCH /sessions/{id}`) and term (`PATCH /terms/{id}`).
- Term transitions: `POST /terms/{id}/{open,close,reopen}`. Reopen needs a reason ≥ 10 chars and
  super-admin. **Precondition failures return a naming message — surface that message, never a
  generic "something went wrong".**

**2. Levels & sections** (`features/classes/`)
- Level list (`GET /levels`) in chain order, showing entry and graduating flags, `?status=all` toggle.
- Create level (`POST /levels`, `Idempotency-Key` REQUIRED) including the **insert-after** path —
  pick a predecessor and the server rewires the chain.
- Edit (`PATCH /levels/{id}`); changing status additionally needs `level.deactivate`.
- Delete (`DELETE /levels/{id}`) — surface the 409's named references.
- Reorder (`POST /levels/reorder`) — whole ordered array, atomic.
- Sections: list, create, rename (`/sections`, `/sections/{id}`), gated by `level.*`.

## Acceptance criteria

- [x] Every screen routed behind the TASK-0041 guards, with nav entries appearing only for callers
      holding the relevant privilege.
- [x] All 17 operations exercised against MSW handlers derived from the committed contract, not
      hand-written fixtures.
- [x] **The eight chain rules' rejection messages reach the user verbatim.** A 422 from
      `POST|PATCH /levels` renders the server's message text; do not paraphrase or map it to a
      generic string. Same for term-transition precondition failures.
- [x] Level list renders in `progressionOrder`, not alphabetically, with entry and graduating
      levels marked. `?status=all` reveals inactive levels.
- [x] Insert-after creation proves the chain rewired — assert the list order after, not just a 201.
- [x] Reorder posts the whole ordered array and re-renders from the response.
- [x] `Idempotency-Key` sent on `POST /sessions` and `POST /levels`; generated per submit, not
      reused across distinct submits.
- [x] Term reopen is unavailable to a non-super-admin caller and requires the ≥10-char reason.
- [x] Every API call through `src/api/` (§4.4 check 3).
- [x] `npm run verify` green with counts, `Skipped: 0`; `npm run test:e2e` green with one new spec
      covering create-session → open-term, and one covering create-level-by-insert-after.

## Cut line, if you run short

Ship both list + create + edit paths first. **Drag-and-drop reorder is the piece to cut** — a
numeric order field or move-up/move-down buttons posting the same `POST /levels/reorder` array
satisfies the contract and the acceptance criterion. Report the cut; do not ship it half-tested.

## Out of scope

- Arms. The backend does not exist yet (TASK-0039).
- Admins/roles screens — a later card, even though the client is current.
- Pupils, results, pins, portal. No backend.
- New component libraries or styling approaches. Design tokens and Base UI only.
- Any change to `src/api/**`, `backend/**`, `contracts/**`.

## Notes for the implementing agent

`features/settings/` (TASK-0041) is now the reference feature folder — follow its shape and its
route-module pattern (`*-routes.tsx` spread into `app-router.tsx`). Reuse `require-privilege.tsx`
and `authenticated-shell.tsx` rather than re-implementing either.

TASK-0041 left the 422 field-error tolerant-lookup helper duplicated in two places, deliberately,
per CONVENTIONS.md §4's "promote only in its own commit". **This card is the third user — promote
it to `src/shared/` now, as its own focused change**, and say so in your report.

Enums (`SessionState`, `TermState`, `LevelStatus`) cross as strings and the client must tolerate an
unknown member without crashing (§8). Render an unrecognised state as its raw string rather than
throwing or blanking.

## Log

Archived to `.agent/tasks/logs/TASK-0042.log.md` on 2026-09-14 at close.
- 2026-09-14 reviewed and CLOSED by orchestrator. Account: `.agent/decisions/2026-Q3.md`.
