# TASK-0015 — Backend scaffold conformance fixes from the §6 audit

Owner: backend-dev
Depends on: —
Contract impact: none
Status: queued

## Goal

TASK-0001's §6 audit found the backend materially exceeds spec, with two blockers carded separately
(TASK-0012, TASK-0013) and three should-fix items. Two of the three are the same class of problem the
Phase 0b cards existed to remove: a check that can pass without meaning anything, and a rule that is
documented rather than enforced. Close them.

## Contract delta

None.

## Acceptance criteria

- [ ] **`/health/ready` must not be an unauthenticated, unthrottled database round trip.**
      `Endpoints/HealthEndpoints.cs:57-65`. Health checks are mapped on `app`, not the rate-limited
      `versionedApi` group (`Program.cs:261-264`), and no global limiter exists (`Program.cs:167-213`
      registers named policies only) — so an anonymous caller can drive one DB query per request
      (`AddDbContextCheck`, `InfrastructureDependencyInjection.cs:114`). §6: "Rate limiting middleware
      on public endpoints."
      The tension the file itself documents is real: throttling a probe causes false unhealthy
      signals and fleet restarts. Resolve it with a dedicated generous policy sized for real probe
      intervals, NOT by reusing an endpoint policy, and NOT by removing the DB check from `ready` —
      readiness genuinely means the database is reachable. State the numbers you chose and the probe
      interval they assume. If you conclude this belongs at the ingress rather than the app, say so
      with reasoning and record it as drift instead of implementing something worse.
- [ ] **Move the persistence-error translation behind an Application port.**
      `Http/GlobalExceptionHandler.cs:3` imports `SchoolManagement.Infrastructure.Persistence` and
      calls `PersistenceErrors.TryTranslate(exception)` inside the HTTP pipeline. The Api project's
      own `.csproj` header scopes its Infrastructure reference to "register services in DI
      (`Program.cs`)"; this is outside that scope, and it puts a persistence concern in the Api layer.
      §6 layering. Introduce an Application abstraction implemented by `PersistenceErrors` and inject
      it.
- [ ] **Add the missing architecture test that would have caught it.** `DependencyDirectionTests`
      asserts that `Application` does not depend on Infrastructure or Api, and that nothing depends on
      Api — but nothing constrains **Api -> Infrastructure**. That is the one layering rule in the repo
      that is documented rather than enforced, which is why the above slipped in. Add the rule, with
      whatever narrow exemption `Program.cs` genuinely needs for DI registration.
- [ ] **The relocated document tests must not pass against a stale artefact.**
      `tests/SchoolManagement.ArchitectureTests/OpenApiDocument.cs:38-44` checks only `File.Exists`.
      Inside `ci.ps1` that is sound — a dedicated gate regenerates immediately before `dotnet test`.
      Outside it, a bare `dotnet test` asserts all eight document properties against whatever JSON is
      on disk, which may predate the current `src`. **Missing fails loudly; stale passes silently** —
      the same defect family as TASK-0010, where a target reported success over a document it had not
      written. Compare the artefact against the newest file under `src/SchoolManagement.Api` by
      timestamp, or hash-check it, and fail with a message as actionable as the existing missing-file
      one.
- [ ] `./backend/scripts/ci.ps1` green with the integration suite RUNNING (`Skipped: 0`) — use
      `backend/scripts/local-env.ps1`. A skipping run is not a pass; the coverage floor also stands
      down when anything skips, so a skipping run degrades two gates while printing green.
- [ ] Contract hash unchanged at `228ae57f`. Nothing here should move the contract; if it does, that
      is a defect — stop and report.

## Out of scope

- The two blockers. `TASK-0012` owns the problem-schema extension members; `TASK-0013` owns
  idempotency. Do not touch either here.
- The coverage attribution question. 77.18% vs the 87.00% recorded at TASK-0002's close is fully
  explained in `.agent/AUDIT.md` and is a loss of attribution, not of checking. **Do not try to
  restore the number** — doing so would mean re-adding an in-process document fetch purely to move a
  metric.
- The nice-to-haves recorded in `.agent/AUDIT.md`: `SampleRecord.cs:11`'s stale `xmin` doc comment,
  the absence of a rule enforcing that async methods *accept* a `CancellationToken`, and
  `RateLimitingOptions.SensitivePolicyName` being registered but unused. Recorded, deliberately not
  carded. The last one gets its first real user in TASK-0003.
- The validation-as-pipeline-behaviour deviation (`ASSUMPTIONS.md` §2.2). Now recorded in `STATE.md
  ## Known drift` with its ratify-or-revert question open; that is a decision, not this card's work.

## Notes for the implementing agent

Read `.agent/STATE.md` first and append last. `.agent/AUDIT.md` findings **S1, S2, S3** are the full
writeups.

Worth knowing before you start: the audit's verdict on this scaffold was that it exceeds spec, and
that the rules §6 cares most about are mechanically enforced rather than merely followed. Two of the
three items here are about extending that enforcement to the last places it is missing. Match the
existing style — this codebase promotes analyser rules to errors and writes architecture tests rather
than relying on review, and your additions should do the same.

## Log
- 2026-08-27 created by orchestrator from TASK-0001's §6 audit findings S1-S3.
