# TASK-0037 — Regenerate the client for the sessions and terms contract

Owner: frontend-dev
Depends on: TASK-0035
Contract impact: none — consumes a contract that has already moved
Status: done

## Goal

`npm run check:api-drift` green again, and the eight new operations reachable through the typed
wrapper surface rather than a raw call.

## Why it exists

TASK-0035 moved the contract from `9dca7f11…` to `a618db62…` (223737 bytes, was 160697): six new
paths — `/sessions`, `/sessions/{id}`, `/terms/{id}`, `/terms/{id}/open`, `/terms/{id}/close`,
`/terms/{id}/reopen` — and eleven new schemas. `frontend/src/api/schema.d.ts` was last generated
against `9dca7f11…`, so §4.4 check 2 is RED right now — confirmed by running it. Third card of
this shape after TASK-0029 and TASK-0033, and deliberately separate for the same reason: the
backend card does not regenerate the client, and the regeneration is not left implicit.

## Acceptance criteria

- [x] `npm run generate:api` run against the COMMITTED `contracts/openapi.json` (never a locally
      rebuilt document), `npm run check:api-drift` then reporting no drift. Hash independently
      recomputed with `sha256sum` first — matches `CONTRACT.lock` byte for byte
      (`a618db6208e45fd846648537baf9d1eb10d256587530ad182c4d490f1eb8c2a6`).
- [x] The eight operations reachable through TASK-0029's existing generic
      `apiGet`/`apiPost`/`apiPatch` surface. ZERO new lines in `client.ts`/`client-types.ts` —
      only `schema.d.ts` regenerated plus new test files.
- [x] `POST /sessions` requires `Idempotency-Key`; the three term transitions and both `PATCH`es
      accept it optionally (CSRF is transport-owned, not a `client.ts` field — see README). Both
      negative directions proven: `POST /sessions` omitting `Idempotency-Key` fails typecheck;
      `GET /sessions` (declares no `Idempotency-Key`) rejects one being passed. Every
      path-parameter op proven to reject an omitted required path parameter. All 8
      `@ts-expect-error`s verified load-bearing by the removal-probe method (removed one at a
      time, confirmed `npm run typecheck` fails at exactly that line, restored, re-verified
      green).
- [x] The `Upcoming`/`Active`/`Closed` state enums (wire values are PascalCase, matching
      `RoleStatus`'s convention) cross as strings and the client tolerates an unknown member
      without crashing (§8) — proven for BOTH `SessionState` (via `ListSessions`) and `TermState`
      (via `OpenTerm`), the sibling proof `client-roles.test.ts` established for `RoleStatus`.
- [x] `npm run verify` (typecheck/lint/test/build) and `npm run test:e2e` green, output pasted.
      `npm run test`: 23 files, 186 passed, 0 failed, Skipped: 0. `test:e2e`: 4 passed,
      Skipped: 0.
- [x] Respected CONVENTIONS.md §3's 180-line cap by adding two new files instead of growing
      `client-roles.test.ts` (already at 176): `client-sessions.test.ts` (126 lines) and
      `client-terms.test.ts` (117 lines).

## Out of scope

Any session, term or promotion SCREEN. This card is the client seam only. The back-office shell
and the settings screens are still unwritten and are still separate cards.

## Log
- 2026-09-07 created by orchestrator on reviewing TASK-0035; drift check confirmed RED first.
- 2026-09-07 implemented by frontend-dev — client regenerated against `a618db62…`; closed same
  session.

  `npm run generate:api` re-run against the committed contract (hash independently recomputed
  with `sha256sum` beforehand, matches `CONTRACT.lock`). Rewrote `src/api/schema.d.ts` —
  +1361/-40 per `git diff --stat`, purely additive: `CreateSession`, `ListSessions`,
  `GetSession`, `UpdateSession`, `UpdateTerm`, `OpenTerm`, `CloseTerm`, `ReopenTerm` plus eleven
  schemas (`SessionDto`, `SessionDetailDto`, `CreateSessionCommand`, `CreateSessionTermInput`,
  `UpdateSessionCommand`, `SessionState`, `CursorPageOfSessionDto`, `TermDto`, `TermState`,
  `UpdateTermCommand`, `ReopenTermCommand`). `check:api-drift` → "No drift." Third confirmation
  of TASK-0033's finding: a new operation on an already-generic HTTP method needs no
  hand-written code in `client.ts`/`client-types.ts` at all.

  Two new test files, colocated with `client.ts`: `client-sessions.test.ts` (126 lines —
  `CreateSession`/`ListSessions`/`GetSession`/`UpdateSession`) and `client-terms.test.ts` (117
  lines — `UpdateTerm`/`OpenTerm`/`CloseTerm`/`ReopenTerm`), neither growing an existing file
  past CONVENTIONS.md §3's 180-line cap. `src/api/README.md` updated to name both.

  **One real bug caught while writing the `TermState` enum-tolerance test**: the MSW
  `server.use(...)` override used the contract's own `{id}` path-template syntax
  (`apiUrl('/api/v1/terms/{id}/open')`), but MSW matches path params as `:id` —
  `openapi-handlers.ts`'s `toMswRoute` helper does that conversion for the default handlers, and
  a hand-written override bypasses it and needs the same conversion by hand. First run produced
  a genuine (non-type) test failure — the default contract-derived handler answered instead of
  the override, because the override's route never matched, so the assertion saw the example
  payload's `"Upcoming"` instead of the injected `"SomeFutureTermState"`. Fixed by writing `:id`
  in the override (with a comment pointing at `toMswRoute` for the next person), not by loosening
  the assertion; re-ran green.

  **All 8 `@ts-expect-error`s verified load-bearing**, same removal-probe method TASK-0029
  established: for each, removed just that comment line from a backup copy, ran `npm run
  typecheck`, confirmed a failure at exactly that line (`TS2554: Expected 3 arguments, but got
  2` for the four required-path-parameter/required-`Idempotency-Key` cases; `TS2353: Object
  literal may only specify known properties, and 'idempotencyKey' does not exist` for the
  declares-no-`Idempotency-Key` case on `GET /sessions`), then restored from backup and
  re-verified `npm run typecheck` green. Re-verified the two `client-terms.test.ts` probes whose
  line numbers shifted after the MSW-route fix, against a fresh post-fix backup, rather than
  trusting stale line numbers.

  **Gates, all run directly by this session, in order**: `npm run typecheck` (`tsc -b`) 0
  errors; `npm run lint` (`oxlint --max-warnings=0`) 0 warnings; `npm run test` (`vitest run`)
  **23 files, 186 passed, 0 failed, Skipped: 0** (was 21 files / 167 passed; the two new files'
  19 cases, 10 + 9, account for the delta exactly); `npm run build` (`tsc -b && vite build`)
  succeeded, 323 modules, 9 chunks, unchanged; `npm run check:api-drift` "No drift";
  `npm run test:e2e` (`playwright test`) **4 passed, Skipped: 0**, unchanged (no e2e spec
  touches `/sessions`/`/terms`). `src/test/http-boundary.test.ts` (§4.4 check 3) re-run
  directly: 2 passed.

  **Files changed**: `frontend/src/api/schema.d.ts` (regenerated, +1361/-40),
  `frontend/src/api/client-sessions.test.ts` (new, 126 lines),
  `frontend/src/api/client-terms.test.ts` (new, 117 lines), `frontend/src/api/README.md`
  (+13/-4). Nothing under `contracts/**` or `backend/**` touched; hash unmoved at `a618db62…`.

  **Confirmed out of scope, not built**: no session/term/promotion screen, no TanStack Query
  hooks, no `features/sessions`/`features/terms` folder — client seam only.
