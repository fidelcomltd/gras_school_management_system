# TASK-0043 — Admin accounts and roles screens

Owner: frontend-dev
Depends on: TASK-0041 (shell, guards), TASK-0042 (feature patterns, shared form helpers).
Contract impact: none. If a shape is missing, STOP and report it (§4.5).
Status: **done** — closed 2026-09-14 by orchestrator. All 9 AC verified; gates green. Spec 6.1.2's self-edit carve-out NOT built and no AC named it — drift re-triggered to `backend-dev`. `decisions/2026-Q3.md`.

> **This is the last product surface with a live backend.** After it, every remaining module
> (arms, pupils, subjects, results, pins, portal) needs backend work first. Finish it and the
> "API shipped, no screen" gap is closed entirely.

## Goal

An administrator can manage who else has access: create and edit admin accounts, suspend and
reactivate them, reset a password, revoke sessions, and define the roles and privileges those
accounts carry.

## Scope

**1. Admin accounts** (`features/admins/`)
- List (`GET /admins`, cursor-paginated), detail (`GET /admins/{id}`).
- Create (`POST /admins`, `Idempotency-Key` REQUIRED) — surfaces the generated temporary password
  **once**, with a clear "this will not be shown again" affordance.
- Edit (`PATCH /admins/{id}`), status change (`PATCH /admins/{id}/status`) for suspend/reactivate.
- Password reset (`POST /admins/{id}/password-reset`) — same one-time-reveal treatment.
- Revoke sessions (`DELETE /admins/{id}/sessions`).

**2. Roles** (`features/roles/`)
- List (`GET /roles`), detail (`GET /roles/{id}`), create (`POST /roles`, `Idempotency-Key`
  REQUIRED), edit (`PATCH /roles/{id}`), delete (`DELETE /roles/{id}`).
- Privilege picker built from `GET /privileges` — the 93-row register, grouped by its module
  field. It is authenticated-only and needs no privilege of its own.
- System roles cannot be deleted; surface the 409's message verbatim.

## Acceptance criteria

- [x] Every screen behind the TASK-0041 guards; nav entries only for callers holding the privilege.
- [x] All 13 operations exercised against contract-derived MSW handlers, not hand-written fixtures.
- [x] **The temporary password is revealed exactly once and never re-fetched.** Assert it is absent
      from any subsequent read of the account. This value is credential material — it must not land
      in a query cache that a later render can re-display, and must never be logged (§8, PII).
- [x] Privilege picker groups by the register's module field and round-trips a selection through
      create and edit. Handles a privilege code the UI does not recognise without crashing (§8).
- [x] Deleting a system role surfaces the server's own 409 message verbatim.
- [x] Suspend → reactivate round-trips and the list reflects the new status without a full refetch
      where the response already carries it.
- [x] Revoking sessions asks for confirmation naming the account first.
- [x] Every API call through `src/api/` (§4.4 check 3).
- [x] `npm run verify` green with counts, `Skipped: 0`; `npm run test:e2e` green with one new spec
      covering create-admin → reveal temp password → suspend.

## Cut line, if you run short

Ship admin accounts complete first, then roles. **Within roles, the privilege picker's grouping and
search is the piece to simplify** — a flat checkbox list posting the same array satisfies the
contract. Report the cut.

## Out of scope

- Role *assignments* and scopes. That is TASK-0030, still blocked on arms — the backend does not
  exist. Do not build an assignment UI against endpoints that aren't there.
- Arms, pupils, results, pins, portal.
- Any change to `src/api/**`, `backend/**`, `contracts/**`.

## Notes for the implementing agent

`features/sessions/` and `features/classes/` (TASK-0042) are the current reference feature folders.
Reuse `src/shared/forms/field-message.ts`, `require-privilege.tsx` and `authenticated-shell.tsx`.

**The temporary password deserves real care.** The backend redacts it from stored idempotency rows
(TASK-0027), and the secret-scan gate has fired twice in this repo on example values of it. Keep it
out of React Query's cache, out of any state that outlives the dialog, and out of error paths.

Privilege codes are a fixed compile-time register — do not hardcode a copy of the 93 rows in the
frontend. Read them from `GET /privileges`.

## Log

Archived to `.agent/tasks/logs/TASK-0043.log.md` on 2026-09-14 at close.
- 2026-09-14 reviewed and CLOSED by orchestrator. Account: `.agent/decisions/2026-Q3.md`.
