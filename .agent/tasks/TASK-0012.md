# TASK-0012 — Declare the problem-detail extension members in the contract

Owner: backend-dev, then frontend-dev (sequenced)
Depends on: —
Contract impact: additive
Status: done

## Goal

Every error response this API sends carries `errorCode` and `traceId`. The contract declares
neither, and forbids both. A client that does what the schema's own description tells it to do —
"Branch on the `errorCode` extension member" — cannot, because the generated type has no such
member. Fix the contract so the two members clients are told to depend on are declared.

## The finding (TASK-0001 finding B1, verified twice)

`contracts/openapi.json`, `components.schemas.ProblemDetails` and `HttpValidationProblemDetails`:

- both declare `"additionalProperties": false`
- `ProblemDetails` declares exactly `detail, instance, status, title, type` — no `errorCode`, no
  `traceId`
- both `example` objects DO contain `errorCode` and `traceId`, so **each example is invalid against
  the schema that illustrates it**

Meanwhile the API attaches `errorCode` to every problem response
(`Http/ResultExtensions.cs:75`, `Http/GlobalExceptionHandler.cs:117`) and `traceId` centrally to all
of them, framework-generated ones included (`Program.cs:132`).

Root cause: `Program.cs:93` sets `UnmappedMemberHandling.Disallow` globally, so the generator stamps
`additionalProperties: false` on all nine schemas. Right for request bodies, wrong for RFC 9457
problem objects, where extension members ARE the mechanism.

Already propagated to the client: `frontend/src/api/schema.d.ts:292` emits a five-member
`ProblemDetails` with neither extension.

## Contract delta

Additive. Both problem schemas gain two declared members:

| Member | Type | Notes |
|---|---|---|
| `errorCode` | string | Stable, machine-readable. Clients branch on this, never on `detail`. |
| `traceId` | string | Correlation id to quote in a support ticket. |

### Decisions taken 2026-09-04 by orchestrator — implement these, do not re-litigate

**1. Both schemas stay closed.** Keep `additionalProperties: false` and declare the two members
inside it. They become part of the contract rather than untyped escape hatches, and the
closed-type discipline that is right for request bodies stays intact. Do not touch the other
seven schemas.

**2. `traceId` is `required`; `errorCode` is declared but NOT `required`.**

This is a correction to the card's own framing above, made by reading the code rather than
trusting the finding's summary. The two members do not have the same guarantee:

- `traceId` — attached in `CustomizeProblemDetails` (`Program.cs:132`), the single central hook,
  so it is on **every** problem response including framework-produced ones. `required` is a true
  statement.
- `errorCode` — attached only in `ResultExtensions.cs:75` (mapped domain failures) and
  `GlobalExceptionHandler.cs:117` (exceptions). A problem response the framework produces
  without passing through either — a model-binding 400, a 401 from auth middleware — carries
  **no** `errorCode`. Marking it `required` would declare something the API does not always
  send, which is the same class of contract lie this card exists to remove.

So `openapi-typescript` will emit `errorCode?: string`. Frontend code narrows with a presence
check before branching — that is not a cast and §7 permits it. Every error branch needs an
`errorCode`-absent fallback path anyway, because TASK-0003 will produce exactly those
middleware 401s.

If you find a path that makes `errorCode` genuinely universal without changing error
*behaviour* (which is out of scope below), stop and report it rather than implementing it.

## Acceptance criteria

- [x] `errorCode` and `traceId` declared on both problem schemas, with descriptions.
- [x] `traceId` is in `required`; `errorCode` is not. Asserted by a test, not eyeballed.
- [x] Each schema's `example` validates against its own schema. Assert it; do not eyeball it.
- [x] `OpenApiContractTests` gains a test that fails if a problem schema omits either member, or if
      any schema's `example` carries a member the schema does not declare. The existing
      `EveryOperation_DocumentsItsErrorResponses` did not catch this; the new test must.
- [x] Contract regenerated via `backend/scripts/generate-openapi.ps1 -Promote`, `CONTRACT.lock`
      updated, new hash recorded here and in `STATE.md ## Contract`.
- [x] `./backend/scripts/ci.ps1` green with the integration suite RUNNING (`Skipped: 0`) — use
      `backend/scripts/local-env.ps1`. A skipping run is not a pass. (Secret scan gate failed — see
      Log; pre-existing, unrelated drift, not from this card's diff.)
- [x] THEN frontend-dev: `npm run generate:api`, confirm `schema.d.ts` `ProblemDetails` carries both
      members, then `npm run verify` and `npm run check:api-drift` green. Done — see Log.
- [x] No hand edit to any generated file.

## Out of scope

- Relaxing `UnmappedMemberHandling.Disallow` globally. It is correct for request bodies, and
  changing it to fix a response-shape problem has a far larger blast radius. If you conclude the
  global setting must change, stop and raise it.
- The other seven schemas.
- How errors are produced. The behaviour is right; only its declaration is wrong.

## Notes for the implementing agent

Read `.agent/STATE.md` first and append last. `.agent/AUDIT.md` finding **B1** is the full writeup.

This blocks the frontend half of TASK-0003: the first UI branching on an error code would otherwise
have to cast, which §7 forbids, or hand-edit a generated file, which §3 forbids. Sequence this
before TASK-0003.

## Log
- 2026-08-27 created by orchestrator from TASK-0001 finding B1, verified against both the committed
  contract and the generated client before carding.
- 2026-09-04 dispatched to backend-dev with both decisions resolved above. Decision 2 corrected
  from the card's original framing: `errorCode` is not universal, so it is not `required`.
- 2026-09-04 backend-dev: implemented. New `ProblemDetailsSchemaTransformer`
  (`Api/OpenApi/ProblemDetailsSchemaTransformer.cs`), registered after `SchemaExampleTransformer` in
  `OpenApiSetup.AddApiOpenApi`. Adds `errorCode`/`traceId` (both `type: string`, described) to the
  `Properties` of the `ProblemDetails` and `HttpValidationProblemDetails` object-level schemas only
  (guarded on `context.JsonPropertyInfo is null`), and adds `traceId` to `Required`. No change to
  `UnmappedMemberHandling` or any other schema. Two new `OpenApiContractTests`:
  `ProblemSchemas_DeclareErrorCodeAndTraceId` (both members declared+described; traceId required,
  errorCode not) and `EverySchemaExample_ValidatesAgainstItsOwnSchema` (generic structural check —
  every schema's `example` must satisfy its own `required` and, when `additionalProperties: false`,
  carry no member absent from `properties`). Proved both tests are real: temporarily swapped
  `artifacts/openapi/SchoolManagement.Api.json` for the OLD committed `contracts/openapi.json`
  (pre-fix) and reran `dotnet test --filter FullyQualifiedName~OpenApiContractTests` — both new tests
  failed with the expected messages (missing errorCode/traceId, traceId not required, example carries
  undeclared errorCode/traceId); restored the freshly generated document and reran — 10/10 pass.
  Contract regenerated via `generate-openapi.ps1 -Promote`: hash
  `228ae57fd80c22f7e0511a665efce5d4722bb3280c66f5e3f4c96fff53795a65` →
  `b287da0394da7d25ced6f25c50da761b5135b4c0557ea64ac839fdd69ae09f2c`, additive only (contract diff
  touches only the two problem schemas). `./backend/scripts/local-env.ps1` (full `ci.ps1` with
  `POSTGRES_TEST_CONNECTION` resolved): Architecture 26/26, Unit 124/124, Integration 30/30, all
  `Failed: 0, Skipped: 0`; coverage 75.57% (floor 60%) PASS; vulnerable-package scan PASS; OpenAPI
  drift PASS. `Secret scan` gate FAILED — verified with `gitleaks detect -v` that all 5 leaks are in
  `.agent/tasks/TASK-0011.md`, its log, and `backend/docs/ASSUMPTIONS.md` (the known 2026-08-27 drift
  entry), none in any file this card touched. Frontend half (regenerate `schema.d.ts`, `npm run
  verify`, `npm run check:api-drift`) intentionally NOT done — out of scope for this dispatch, per
  dispatch instructions.
- 2026-09-04 frontend-dev: ran `npm run generate:api` against the committed
  `contracts/openapi.json` (hash `b287da03…`). `schema.d.ts` `ProblemDetails` and
  `HttpValidationProblemDetails` both now emit `errorCode?: string` (described, not in
  `required`) and `traceId: string` (in `required`) — confirmed by direct read of the
  generated file, not eyeballed. Adapted `frontend/src/lib/http/http-error.ts`: the
  hand-duplicated `ProblemDetails` interface (which didn't even declare `errorCode`/`traceId`
  and derived `.code` from `type`, contrary to the schema's own "branch on errorCode" guidance)
  is replaced by `type ProblemDetails = components['schemas']['ProblemDetails']` and a new
  `ValidationProblemDetails` alias, both imported from the generated schema — no more drift-prone
  duplicate. `ApiError.code` renamed to `ApiError.errorCode` (typed `string | undefined`,
  presence-checked from `problem?.errorCode`, no cast) plus a new `ApiError.traceId`. Added
  `fieldErrorsOf()` to narrow the union safely for the `errors` field, which only
  `HttpValidationProblemDetails` declares. Verified the errorCode-absent fallback path with a new
  test (`leaves errorCode undefined for a problem response the framework produced directly`) that
  strips `errorCode` from a 401 problem body and asserts callers fall back to `error.isUnauthorized`
  (i.e. `kind`), not to `errorCode`. Fixed two other tests that depended on the old `.code`/`type`
  behaviour or omitted the now-required `traceId`. `client.ts` needed no change — it only touches
  success-response shapes. `problemResponse()` test helper (`src/test/msw/handlers.ts`) gained
  optional `errorCode`/`traceId` fields so tests can construct realistic bodies.
  Files changed: `frontend/src/api/schema.d.ts` (generated, +8 lines), `frontend/src/lib/http/http-error.ts`,
  `frontend/src/lib/http/http-error.test.ts`, `frontend/src/lib/http/request.test.ts`,
  `frontend/src/lib/http/index.ts`, `frontend/src/test/msw/handlers.ts`. No `backend/**` or
  `contracts/**` touched; no hand edit to `schema.d.ts`.
  Gates: `npm run typecheck` — clean, no output. `npm run lint` (oxlint 1.80.0) — clean, no output,
  exit 0. `npm run test` — `Test Files 14 passed (14)`, `Tests 126 passed (126)` (124 + 2 new),
  0 skipped. `npm run build` — `tsc -b && vite build` succeeded, `✓ built in 2.24s`. `npm run
  check:api-drift` — `src/api/schema.d.ts matches contracts/openapi.json. No drift.` All green,
  nothing skipped. No spec rule bent. Nothing new spotted for TASK-0014 beyond what TASK-0001
  already found. TASK-0012 frontend half done; card ready for orchestrator review/close.
- 2026-09-04 CLOSED by orchestrator. Both halves landed and were independently re-verified rather
  than accepted on report: contract hash matches `CONTRACT.lock`, frontend drift gate clean,
  `tsc -b` clean, 126 frontend tests with 0 skipped, §4.4 check 3 finds no client bypass, and the
  backend suite ran green on the combined tree (26/124/30, `Skipped: 0`) once TASK-0017 restored
  the Secret scan. Held at `review` rather than closed on the red gate, per §10.2 — the failure was
  provably unrelated, but waiving a DoD criterion is how a card closes falsely.
