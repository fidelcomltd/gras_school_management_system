# TASK-0005 — School settings: identity, registration number, config versioning

Owner: backend-dev
Depends on: TASK-0002
Contract impact: additive (new `/api/v1/settings/*` and `/api/v1/config-versions*` paths; no
existing path changes)
Status: queued

## Goal

An administrator configures who the school is and how its registration numbers are formed, and
every such change is recorded as an immutable version rather than an overwrite. This is the
first product surface in the system and the substrate for everything that prints a result sheet:
the school name, logo, head teacher name and signature on a published sheet all come from the
configuration snapshot this card makes possible.

This is deliberately **not** the whole of module 6.2. The grading scale, assessment structure,
rating scales, trait lists, development domains and the fee notice are separate cards, because
6.2.13 replaces their seed data and they carry the two most expensive-to-reverse decisions in
the product (`25-open-conflicts-to-resolve.md` items 1 and 2).

## Contract delta

Additive. New paths under `/api/v1`:

| Endpoint | Privilege | Notes |
|---|---|---|
| `GET /settings` | `settings.view` | Everything in one payload for the settings area. Include only the groups this card implements; later cards extend it. |
| `PATCH /settings/identity` | per 6.2.3 | Name, short name, address, phone, email, motto, head teacher name. |
| `POST /settings/identity/logo` | per 6.2.3 | Multipart. PNG/JPEG, max 2 MB, min 300x300, derivatives at 200 and 64 px. |
| `POST /settings/identity/signature` | per 6.2.3 | Multipart. PNG preferred, max 1 MB, recommended 600x200. |
| `PATCH /settings/abbreviation` | `settings.abbreviation.update` | Body carries the new value, the literal confirmation token `CHANGE`, and a mandatory reason. |
| `PATCH /settings/reg-number` | per 6.2.4 | Separator, serial width, reset rule. |
| `GET /settings/reg-number/preview` | per 6.2.4 | Returns the next number that would be issued under **supplied unsaved** parameters. |
| `GET /config-versions`, `GET /config-versions/{id}` | `audit.view` | Version history and one full version. Cursor-paginated per 9.5. |

Take the exact privilege strings from the register shipped by TASK-0002 (§4.4.2). Do not invent
one. `GET /setup/checklist` is **out of scope** — it reports on setting groups this card does
not build, so it would report a half-truth.

Regenerate `contracts/openapi.json` and `CONTRACT.lock` via
`backend/scripts/generate-openapi.ps1 -Promote`.

## Acceptance criteria

- [ ] `school_profile` is a singleton with every field and validation rule in 6.2.3, including
      `timezone` fixed at `Africa/Lagos` and not editable in this version.
- [ ] Registration number configuration per 6.2.4: separator one of `/ - .` default `/`;
      `serial_width` 3–6 default 4; `serial_reset` `per_year` | `continuous` default `per_year`;
      `year_source` fixed at admission year and not editable.
- [ ] The preview endpoint composes from the current abbreviation, separator and width using the
      **next serial that would actually be issued** — `GRAS/2026/0040` with 39 pupils already
      admitted in 2026 — and recomposes from unsaved parameters without saving them.
- [ ] Reducing `serial_width` is rejected when an issued serial in the current counter exceeds
      the new width, with the exact message form in 6.2.10: `Serial 1043 will not fit in a width
      of 3. Choose 4 or more.`
- [ ] Changing the abbreviation requires the literal word `CHANGE` and a reason, writes an audit
      event, and **rewrites no already-issued registration number**. The response carries the
      count needed to build the 6.2.4 dialogue.
- [ ] The serial counter is keyed on **admission year alone**, not on the abbreviation, so a
      mid-year abbreviation change neither restarts the serial nor produces two pupils whose
      numbers differ only by prefix.
- [ ] Changing the abbreviation to a value used historically is allowed (6.2.11).
- [ ] `config_version` is append-only: every save to a versioned group writes a new row holding
      the whole serialised configuration, plus actor, timestamp, reason where required, and a
      monotonic version number. Nothing is overwritten.
- [ ] Optimistic concurrency on the config version. A stale save is rejected with the 6.2.11
      message, the first save wins, and **both attempts appear in the audit log**.
- [ ] Deleting the logo with no replacement is rejected: `A logo is required. Upload a
      replacement before removing the current one.`
- [ ] Uploads follow 9.6: content type verified by magic bytes not by extension or declared
      type, SVG rejected outright, EXIF stripped, stored outside the web root under generated
      names, served through an endpoint applying the parent record's privilege check, with
      `Content-Disposition` and `nosniff`.
- [ ] Explicit EF Core migration, reviewed, not auto-applied on startup.
- [ ] Tests per CLAUDE.md §6: every endpoint gets happy path, validation failure, unauthorized
      and not-found. Plus unit tests for serial composition, the width-reduction rejection, and
      the append-only guarantee of `config_version`.
- [ ] `./backend/scripts/ci.ps1` green, output pasted. Contract regenerated, lock hash matches.

## Out of scope

- **Grading scale (6.2.5), assessment structure (6.2.6), trait lists (6.2.7), rating scales,
  development domains, result rules (6.2.8), pin defaults, fee notice (6.2.13).** Separate
  cards. Do not seed or model them here.
- The 6.2.9 published-result warnings, the `needs_recompute` flag and `GET /settings/impact`.
  They depend on `result_set`, which does not exist. The `config_version` mechanism this card
  builds is what they will later hang off.
- `GET /setup/checklist` (see Contract delta).
- Any settings **screen**. Frontend follows once the contract is committed.
- The audit log module itself. Use the seam from TASK-0002; if it is still a seam, the audit
  assertions above become tests against the seam and a TODO carries this card's number.

## Notes for the implementing agent

Read `.agent/STATE.md` first and append to it last (§4.1).

Read `product-specification/04-module-school-settings.md` §6.2.3, §6.2.4, §6.2.9, §6.2.10,
§6.2.11 and the `GET /settings` and registration rows of §6.2.12. Then §6.2.2 for what exists on
installation. Then `14-non-functional-requirements.md` §9.5 (cursor pagination — never offset)
and §9.6 (uploads).

Two things in this card look like details and are not:

1. **The snapshot rationale in 6.2.9.** A denormalised copy per result set beats a version
   reference, and the spec explains why with a concrete failure. Build `config_version` so that
   a whole serialised configuration can be copied out of it later. Do not optimise it into a
   reference.
2. **A registration number is a historical identifier, not a derived field.** Nothing in this
   card may recompute an issued number. The school holding two prefix formats side by side is
   correct behaviour, not a bug to be tidied.

Error messages quoted in the spec are user-facing copy the school will read. Reproduce them
verbatim, and put the machine-readable code in the ProblemDetails `type`, per §6.

**Stop on ambiguity.** If a validation rule is under-specified, write the question into this
card's Log and raise it rather than choosing.

## Log
- 2026-08-26 created
