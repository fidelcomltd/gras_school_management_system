# TASK-0074 — Regenerate the typed client against `152dc1c2…` and get `frontend-ci` green

Owner: frontend-dev
Depends on: nothing. The contract is already promoted and `CONTRACT.lock` matches it.
Contract impact: **none** — this CONSUMES a contract that has already moved. You do not touch
`contracts/**`, and you do not run the backend generator.
Status: queued 2026-09-16
Reads: `frontend/src/api/README.md` (how the generated seam is shaped); `frontend/package.json`
       scripts `generate:api` and `check:api-drift`; `frontend/scripts/generate-api-schema.mjs` and
       `check-api-schema-drift.mjs` only if a script misbehaves; `.agent/spec/frontend.md`.
       **Do NOT open `contracts/openapi.json` — it is 420 KB. `jq` any slice you need.**

## Goal

`frontend/src/api/schema.d.ts` is stale and `npm run check:api-drift` is RED, so **`frontend-ci`
has been failing on every push all day** (runs 35132126849, 35104802235, 35042713956 — all the same
schema-drift failure). This is the established backend-first pattern, not an accident: the backend
ships, the contract moves, and a regeneration card catches the client up. This is that card.

The drift entry recording this is stale in the orchestrator's favour — it names TASK-0063's
`b293db2bc2b4…`. **The contract has moved AGAIN since, by TASK-0069.** Regenerate against the live
document, not against any hash quoted in prose.

## Contract delta

None. The current contract, verified mechanically against the working tree on 2026-09-16 with
`sha256sum` and `jq`, not read off a card:

- hash `152dc1c27db77bfc6c2697580d8b3f86ebe16af42a828360ce235a6c6f215d0f`
- **54 paths, 102 schemas**, api version `v1`
- `contracts/CONTRACT.lock` matches this hash

What arrived since the client was last generated: TASK-0063's `POST /pupils/{id}/registration-number`,
then TASK-0069's grading-band and assessment-structure surface. **Confirm that list against the
generated diff rather than trusting this sentence** — if the diff shows something neither card
explains, STOP and report it rather than committing it.

## Acceptance criteria

- [ ] `npm run generate:api` regenerates `src/api/schema.d.ts` from the CURRENT
      `contracts/openapi.json`. **The file is generated output — not one hand-edit, not one manual
      touch-up, however small.** If the generator produces something wrong, fix the generator input
      or report it; do not correct its output by hand.
- [ ] `npm run check:api-drift` is GREEN.
- [ ] TypeScript compiles clean across the app, and the existing test suite passes. **A regenerated
      schema can break call sites that were valid against the old shape** — if it does, fixing those
      call sites is in scope; changing what they mean is not.
- [ ] Lint passes.
- [ ] The generated diff is REVIEWED and summarised in your report: which operations and schemas
      appeared, and that nothing was removed or narrowed unexpectedly. A regeneration that silently
      drops an operation the app already calls is the failure mode this criterion exists for.
- [ ] `openapi-typescript` stays pinned at `7.13.0` exact, as a devDependency. **Do not upgrade it
      as part of this card**, and do not let a lockfile churn smuggle a bump in.

## Out of scope

- Any new screen or feature. This card makes the client match the contract and nothing else.
- Any change under `contracts/**` or `backend/**`. If the contract itself looks wrong, report it —
  the orchestrator owns that decision, and a contract change travels in the other direction
  (`rules/contract.md`).
- Building UI for the newly available grading/assessment operations. That is a separate card; having
  the types is not a reason to start consuming them.
- Do not run the full gate (`rules/gates.md` §1).

## Notes for the implementing agent

`src/api/client.ts` is the hand-written typed request helper over `src/lib/http/`; historically these
regeneration cards (TASK-0037, 0040, 0044, 0047, 0052, 0064) have needed **zero new wrapper code** —
the generic wrapper absorbs new operations. If you find yourself writing wrapper code, that is a
signal something is different about this contract shape, and it is worth saying so explicitly.

One prior regeneration hit a real type-level trap worth knowing about: TASK-0054 found that a
non-JSON (`text/csv`) response body resolved `SuccessBody` to `never`, fixed with a `ResponseBodyOf`
helper. If a new operation has an unusual response content type, check that seam rather than
assuming it flows through.

**Do not close this card on a local green alone.** The point of it is `frontend-ci`, so the
verification that matters is that the drift gate CI actually runs would now pass.

## Log
- 2026-09-16 carded by the orchestrator after `gh` authentication revealed `frontend-ci` red on
  every push of the day for this one reason. The standing drift instruction — *the next
  `frontend-dev` dispatch of any kind regenerates the client before doing anything else* — is hereby
  discharged by giving it its own card rather than bolting it onto an unrelated one.
