# TASK-0044 — Regenerate the frontend client for arms

Owner: frontend-dev
Depends on: TASK-0039 (closed 2026-09-08 — the contract move this card consumes)
Contract impact: none. Consumes a committed contract; must not change one.
Status: done

> **Fifth run of one recurring card.** TASK-0029, 0033, 0037, 0040 were the same work against
> earlier contract moves. **[TASK-0040.md](TASK-0040.md) is the template — follow it exactly**;
> this card only records what differs.

## Goal

`src/api/schema.d.ts` matches the committed contract, §4.4 check 2 green, all arms operations
callable through the existing typed wrapper.

## Contract delta

**None.** Already committed at sha256
`84a3444a5172a524a64860e7b296ee6c15fc6f8d35d62cd7c873c803dd61b420`. Recompute it yourself and
confirm against `CONTRACT.lock` before starting.

New/changed: the `/arms*` operations (list, next-label, create, bulk with `dryRun`, detail, patch,
delete); `ArmCount` added to `SessionDto` and `SessionDetailDto`; a new 409 case on
`POST /terms/{id}/open` (`term.no_arms_for_session`).

## Acceptance criteria

- [x] `npm run generate:api`; `check:api-drift` → "No drift."
- [x] Arms operations reachable through the existing generic wrapper. Expect **zero** new lines in
      `client.ts`/`client-types.ts`; justify any you add.
- [x] Both negative typing directions proven, per TASK-0040 — omitted required `Idempotency-Key`
      on `POST /arms` and `POST /arms/bulk`, and an `idempotencyKey` rejected on `GET /arms` and
      `GET /arms/next-label`.
- [x] Path-parameter operations reject an omitted path parameter.
- [x] §8 enum tolerance proven for the arm `status` enum (`active`/`inactive`/`closed`) via a
      `server.use(...)` override. **Route templates written `:id`, never `{id}`.**
- [x] `npm run verify` green with counts, `Skipped: 0`.
- [x] **Run the secret scan before reporting** — from the repo root:
      `gitleaks detect --source . --no-git --config backend/.gitleaks.toml --redact --no-banner`.
      It must report no leaks. `npm run verify` does NOT cover this, and TASK-0043 turned the
      backend gate red with a test fixture that looked like a credential. Any fixture you add must
      be unmistakably fake — never a random high-entropy string, and never an allowlist entry.

## Out of scope

- **Every screen.** No arms UI — that is a later card.
- `client.ts`/`client-types.ts` changes beyond a proven gap.
- Anything under `backend/**` or `contracts/**`.

## Log
- 2026-09-08 created by orchestrator on closing TASK-0039.
- 2026-09-08 implemented by frontend-dev; closed same session. Hash independently recomputed
  (`sha256sum contracts/openapi.json`) and confirmed to match `CONTRACT.lock`
  (`84a3444a5172a524a64860e7b296ee6c15fc6f8d35d62cd7c873c803dd61b420`) before starting.
  `npm run generate:api` rewrote `src/api/schema.d.ts` (+965/-16 per `git diff --stat`), purely
  additive — the `ListArms`, `CreateArm`, `GetNextArmLabel`, `BulkCreateArms`, `GetArm`,
  `UpdateArm`, `DeleteArm` operations and their schemas (`ArmDto`, `ArmStatus`,
  `CreateArmCommand`, `UpdateArmCommand`, `BulkCreateArmsCommand`, `BulkCreateArmsLevelEntry`,
  `BulkCreateArmsResponse`, `NextArmLabelResponse`, `CursorPageOfArmDto`) plus the additive
  `ArmCount` field on `SessionDto`/`SessionDetailDto`. `check:api-drift` → "No drift." Zero new
  lines in `client.ts`/`client-types.ts` — sixth confirmation of the same finding TASK-0033/0037/
  0040 established.

  New tests, both under CONVENTIONS.md §3's 180-line cap: `client-arms.test.ts` (111 lines —
  `ListArms`, `GetNextArmLabel`, `CreateArm`, `BulkCreateArms`) and `client-arms-detail.test.ts`
  (90 lines — `GetArm`, `UpdateArm`, `DeleteArm`).

  **Both negative typing directions proven on the exact pairs the card named**: `POST /arms` and
  `POST /arms/bulk` both reject an omitted required `Idempotency-Key`; `GET /arms` and
  `GET /arms/next-label` both reject a passed `idempotencyKey` (neither declares the header —
  `ListArms`'s real optional filters and `GetNextArmLabel`'s required `levelId`/`sessionId` are
  two differently-shaped query types, so this isn't "an empty type rejects everything"). Every
  path-parameter operation (`GetArm`, `UpdateArm`, `DeleteArm`) proven to reject an omitted `id`.

  **§8 enum tolerance proven for `ArmStatus`** via two `server.use(...)` overrides returning
  `status: "SomeFutureArmStatus"`: one on `GET /arms` (no path parameter, no `:id` conversion
  needed) and one on `GET /arms/{id}` written as `apiUrl('/api/v1/arms/:id')` — the route that
  actually exercises the `{id}` → `:id` MSW conversion (`toMswRoute` in `openapi-handlers.ts`),
  per the card's explicit warning that a prior dispatch (TASK-0037) lost a run writing an
  override route with the literal `{id}`.

  **All 7 `@ts-expect-error`s verified load-bearing by the removal-probe method**: all seven
  comments deleted at once from `.bak` copies, `npm run typecheck` re-run, confirmed exactly
  seven errors at exactly the seven expected lines (`TS2554` for the three path-parameter/
  Idempotency-Key-required cases needing a third argument, `TS2353` for the two `idempotencyKey`-
  rejected cases, `TS2554` for the two `POST` missing-required-argument cases), then restored
  from `.bak` and re-verified `npm run typecheck` clean before deleting the backups.

  **Gates, all run directly by this session, in order**: `npm run typecheck` (`tsc -b`) 0 errors;
  `npm run lint` (`oxlint --max-warnings=0`) 0 warnings; `npm run test` (`vitest run`) **40 files,
  286 passed, 0 failed, Skipped: 0** (was 38/270 after TASK-0043; +16 new cases across the two new
  files matches exactly); `npm run build` (`tsc -b && vite build`) succeeded, 521 modules,
  unchanged (test files aren't bundled); `npm run check:api-drift` "No drift" (re-confirmed after
  the gate run). `gitleaks detect --source . --no-git --config backend/.gitleaks.toml --redact
  --no-banner` → "no leaks found" (5.60 MB scanned) — no fixture in the new tests resembles a
  credential; all sample values are the same fixed UUID-shaped constants already used throughout
  `src/api/client-*.test.ts`.

  **Files changed**: `frontend/src/api/schema.d.ts` (regenerated, +965/-16),
  `frontend/src/api/client-arms.test.ts` (new, 111 lines),
  `frontend/src/api/client-arms-detail.test.ts` (new, 90 lines), `frontend/src/api/README.md`
  (+12/-6, tests table and zero-new-code precedent list updated). Nothing under `contracts/**` or
  `backend/**` touched; hash unmoved at `84a3444a…`.

  **Confirmed out of scope, not built, exactly as the card scoped**: no arms screen, no bulk-create
  UI, no TanStack Query hooks, no `features/arms` folder — client seam only.
