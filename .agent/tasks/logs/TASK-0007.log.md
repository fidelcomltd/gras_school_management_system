# TASK-0007 — full implementation log

Moved out of `TASK-0007.md` on 2026-09-04 per CLAUDE.md §13 (task cards cap at ~120 lines; a card
is a brief, not a transcript). The card is closed; this is the record of how it got there.

- 2026-08-26 created by orchestrator after verifying the advisory's scope during TASK-0002 review
- 2026-08-27 backend-dev: worked the options in order.
  - **Before:** `dotnet list package --vulnerable --include-transitive` — `SchoolManagement.IntegrationTests`
    reported `SSH.NET 2025.1.0 High GHSA-q939-rpr3-3284`; every other project clean. Matches the card.
  - **Option 1 (bump)** — raised `Testcontainers.PostgreSql` 4.13.0 → 4.14.0 in
    `backend/Directory.Packages.props`. `dotnet restore` succeeded with no other file changes needed.
  - **After:** `dotnet list package --vulnerable --include-transitive` — all six projects report "no
    vulnerable packages". `SSH.NET` now resolves to `2026.0.0` via `Docker.DotNet.Enhanced` 4.3.3 (the
    4.14.0 line's replacement for the plain `Docker.DotNet` the 4.13.0 line used).
  - Option 1 succeeded → stopped, per the card's "work in order, stop at the first that succeeds".
    Options 2 and 3 not attempted; `backend/scripts/ci.ps1` untouched.
  - Ran `./backend/scripts/ci.ps1` in full. ALL GATES PASSED, including "Vulnerable dependencies":
    "No known vulnerable packages." Build 0 warnings/0 errors, format clean, OpenAPI drift clean
    (contract untouched, still hash `228ae57f...`).
  - **Docker is absent and `POSTGRES_TEST_CONNECTION` is unset in this session** (confirmed before the
    run, not searched for). The integration project therefore compiled but every test SKIPPED:
    `Skipped! - Failed: 0, Passed: 0, Skipped: 30, Total: 30` for `SchoolManagement.IntegrationTests`.
    Coverage gate correspondingly printed "COVERAGE FLOOR NOT ENFORCED" (25.64% line / 30.28% branch,
    not comparable). Per the dispatch's explicit instruction: **this run is reported as INCOMPLETE**,
    not a pass, on the two DB-dependent gates (integration tests, coverage floor) — pending a database
    re-run the orchestrator is arranging separately. The vulnerable-dependency gate itself does not
    depend on a database (`dotnet list package --vulnerable` is a static/offline restore-graph check)
    and is verified green on its own terms.
  - Files changed: `backend/Directory.Packages.props` (one version bump),
    `backend/docs/ASSUMPTIONS.md` §3.8 (rewritten to record the resolution and the open re-verification
    item). No `backend/src/**` change. `contracts/**` unchanged (hash still `228ae57f...`).
  - Not committed, left in the working tree per dispatch instructions. Status set to `review` —
    orchestrator to close and to arrange the database re-run.
- 2026-08-27 backend-dev: database re-run performed at the orchestrator's request, credential loaded
  from `C:\Users\HP\.gras\pg-test.txt` into the process environment for one command only, never echoed
  or written into the repo (deleted the raw output file immediately after redacting it).
  - **Result: `IntegrationTests` — `Failed: 30, Passed: 0, Skipped: 0, Total: 30`.** Every test's
    collection fixture (`ApiTestFixture.InitializeAsync`) threw the same
    `Npgsql.PostgresException: 42501: permission denied for schema public` while EF Core's migrator
    tried to create `__ef_migrations_history` (`NpgsqlHistoryRepository.CreateIfNotExistsAsync`,
    `ApiTestFixture.cs:82`). This is a database-role privilege problem — the `schoolmanagement_tests`
    role lacks `CREATE` on schema `public` (consistent with PostgreSQL 15+'s changed default of no
    longer granting `CREATE` on `public` to `PUBLIC`) — not a code, Testcontainers-version, or
    pooler-transport problem. **Not fixed here** per the dispatch's explicit instruction not to "fix"
    connection/infra issues found during this re-run; reported verbatim instead. This blocks TASK-0007
    closing under the "must be true" bar (`Skipped: 0` with non-zero passed), even though it has
    nothing to do with this card's SSH.NET/Testcontainers change — it needs a `GRANT CREATE ON SCHEMA
    public TO <test role>;` (or equivalent) run against the Neon `schoolmanagement_tests` database,
    which is a credential/provisioning action outside this card's file scope.
  - Coverage (measured this time, suite not skipped): **line 60.48%, branch 43.36%.** No
    "COVERAGE FLOOR NOT ENFORCED" line printed — the gate ran and enforced against the 60% floor.
    Note the floor is evaluated on line coverage only; branch coverage is reported but not gated
    (matches ASSUMPTIONS.md §2.10, unchanged by this card).
  - Vulnerable dependencies: still green — "No known vulnerable packages." (7 projects, corrected
    from "six" in this doc and in STATE.md per the orchestrator's note.)
  - OpenAPI contract drift: "The committed contract matches the code." Confirmed by hash:
    `contracts/openapi.json` sha256 `228ae57fd80c22f7e0511a665efce5d4722bb3280c66f5e3f4c96fff53795a65` —
    unchanged, matches `CONTRACT.lock`. Held.
  - Overall: `FAILED GATES (1): Tests`. Not a pass. Card stays in `review`, not closed — this run
    confirms the Option 1 fix survives a build/restore/coverage/vuln/drift pass but could not
    confirm the integration suite runs green against this database, for a reason unrelated to
    TASK-0007's change.
- 2026-08-27 orchestrator review: diff verified independently — one-line bump, contract hash held at
  `228ae57f`, `contracts/**` `backend/src/**` `backend/scripts/**` untouched, vulnerable-package scan
  re-run by the orchestrator and clean across all seven projects. The card's own remediation is
  CORRECT and COMPLETE. **Not closed.** Two things stand in the way, neither of them this card's
  fault: (1) the database re-run failed 30/30 at collection-fixture init on
  `Npgsql 42501: permission denied for schema public` — a provisioning gap in the new
  least-privilege test role, now `STATE.md` `## Open questions` item 10, and the fix is a human-run
  `GRANT`; (2) the bump swapped the transitive family `Docker.DotNet` -> `Docker.DotNet.Enhanced`,
  a code path no run on this Docker-less machine can exercise — accepted as `## Known drift`
  2026-08-27 rather than pretended verified. Status set to `blocked`: nothing here is awaiting a
  reviewer, it is awaiting the grant. Re-run `ci.ps1` with a working credential and this closes.
- 2026-08-27 **CLOSED by orchestrator** on a verified live run after the human granted `CREATE` on
  schema `public` to the test role. `ALL GATES PASSED`: integration suite `Failed: 0, Passed: 30,
  Skipped: 0` — so `Testcontainers.PostgreSql` 4.14.0 is confirmed not to have broken the harness,
  which was the one thing the earlier runs could not establish. Vulnerable dependencies clean,
  contract drift clean, hash `228ae57f` unchanged. Remaining accepted risk, unchanged and recorded
  in `STATE.md` `## Known drift` 2026-08-27: `Docker.DotNet.Enhanced` itself is unexercised because
  a set `POSTGRES_TEST_CONNECTION` bypasses Testcontainers entirely and Docker is absent here.
  Options 2 and 3 remain struck as not reached.
