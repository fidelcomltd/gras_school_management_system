# TASK-0071 — Log (archived at close)


- 2026-09-16 Carded by the orchestrator; §8.4 restated and independently verified. `decisions/2026-Q3.md`.
- 2026-09-17 Pre-dispatch check (human asked to run it): **held.** No `result_set`/`subject_score`/
  computed tables in the tree (only seam interfaces); ROADMAP Phase 3 row 1 was uncarded; no
  delta or Notes; annual cumulative belonged to §6.7.10's card.
- 2026-09-17 **Rewritten on human instruction** against TASK-0076 (new) and TASK-0077 (new, since
  §6.2.8 result rules were also absent). Annual cumulative moved out. Compute-permitted states ruled by
  the orchestrator. Two open questions raised for the human.
- 2026-09-17 **Both human rulings received**, recommendations accepted as written; open questions converted to
  binding rulings with an AC each. Publication re-check of stale sibling level positions logged as drift.
- 2026-09-18 **Pre-dispatch amendment by the orchestrator.** Error codes renamed to the dotted convention
  (`result_set.*`; TASK-0076's are `score_sheet.*`) — the same unprefixed-code error the orchestrator made in
  TASK-0077's delta, fixed before dispatch this time. `flags[].code` values are response-body data, not problem
  codes, and stay as written. Ports named by path now that 0076/0077 exist. Amendment transcribed into
  `decisions/2026-Q3-contract-deltas.md`.
- 2026-09-18 **Dispatch 1 stopped cleanly at the stage 2 boundary**, as instructed: calculator (29 unit tests) and
  tables/migration `AddResultComputationTables`/handler/endpoint/audit (10 integration tests) built. Agent: build
  and format clean, unit 898/898, arch 33/33, integration (compute + results) 34/34, Skipped 0, local container.
  **Stage 3 (the §8.4 fixture, self-check, §8.5 assertions) not started.** Contract regenerated locally only
  (66 paths); NOT promoted — promotion waits for the whole card and two open questions.
- 2026-09-18 **Open, for the human, before promotion:** (1) banding a fractional average — agent used half-open
  `[lower, upper+1)`; spec silent; fixture indifferent. (2) unknown `resultSetId` returns 403 not the delta's
  404 — scope layer fails closed on `Unresolvable` before the handler; first lookup-scoped route. Agent-added
  `result_set.not_found` (404 code) follows convention and is accepted.
- 2026-09-18 **HUMAN RULINGS, both recommendations accepted:** (1) a fractional average takes the band whose
  lower bound it has reached — half-open `[lower, upper+1)`, so 84.60 is B and 85.00 is A; threshold semantics,
  as `passMark`'s "at or above". Engine unchanged. (2) unknown `resultSetId` stays 403; the contract drops the
  unreachable 404 before promotion. The handler's `result_set.not_found` branch stays as defence in depth. The
  scope layer's unresolvable-target semantics belong to TASK-0060.
- 2026-09-18 **Stage 3 dispatch interrupted at 06:36** (IDE session hung, stopped by the human). It left the fixture
  data file, a self-check test and the UnitTests `.csproj` link, all unverified. Stages 1–2 plus the 403 ruling were
  checkpointed as `fe5fd56` on branch `task-0071` (renamed from `task-0070-wip`), stage-3 files excluded. Stage 3
  re-dispatched fresh on top of the partial files.
- 2026-09-18 **Stage 3 built and green**, re-dispatch after the interruption. §8.4 fixture (54 pupils, 2 arms), self-check
  (no engine), fixture-driven integration test covering every §8.5 assertion except the annual cumulative, plus
  §6.7.12's counted-zero and one-pupil-arm cases and §8.4.5's `exam_then_ca` alternate outcome (6th/7th/8th). Engine
  untouched; tests only, 834 lines. The agent fixed two build breaks in the interrupted files (CA1034 nesting; a
  `Results` namespace shadowing `Microsoft.AspNetCore.Http.Results`). Judgment call, accepted: the fixture maps its
  "Mathematics" to the SEEDED subject, since `NameKey` is unique. **Orchestrator re-ran:** unit `~Results` 61/61, integration
  fixture+compute 13/13, Skipped 0, local container. Remaining to close: promote the contract, full gate, ledger.
- 2026-09-18 **Closed by the orchestrator.** Contract promoted `84b46211…` → `0ebca075…` (66 paths, additive verified
  mechanically; optional `Idempotency-Key` accepted as the delta's intent). Full gate 1330/1330, Skipped 0, local container.
