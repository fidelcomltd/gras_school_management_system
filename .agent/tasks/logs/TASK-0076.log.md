# TASK-0076 — Log (archived at close)


- 2026-09-17 Carded by the orchestrator, splitting ROADMAP Phase 3 row 1. The completeness gate
  moved to the submission card, because it needs traits, attendance and remarks, none of which exist.
- 2026-09-17 Dispatch A returned (backend-dev): 5 ACs, build clean, unit 829/829, arch 33/33, scoped integration 49/49,
  Skipped 0, contract byte-identical. ~1,300 hand-written lines against a 900 stop, flagged honestly. Correctly
  refused to run `ci.ps1` (gates.md §1; the dispatch text was wrong). **Review defect:** term-close blocks on
  state alone; §6.3.6 blocks only sets "with marks entered". Fix pending. Open spec question: §6.3.6 omits
  Returned for Correction.
- 2026-09-17 Orchestrator gate #1 silently ran on hosted Neon via `~/.gras/pg-test.txt`; human called the wait
  unacceptable. **Stopped by the orchestrator mid-integration-stage** (the Neon test DB may be left mid-reset;
  it only matters for a confirmed hosted run, whose per-test reset truncates everything). Re-run on the local
  container with TASK-0078's resolver; first log line confirmed `local container`.
- 2026-09-17 Term-close fix returned: correlated EXISTS on a non-voided `subject_score`; 2 new tests shown failing
  on the old query; agent's filtered run 32/32, Skipped 0, on the local container. Returned for Correction
  still non-blocking. Orchestrator recommended blocking it too; awaiting the human.
- 2026-09-17 **Orchestrator false green, caught:** first scoped-gate launch ran from `.agent/`, so
  `./backend/scripts/ci.ps1` was not found, yet the background wrapper exited 0 with no log. Re-launched by absolute
  path; first log line confirmed `local container`. Launch gates by absolute path; judge by the log's counts.
- 2026-09-17 **HUMAN RULING: Returned for Correction also blocks term close** (with marks entered). Otherwise a
  returned set is stranded: score entry 409s once the term closes, and reopening is refused once the next term opens.
- 2026-09-17 **Dispatch A DONE.** Orchestrator scoped gate (absolute path, local container): ALL GATES PASSED, 895
  tests, Skipped 0, integration 33/33. Earlier full local gate on A: integration 342/342. Contract unmoved.
- 2026-09-17 **Dispatch B died on the session rate limit — FIFTH occurrence** (0003, 0019, 0005a, 0070, 0076B),
  after 268 tool calls, while waiting on its own test run. No report. Tree inventoried before anything was
  believed: 10 Application/Api files, `ScoreSheetEndpoints`, 2 new test classes, no TODO/FIXME; generated
  artifact carries the two expected paths (62 → 64) and `contracts/openapi.json` is correctly NOT promoted.
  Orchestrator running the scoped gate to find out what is actually finished.
- 2026-09-17 Orchestrator scoped gate over the dead dispatch's tree: build/format/unit/arch/secret PASS;
  **4 integration failures, all `ScoreSheetEndpointsTests`**. Triaged: (1) `stale_version` returned where
  `result_set_locked` is due — production ordering bug, state must be checked before version; (2) a second
  consecutive save 409s on the version the first save RETURNED — production bug, would break §6.7.4's
  30-second autosave; (3) `term.Close` failing in test setup — test bug; (4) audit assertion expecting one
  record where an idempotency-purge record is also written — test bug. Contract-drift FAIL is expected
  (unpromoted by design). Fresh agent dispatched with the verbatim failures and this triage.
- 2026-09-17 **Dispatch B finished by a THIRD agent** (fresh, after the rate-limit death): 4 gate failures fixed —
  two production (state-before-version ordering; `ScoreSheetVersion` hashing raw `jsonb` text, which Postgres
  re-serialises on read, so a save's own returned version failed the next save's staleness check and would have
  broken §6.7.4's 30-second autosave) and two test faults (term-close setup missing `times_school_opened`;
  audit assertion assuming a single record). Root cause on the version bug was deeper than the orchestrator's triage.
- 2026-09-17 **Contract promoted by the orchestrator** after a mechanical additive diff: 2 paths, 9 schemas,
  zero removals, zero new required properties, zero changed existing paths. `c5c4d6c6…`, 64 paths, 132 schemas.
- 2026-09-17 **Final gates:** scoped run 926/926, Skipped 0, contract drift PASS. Ledger gate then FAILED on the
  orchestrator's own stale `## Contract` block — TASK-0075's gate working as designed. While fixing it the
  orchestrator DAMAGED STATE.md (a `## Contract` match hit the index's `### Contract`, deleting Layout and
  Toolchain); caught by reading the diff, restored verbatim from it. Cheap gates then ALL PASSED.
