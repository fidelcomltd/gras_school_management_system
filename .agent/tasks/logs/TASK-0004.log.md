# TASK-0004 — full implementation log

Moved out of `TASK-0004.md` on 2026-09-04 per CLAUDE.md §13 (task cards cap at ~120 lines; a card
is a brief, not a transcript). The card is closed; this is the record of how it got there.

- 2026-08-26 created
- 2026-08-26 dispatched to frontend-dev. Parallel with TASK-0002 (backend) per §4.3. TASK-0002 will regenerate contracts/openapi.json mid-flight; regenerate and re-run the drift check before reporting.
- 2026-08-26 completed by frontend-dev. Contract hash checked before and after work
  (`40e14997379ecb7edb5acd25f4bb8493e30cb9dc445c9ffde67a05ae67db6eb6`) — unchanged throughout,
  TASK-0002 had not promoted a new contract yet, so no mid-flight regeneration was needed.
  `npm run verify` green (typecheck, lint zero-warning, 124/124 tests, build). Deviation:
  `openapi-typescript@7.13.0` declares `peerDependencies: { typescript: "^5.x" }` and this
  repo runs TypeScript 6.0.3; a plain `npm ci` rejects that with ERESOLVE. Also changed
  `.oxlintrc.json`: `ignorePatterns` narrowed from `src/api/**` to `src/api/schema.d.ts` (so
  the new hand-written `client.ts` is linted) and `scripts/**` added (Node codegen scripts,
  never bundled/shipped — same category as `dist`/`coverage`). Status → done.
- 2026-08-26 orchestrator review: gates re-verified independently, pipeline accepted against
  §7. One finding on the peer-dependency deviation above — the original fix
  (`frontend/.npmrc` with `legacy-peer-deps=true`) was too broad: it disables peer checking
  for every install in the package, not just this one conflict, which would let a genuine
  future peer conflict (e.g. a library that really does need React 18) install silently
  broken instead of erroring. Corrected per orchestrator instruction: removed
  `frontend/.npmrc`; added a scoped `package.json` `overrides` entry instead —
  `{ "openapi-typescript": { "typescript": "$typescript" } }` — which pins only
  openapi-typescript's own `typescript` resolution to this repo's `typescript`
  devDependency range and leaves every other package's peer checking intact. Justification
  moved from the deleted `.npmrc` into `frontend/CONVENTIONS.md` §1. Verified with a fully
  clean resolution (`node_modules` and `package-lock.json` both removed, no `.npmrc`
  present): `npm install` resolves without ERESOLVE (264 packages, 0 vulnerabilities — down
  from 271/1-high under the old `--force`-tainted lockfile), and a subsequent `npm ci`
  against the regenerated lockfile is clean. `npm run verify` and `npm run check:api-drift`
  re-run green against that install (see orchestrator report for pasted output). STATE.md
  Decisions entry corrected to match. Revisit the override once openapi-typescript ships a
  TS 6-aware peer range upstream. Status remains done.
- 2026-08-26 orchestrator review, second pass. Correction verified: `.npmrc` gone, scoped
  `overrides` present, no `legacy-peer-deps` anywhere but the CONVENTIONS.md explanation of why
  it was not used. Gates re-run by the orchestrator on the rebuilt tree — 124/124, zero lint
  warnings, clean build, `check:api-drift` clean, `npm audit` 0 vulnerabilities. Drift check
  verified in both directions earlier (tampering schema.d.ts exits 1 with an actionable diff;
  restoring it passes).
  **One process finding, accepted rather than reverted.** The correction regenerated
  `package-lock.json` wholesale instead of adding one devDependency surgically, sweeping 73
  resolved versions (21 added, 7 removed) — including `@base-ui/react` 1.6.0->1.7.0, `axios`
  1.19.0->1.20.0, `oxlint` 1.76.0->1.80.0, `vite` 8.2.0->8.2.2, `vitest` 4.1.10->4.1.11,
  `@tanstack/react-query` 5.101.4->5.102.6. None of that was in this card, and the card
  explicitly said no dependency additions beyond the generator. Accepted because every bump sits
  inside a caret range package.json already declared, the gates are green, and the security
  position improved. Recorded in STATE.md Decisions, with the two bumps worth watching in Known
  drift. **It must land as its own commit, separate from the codegen work** (§8, one logical
  change per commit) so a later bisect can distinguish a Base UI regression from a codegen bug.
- 2026-08-26 Carried forward, not a defect in this card: `SuccessBody` in `src/api/client.ts`
  resolves 200 and 201 only, so a 204 operation would infer `never`. Nothing returns 204 today;
  §6 mandates 204 on delete and no-body update, so the card adding `apiDelete`/`apiPatch` owns it.
- 2026-08-26 CLOSED by orchestrator.

