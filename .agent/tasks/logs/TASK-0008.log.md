# TASK-0008 — full implementation log

Moved out of `TASK-0008.md` on 2026-09-04 per CLAUDE.md §13 (task cards cap at ~120 lines; a card
is a brief, not a transcript). The card is closed; this is the record of how it got there.

- 2026-08-26 created at the human's request, after the ergonomics gap surfaced while trying to run
  TASK-0002's integration tests
- 2026-08-26 orchestrator folded the local-gitleaks requirement into this card. Rationale: this card
  creates the file that holds the credential, so the tooling that would catch it being committed
  belongs in the same change rather than trailing it. Raised with the human as the default; split it
  out if they prefer.
- 2026-08-27 dispatched to backend-dev, with the orchestrator's four-source resolution design
  (already-set env var → `$env:GRAS_LOCAL_ENV_FILE` → `$HOME/.gras/pg-test.txt` → an inline
  `$connectionString` slot) specified in the dispatch rather than left to the agent.
- 2026-08-27 implemented and moved to `review`. Summary below; full detail in the orchestrator's
  report.

  **Files:** `backend/scripts/local-env.template.ps1` (new, committed) implements the four-source
  resolution and takes `-Gate` (default `ci.ps1`); `backend/scripts/local-env.ps1` created locally
  (git-ignored, uncommitted, byte-identical to the template — the inline slot stays empty and it
  resolves via `$HOME/.gras/pg-test.txt`, so no credential is ever embedded as static text in either
  file). `backend/.gitignore` gained one exact-path rule (`scripts/local-env.ps1`, no wildcard).
  `backend/README.md` "Configuration and secrets" gained a subsection explaining why this one
  variable bypasses `IConfiguration`/user-secrets, plus the gitleaks prerequisite row moved from
  Optional to Required. `backend/docs/ASSUMPTIONS.md` gained §3.9 (below).

  **`git check-ignore -v`:** real script shows `backend/.gitignore:48:scripts/local-env.ps1
  backend/scripts/local-env.ps1` (exit 0); template shows nothing (exit 1). Confirmed, not inferred.

  **gitleaks:** was not installed; installed via `winget install --id Gitleaks.Gitleaks`
  (no admin), version 8.30.1. `ci.ps1`'s Secret scan gate now actually executes instead of
  warning-and-skipping. Demonstrated the template does not trip it (isolated `--no-git` scan, 0
  leaks) and that a filled-in copy of the real script does (same scan, 1 leak, rule
  `postgres-connection-string-with-password`, using an obviously-fake demo value, never staged).

  **Discovery, not caused by this card:** gitleaks running for real for the first time — anywhere,
  including what CI would have found — surfaces 9 pre-existing leaks across `backend/README.md`,
  `appsettings.Development.template.json`, `DesignTimeDbContextFactory.cs`,
  `backend/ci/github-actions-backend.yml`, `.agent/STATE.md` and `contracts/CONTRACT.lock`. All are
  non-secret (documented placeholders / content hashes) and all are outside this card's touchable
  files or its `backend/src/**` restriction; `gitleaks detect` also scans git history by default, so
  editing current content would not even clear it. Recorded as `docs/ASSUMPTIONS.md` §3.9 with three
  options, none decided here. This means `ci.ps1`'s Secret scan gate is **not** green end to end —
  see the full gate run below.

  **Full `ci.ps1` run** (via `./scripts/local-env.ps1`, credential resolved from
  `$HOME/.gras/pg-test.txt`, never printed): Restore/Build/Format/Generate-OpenAPI/Vulnerable-deps/
  Contract-drift all PASS. **Tests FAIL** — 30/30 integration tests fail at fixture init with
  `42501: permission denied for schema public` (Architecture 24/24 and Unit 124/124 pass) — this is
  `## Open questions` item 10, expected and external, not a regression from this card. **Secret scan
  FAIL** — 9 pre-existing leaks per §3.9 above, also external to this card, newly surfaced rather
  than caused. Coverage floor gate printed PASS at 60.48%/43.36%, but that number is **not
  meaningful**: the integration tests failed rather than skipped, so `ci.ps1`'s skip-detector (which
  only stands down the floor when tests are *skipped*) did not recognise the run as incomplete —
  worth flagging to whoever next touches that gate, not fixed here as it is outside this card's scope.

  Two acceptance boxes are therefore not fully green, both for reasons outside this card's file
  scope: "gitleaks still passes" (blocked by the §3.9 pre-existing findings) and, transitively, "a
  developer can go from a fresh clone to running integration tests" (blocked by Open Questions #10,
  already known). Every other acceptance criterion is met and verified as shown above.
- 2026-08-27 orchestrator review. Deliverables verified independently, not taken on report:
  `git check-ignore -v backend/scripts/local-env.ps1` → `backend/.gitignore:48`, exit 0;
  `git check-ignore -v backend/scripts/local-env.template.ps1` → no output, exit 1; the rule is an
  exact path with a comment saying why, so the template is not at risk. Scanned every tracked file
  AND all git history for the real credential's host, username and password: zero hits, and neither
  `local-env.ps1` nor the template contains it — the resolver reads it from outside the tree.
  gitleaks 8.30.1 confirmed installed and the gate confirmed executing (`6 commits scanned`).
  **Not closed.** Two gates red, both external: Tests (Open question 10, the missing `GRANT`) and
  Secret scan (nine pre-existing non-secret findings, now TASK-0011 — all nine verified by hand).
  The card's central criterion IS met: with the script the integration tests went from silently
  skipping to actually executing, which is the property it existed to create. Status `blocked`
  rather than `review`: nothing awaits a reviewer, it awaits the grant and TASK-0011.
  One correction to the agent's report for the record: the coverage floor printing `PASS` at 60.48%
  on a run where 30 tests FAILED is a real gate-integrity gap (the detector recognises skips, not
  failures). Recorded as drift; it cannot produce a false overall green because the Tests gate is
  red on the same run.
- 2026-08-27 **CLOSED by orchestrator.** All criteria ticked against evidence the orchestrator
  verified directly, not on report: `git check-ignore -v` both directions (real script ignored at
  `backend/.gitignore:48`, template not ignored, exit 1); no part of the real credential in any
  tracked file or anywhere in git history; `local-env.ps1` run end to end, printing only
  `resolved from: file at $HOME/.gras/pg-test.txt` and never the value; gitleaks 8.30.1 confirmed
  installed and its gate confirmed executing. The two criteria that could not go green at review
  time both now hold: gitleaks passes (via TASK-0011, which this card's gitleaks install revealed)
  and the integration suite actually runs — `Failed: 0, Passed: 30, Skipped: 0`, the exact property
  this card existed to make reachable. One caveat on the fresh-clone criterion: the README path is
  complete for the script and the variable, but a new developer also needs their database role to
  hold `CREATE` on schema `public` — the trap that cost this session a cycle. Worth a README line;
  not reopening the card for it.
