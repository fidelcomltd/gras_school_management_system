# TASK-0015 — full implementation log

Moved out of `TASK-0015.md` on 2026-09-14 (context-budget pass). A card is a brief, not a
transcript: the card body is what a future agent reads to copy the pattern, and it stays
under ~120 lines. This is the record of how the card got to closed. Nothing was edited on
the way across.

- 2026-08-27 created by orchestrator from TASK-0001's §6 audit findings S1-S3.

- 2026-09-04 orchestrator corrected the pinned contract hash before dispatch: `228ae57f` was the
  hash when the card was written on 2026-08-27; TASK-0012 moved it to `b287da03`. Also steering the
  `/health/ready` criterion: implement the app-level generous limiter, do not argue for ingress —
  the deployment target is Open question 5 and undecided, so an ingress claim is unverifiable today.

- 2026-09-04 dispatched to backend-dev.

- 2026-09-04 backend-dev CLOSED. All five acceptance criteria met:
  **S1** — new `RateLimitingOptions.HealthPolicyName` fixed-window policy, `RequireRateLimiting`'d
  onto `/health/ready` only (`HealthEndpoints.cs`); `/health/live` stays unthrottled (no dependency
  checks, nothing to amplify). Numbers: `HealthPermitLimit=120`, `HealthWindowSeconds=60` (2 req/s
  sustained). Assumed probe-interval floor: 5 seconds (tightest common default — K8s default 10s/
  min 1s, AWS ALB/NLB min 5s, Docker default 30s) with up to 3 probers sharing one partition key
  (LB + kubelet + external uptime monitor behind shared NAT) → worst case 36 req/min; 120 gives
  >3x headroom. App-level limiter per the steer above — no ingress argument made, Open question 5
  untouched. Raised in dev config and the integration test fixture (mirrors existing `PermitLimit`
  override) to avoid flakiness.
  **S2** — new port `SchoolManagement.Application.Abstractions.Persistence.IPersistenceErrorTranslator`
  (`Error? TryTranslate(Exception)`), implemented by `Infrastructure.Persistence.PersistenceErrorTranslator`
  (delegates to the unchanged, still-independently-testable static `PersistenceErrors.TryTranslate`),
  registered singleton. `GlobalExceptionHandler` now depends on the port; its `using` on
  `SchoolManagement.Infrastructure.Persistence` is gone.
  **S3 (arch test)** — added `DependencyDirectionTests.Api_DoesNotDependOnInfrastructureOutsideComposition`.
  Verified empirically (ran the test before choosing exemptions) that exactly one exemption beyond
  `Program.cs` was needed: `StartupEnvironmentGuard` (an `IHostedService` reading Infrastructure's
  `DatabaseOptions` at `StartAsync`, before traffic — startup wiring, not request-path, same
  category as Program.cs). `HealthEndpoints.cs`'s reference to `InfrastructureDependencyInjection
  .ReadinessTag` needed NO exemption — it is a `const`, inlined by the compiler, so it leaves no
  metadata reference for NetArchTest to see. **Flagging a deviation from this card's literal
  wording** ("whatever narrow exemption Program.cs genuinely needs"): the `StartupEnvironmentGuard`
  dependency pre-existed, was not named by this card or by AUDIT.md's S2, and extending the
  exemption to it (rather than porting `DatabaseOptions` too, which is not in this card's scope)
  was my call to unblock the test without scope creep. Full reasoning:
  `decisions/2026-Q3.md`, 2026-09-04 TASK-0015 entry. Please confirm this reading is acceptable.
  **S3 (stale artefact)** — `OpenApiDocument.Load()` now compares the artefact's mtime against the
  newest `.cs` file under `src/SchoolManagement.Api` (excluding `bin`/`obj`) and throws naming both
  paths and timestamps when the artefact is older. Verified the failure message fires correctly
  before regenerating, and clears after.
  **Contract**: `sha256sum` and `ci.ps1`'s own drift gate both confirm `b287da03…` byte-identical
  — unchanged. `/health/*` stay `.ExcludeFromDescription()`'d; the touched doc comments sit on
  types that never reach the document.
  **Gates** — `./backend/scripts/local-env.ps1` (DB from `$HOME/.gras/pg-test.txt`), all ten green:
  ```
  SUMMARY
    PASS: Restore
    PASS: Format
    PASS: Build (warnings as errors)
    PASS: Generate OpenAPI document (no database)
    PASS: Unit & architecture tests
    PASS: Integration tests
    PASS: Coverage threshold (60%)
    PASS: Vulnerable dependencies
    PASS: Secret scan
    PASS: OpenAPI contract drift
    Tests: total=181 passed=181 failed=0 skipped=0
    Coverage: line=75.86% branch=57.60%
  ALL GATES PASSED
  ```
  Diff: 10 files changed + 2 new (`IPersistenceErrorTranslator.cs`, `PersistenceErrorTranslator.cs`),
  171 insertions / 6 deletions. Out-of-scope items (TASK-0012/0013 territory, the coverage
  87.00%/77.18% attribution question, the recorded nice-to-haves) untouched, as instructed.

- 2026-09-04 **Reopened by orchestrator — the staleness check was narrower than the defect it
  targets.** The card's own wording ("newest file under `src/SchoolManagement.Api`") was
  faithfully implemented but was itself too narrow: the document is also shaped by types in
  `SchoolManagement.Application` (`PingResponse`, `PagedResult<SampleRecordDto>`,
  `CreateSampleRecordResponse`, all returned from `ReferenceEndpoints.cs`), so an edit there
  changed the document while the Api-only check stayed green — the "stale passes silently" case,
  still open on a live path. Narrowing attributed to the card, not to the implementation.
  Two other items from the original close were ruled on, no action needed: the
  `StartupEnvironmentGuard` architecture-test exemption is ACCEPTED (orchestrator recording it in
  `## Known drift` with a trigger); the contract hash was independently reconfirmed at `b287da03…`.

- 2026-09-04 **backend-dev fix, RECLOSED.** `OpenApiDocument.FindNewestApiSourceFile` renamed to
  `FindNewestBackendSourceFile` and widened from `src/SchoolManagement.Api` to all of `backend/src`
  (all four projects) — same `bin`/`obj` exclusion, same relative-path-and-both-timestamps failure
  message, same null-safe behaviour when the directory is absent. Class `<remarks>` and the
  method doc comment rewritten: the old "that project is what generate-openapi.ps1 starts"
  justification is gone, replaced with the cross-project example above and a new paragraph
  recording the accepted trade-off (a wider scan is slightly more likely to hit filesystem
  timestamp jitter on a fresh checkout; accepted because it fails loudly and by name rather than
  passing silently, and cannot fire at all inside `ci.ps1`).
  **Verified empirically, not asserted:** touched
  `src/SchoolManagement.Application/Reference/Ping/PingQuery.cs`, confirmed
  `Api_DoesNotDependOnInfrastructureOutsideComposition`'s sibling tests failed with the message
  naming that exact file and its timestamp, then regenerated the artefact and confirmed all 27
  architecture tests passed again.
  **Contract**: `sha256sum` reconfirms `b287da0394da7d25ced6f25c50da761b5135b4c0557ea64ac839fdd69ae09f2c`
  byte-identical; `ci.ps1`'s drift gate agrees.
  **Gates** — `./backend/scripts/local-env.ps1`, all ten green:
  ```
  SUMMARY
    PASS: Restore
    PASS: Format
    PASS: Build (warnings as errors)
    PASS: Generate OpenAPI document (no database)
    PASS: Unit & architecture tests
    PASS: Integration tests
    PASS: Coverage threshold (60%)
    PASS: Vulnerable dependencies
    PASS: Secret scan
    PASS: OpenAPI contract drift
    Tests: total=181 passed=181 failed=0 skipped=0
    Coverage: line=75.86% branch=57.60%
  ALL GATES PASSED
  ```
  Nothing else in the accepted diff touched, per instruction. `.agent/STATE.md` left to the
  orchestrator this round, per instruction; `.agent/decisions/2026-Q3.md`'s 2026-09-04 TASK-0015
  entry amended in place rather than re-appended.

- 2026-09-04 **Reopened again, narrowly — criterion 1 (the health rate limit) landed with no test,
  and `ApiTestFixture.cs:124-125` made a false coverage claim.** `.RequireRateLimiting(RateLimitingOptions
  .HealthPolicyName)` was the one new production behaviour in this card and nothing asserted it —
  deleting the line would have left the whole suite green. Separately, the fixture comment claimed
  "the rate limiter itself is verified by a dedicated test that sets its own limit"; no such test
  existed anywhere in the backend (confirmed by grepping `429`/`TooManyRequests` outside `bin`/`obj`).
  Both pre-existing, not introduced this round. Status set back to `in-progress` per instruction.

- 2026-09-04 **backend-dev fix, addressing all three in-scope items:**
  **(1)** New file `tests/SchoolManagement.IntegrationTests/HealthRateLimitTests.cs`, two tests.
  `Ready_IsRejectedOnceTheHealthPolicyLimitIsExceeded` builds its own client via
  `Fixture.WithWebHostBuilder(...)`, layering `HealthPermitLimit=2` on top of the shared fixture's
  already-booted database connection (no new container, no new migration run), sends three
  requests to `/health/ready`, and asserts the third is `429`. **Verified the test can actually
  fail, not just pass:** commented out `.RequireRateLimiting(RateLimitingOptions.HealthPolicyName)`
  in `HealthEndpoints.cs`, rebuilt, reran — `Ready_...` failed with `third.StatusCode should be
  TooManyRequests but was OK` and the assertion's own message; `Live_...` still passed, correctly,
  since `/health/live` was never gated on that line. Restored the line, rebuilt, reran — both
  passed. `git diff --stat` on `HealthEndpoints.cs` after restoring matches the prior round's
  diff exactly (17 lines), confirming the round-trip left no residue.
  **(2)** `Live_StaysUnthrottled_EvenUnderAHealthPolicyTightEnoughToRejectReady` — same technique,
  `HealthPermitLimit=1`, five requests to `/health/live`, all asserted `200`. Cost: one more
  `[Fact]` in the same file, no new fixture.
  **(3)** `ApiTestFixture.cs:124-125`'s comment rewritten: no longer claims a dedicated test
  verifies "the rate limiter itself" (singular, implying all policies); now says explicitly that
  only the health policy has coverage (naming `HealthRateLimitTests`) and that the default and
  sensitive policies have none, pointing at the drift entry the orchestrator is recording with
  TASK-0003 as the trigger.
  **Out of scope honoured:** no test added for the default or sensitive policies; no other
  refactor of `ApiTestFixture`.
  **Contract**: unchanged, `sha256sum` on both files still
  `b287da0394da7d25ced6f25c50da761b5135b4c0557ea64ac839fdd69ae09f2c`.
  **Gates** — `./backend/scripts/local-env.ps1`, all ten green:
  ```
  SUMMARY
    PASS: Restore
    PASS: Format
    PASS: Build (warnings as errors)
    PASS: Generate OpenAPI document (no database)
    PASS: Unit & architecture tests
    PASS: Integration tests
    PASS: Coverage threshold (60%)
    PASS: Vulnerable dependencies
    PASS: Secret scan
    PASS: OpenAPI contract drift
    Tests: total=183 passed=183 failed=0 skipped=0
    Coverage: line=75.16% branch=57.60%
  ALL GATES PASSED
  ```
  Diff this round: 3 files touched (`HealthEndpoints.cs` unchanged from prior round after the
  verification round-trip, `ApiTestFixture.cs` +12 lines of comment, `RateLimitingOptions.cs`/
  `Program.cs`/others untouched) plus 1 new file (`HealthRateLimitTests.cs`, ~90 lines).
  `.agent/STATE.md` left to the orchestrator again, per instruction. Status left `in-progress`
  for the orchestrator to close.

- 2026-09-04 CLOSED by orchestrator after three review rounds. Diff reviewed against §6; contract
  hash verified independently at `b287da03`; gates green with 183/183 and `Skipped: 0`. Two findings
  raised in review, both fixed: the staleness scan was Api-only (a narrowing in this card's own
  wording, corrected above), and the health limiter landed untested. A third finding — no test
  anywhere asserts a 429, and `ApiTestFixture.cs` falsely claimed one existed — was pre-existing,
  is out of scope here, and is now drift owned by `backend-dev` with TASK-0003 as the trigger.
