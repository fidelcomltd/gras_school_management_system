# TASK-0078 — Log (archived at close)


- 2026-09-17 Carded by the orchestrator on human directive, after a TASK-0076 gate silently ran against
  hosted Neon.
- 2026-09-17 Dispatch 1 returned: resolver opt-in, CI/explicit-host guard and `-SkipIntegration` done; 41 assertions,
  fail-before shown. **Blocked on AC3:** `DatabaseAvailability`'s endpoint resolution is C# in the test
  assembly with no PowerShell path to it that isn't a copy, a new project or an unrunnable test. Correctly refused to pick.
- 2026-09-17 **Orchestrator ruling: AC3's pre-flight probe WITHDRAWN**, an over-constraint in this card. The
  safety property already holds: `ApiTestFixture.cs:76` throws (fails, never skips) when no endpoint answers.
  Replaced by a container-source first line without the endpoint, plus a pure `Get-IntegrationFailureGuidance`
  that prints the ask-the-human text after a failed container-backed integration stage, tested. Resumed the same agent.
- 2026-09-17 Dispatch 2 returned: ruling applied. Orchestrator re-ran `postgres-test-connection.tests.ps1` independently: PASSED 46. `ci.ps1` diff reviewed. Closes on the next cheap-gate run, which cannot start until the live TASK-0076 gate ends (gates.md §4).
- 2026-09-17 **Orchestrator full gate (first run on the local-container default): 10/11 PASS.** 1204 tests, Skipped 0,
  integration 342 in 7m26s on `postgres:17.6-alpine`, line 80.49%. **FAIL: Secret scan**, 3 history + 12 working
  tree: 9 × `postgres-connection-string-with-password` in this card's own test strings, plus the 3 known
  `CONTRACT.lock` fixture digests (TASK-0075 drift). Verified mechanically none holds the real hosted host or password.
  Scope extended: placeholder passwords (no config change) + fixture-path `generic-api-key` allowlist, with a
  planted-credential proof. Resumed the same agent.
