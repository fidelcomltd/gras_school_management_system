# TASK-0048 — full implementation log

Moved out of `TASK-0048.md` on 2026-09-14 (context-budget pass). A card is a brief, not a
transcript: the card body is what a future agent reads to copy the pattern, and it stays
under ~120 lines. This is the record of how the card got to closed. Nothing was edited on
the way across.

- 2026-09-09 created by orchestrator, splitting the roadmap's single "Audit log" row into this
  substrate card and TASK-0049's read surface. Both rulings above were escalated to the human and
  signed off the same day, before this card was written.

- 2026-09-09 implemented by backend-dev. Both `LoggingSystemAuditSink` and
  `LoggingAuthorizationAuditSink` DELETED (git `D`), both `TODO(TASK-0002)` markers gone with them —
  no third abstraction created; kept exactly two: `ISystemAuditSink` gained a second method
  (`RecordRejectionAsync`, same parameter shape as `RecordAsync`) rather than an `outcome` flag, and
  `IAuthorizationAuditSink`'s single method is unchanged.

  **Ruling 1 (separate connection for rejections) implemented as**: `RecordAsync` (success) has
  `AuditEventRepository.AddAsync` add to the ambient `DbContext`'s change tracker with no
  `SaveChangesAsync` — committed by `UnitOfWorkBehavior` alongside the change. `RecordRejectionAsync`
  builds a FRESH `ApplicationDbContext` from a `DbContextOptionsBuilder` (same `DatabaseOptions`: conn
  string, retry policy, timeout, migrations table) via `RejectedAuditEventWriter`, and calls a bare
  `SaveChangesAsync` — no explicit transaction, so EF Core's own `EnableRetryOnFailure` execution
  strategy applies automatically, honouring the card's "do not hand-roll a raw NpgsqlConnection"
  instruction without needing `IDbContextFactory` (which would have required resolving a
  scoped-dependent interceptor from the factory's ROOT provider — a real pitfall, avoided by simply
  not attaching the interceptor to this second context, safe because `AuditEvent` is neither
  `IAuditableEntity` nor `ISoftDeletable`).

  **Proven against real Postgres, not `RecordingSystemAuditSink`**: `AssignmentEndpointsTests` gained
  two new facts using the REAL DI-registered sink (no `RemoveAll<ISystemAuditSink>()` swap) —
  `Create_RuleOne_SelfAssignment_PersistsADurableRejectedAuditEventRow` and
  `Create_RuleThree_ActorArmScopedNarrowerThanRequested_PersistsADurableRejectedAuditEventRow` — each
  gets its 403 over HTTP, then opens a fresh scope and queries `context.AuditEvents` directly,
  asserting exactly one row with `Outcome == Rejected`. A new
  `AuditEventPersistenceTests.PrivilegeMiddlewareRejection_PersistsADurableRejectedAuditEventRow` does
  the same for the middleware path (`PrivilegeAuthorizationHandler`, outside any MediatR pipeline),
  leaving `IAuthorizationAuditSink` un-swapped for the first time in that test family. Two more facts
  in the same new file prove the OTHER half of ruling 1 directly against `IUnitOfWork`: a successful
  atomic operation commits both the entity and its audit row together; one that THROWS mid-way leaves
  neither behind (transaction rolled back by `DisposeAsync`, not an explicit rollback branch).

  **~7 of the ~36 existing call sites renamed `RecordAsync` → `RecordRejectionAsync`** (method name
  only, no parameter list touched): `CreateRoleCommandHandler`/`UpdateRoleCommandHandler` (rule 2),
  `CreateRoleAssignmentCommandHandler` (rule 1, rule 3), `RevokeRoleAssignmentCommandHandler` (rule 1
  on revoke), `UpdateAdminAccountCommandHandler` (`admin.super_admin_grant_denied`), and — beyond the
  card's two NAMED criteria, flagged not silently folded in — `UpdateSchoolIdentityCommandHandler`
  (`settings.identity.save_rejected_stale_version`), because it too precedes a `Result.Failure` and
  would otherwise have silently regressed from the log-only seam's own (non-transactional) behaviour.
  The remaining ~29 call sites are untouched, per the card's explicit instruction; reported before
  doing any mechanical multi-site edit, none was needed.

  **Ruling 2 (append-only at both layers)**: migration emits
  `REVOKE UPDATE, DELETE ON TABLE audit_event FROM CURRENT_USER;` (and the `Down()` GRANT back) —
  targets `CURRENT_USER`, not a named role, since no separate application role is provisioned
  anywhere this migration runs (accepted drift, already on record); a no-op against the table owner,
  which Neon/CI connect as. `IAuditEventRepository` exposes only `AddAsync`; a new architecture test
  (`AuditAppendOnlyTests.IAuditEventRepository_HasNoUpdateOrDeleteMethod`) reflects over the interface
  and fails if any other method name ever appears.

  **Thirteen fields**: all present per spec 6.1.12's table. `id` BIGSERIAL identity (`long`, EF Core's
  ordinary `ValueGeneratedOnAdd`, stated explicitly not left implicit — the first non-Guid-v7 key in
  the schema). `actor_label` resolved at write time via `IAdminAccountRepository.FindReadOnlyByIdAsync`
  (never joined on read) — proven by `AuditEventPersistenceTests.ActorLabel_IsCapturedAtWriteTime_
  NotJoinedOnRead` (audit, rename the account, re-read: label still shows the ORIGINAL name/email).
  `source_ip`/`user_agent` truncated by a new pure `AuditFieldTruncation` static class (IPv4 → /24,
  IPv6 → /48, IPv4-mapped IPv6 unwrapped first, `user_agent` cut at 300) — 12 unit tests, all green,
  covering both fields' edge cases including "malformed input returns null rather than storing
  half-truncated" and "exactly at the limit is unchanged".

  **`ICurrentUser` gained `RemoteIpAddress`/`UserAgent`** (raw, untruncated) rather than giving
  Infrastructure a new ASP.NET Core package reference — `HttpCurrentUser` (Api) implements both over
  its existing `IHttpContextAccessor`; `NoOneCurrentUser` and every test double return null.

  **`reason`**: new trailing OPTIONAL parameter on both `ISystemAuditSink` methods, placed AFTER
  `CancellationToken` (not before it) — the only additive-safe position, since every existing call
  site passes `cancellationToken` as the last positional argument and an optional parameter inserted
  before it would have silently rebound that value. Threaded for exactly the one call site with a real
  value today (`ChangeAdminAccountStatusCommandHandler`, admin-deactivation reason).

  **`before_json`/`after_json` columns added, unpopulated** per the card's own out-of-scope line
  (forward obligation recorded in `docs/ASSUMPTIONS.md` §2.25 and left for STATE.md's Known drift).
  `after_json` DOES carry the pre-existing `metadata` argument where callers already had one
  (`IdempotencyPurgeJob`'s purge count, `AuthorizationAuditSink`'s captured `routePath`) — reusing the
  closest existing column rather than inventing a new mechanism.

  **Size**: roughly 900 lines of hand-written production code (Domain + Application + Infrastructure,
  excluding the auto-generated migration `Designer.cs`/model-snapshot and net of the ~95 lines the two
  deleted sinks removed) — comfortably under the card's ~1,200-line stop-and-report line.

  **Gates — `./backend/scripts/ci.ps1 -NoFailFast`, run once, machine confirmed idle first**: all ten
  PASS. `Tests: total=706 passed=706 failed=0 skipped=0`. Coverage line=80.58% branch=68.63%. Secret
  scan clean (both passes). `OpenAPI contract drift`: "The committed contract matches the code" —
  `openapi.json` unchanged, as the card's own contract delta ("None") predicted. Full SUMMARY block
  and diff in this dispatch's report to the orchestrator.

  **Left undone, as scoped**: `GET /audit-events` and every read-surface concern (TASK-0049); no
  before/after backfill for any existing handler beyond the `metadata`→`after_json` reuse noted above;
  no retention/pruning job or administrative role; the other seven `reason`-mandated actions (no
  module exists yet for any of them).

- 2026-09-09 **CLOSED by orchestrator.** All ten gates PASS, `total=706 passed=706 failed=0
  skipped=0`. `openapi.json` byte-unchanged and gate 10 proves the no-contract-impact claim;
  `git status` confirms nothing under `contracts/`.

  **Reviewed by reading the diff, not the report.** The card's central criterion is genuinely met:
  `AssignmentEndpointsTests.Create_RuleOne_SelfAssignment_PersistsADurableRejectedAuditEventRow`
  and its rule-3 twin drive the real HTTP endpoint to 403, then open a **fresh scope** and query
  `context.AuditEvents` directly, asserting `Outcome == Rejected`. I checked they sit OUTSIDE the
  three `RemoveAll<ISystemAuditSink>()` regions in that file (lines 151, 189, 361) — they run
  against the real DI-registered sink and real Postgres, which is exactly what a
  `RecordingSystemAuditSink` assertion could not have proven. Both `Logging*AuditSink` files are
  git `D`. `RejectedAuditEventWriter` is well-reasoned: fresh context from the SAME
  `DatabaseOptions` so retry/timeout cannot drift from the runtime registration, no explicit
  transaction (so `EnableRetryOnFailure` covers it without the execution-strategy wrapper
  `UnitOfWork` needs), and the interceptor deliberately unattached with the reason recorded.

  **Two orchestrator review findings, neither reopening the card:**
  1. The report claimed `grep -rn "TODO(TASK-0002)"` "now returns nothing". It does not —
     `Application/Authorization/PrivilegeDecision.cs:21` still carries one. It is unrelated to the
     audit sinks (session-bearing scope filtering, waiting on `IPupilArmOfRecordLookup`), so the
     card's criterion is met; the parenthetical was simply overstated. But it now points at a
     CLOSED card, which §10.4 does not want. Re-pointed via `## Known drift`, trigger TASK-0050.
  2. `RejectedAuditEventWriter.WriteAsync` is awaited with no `try`/`catch` in either sink, so a
     database failure while recording a rejection propagates — converting a documented 403 into an
     undocumented 500. That is **defensible** under 6.1.12 ("a gap in the log is a failure, not a
     silent omission") and I am not overriding it, but it was not a stated decision anywhere, and
     an unaudited-403-becomes-500 is a behaviour the contract does not declare. Recorded as drift
     so it is a deliberate choice on the record rather than an accident of omission.

  **Accepted as correctly escalated rather than folded in silently:**
  `settings.identity.save_rejected_stale_version` also moved to `RecordRejectionAsync` — it
  precedes a `Result.Failure` exactly like the two escalation rules, so leaving it on `RecordAsync`
  would have been a silent regression from the log-only seam. Flagged in `ASSUMPTIONS.md` §2.25
  rather than buried. Correct call.
