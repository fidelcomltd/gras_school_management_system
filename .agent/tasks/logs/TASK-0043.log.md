# TASK-0043 — implementation log (archived)

Moved out of the card body on 2026-09-14 at close, per `rules/governance.md` section 2.
Card: `.agent/tasks/TASK-0043.md`. Closure account: `.agent/decisions/2026-Q3.md`.

## Log
- 2026-09-08 created by orchestrator on closing TASK-0042, continuing the human's priority ruling.
- 2026-09-08 **implemented by frontend-dev — status → review.** Client unchanged (hash `82870944…`,
  not regenerated, per the dispatch note). No `backend/**`/`contracts/**` touched.

  **Files added** — `frontend/src/features/admins/`: `types.ts`, `api.ts` (7 hooks, one per
  endpoint), `admin-schema.ts`, `admins-list-screen.tsx` (+`.test.tsx`), `admin-detail-screen.tsx`
  (+`.test.tsx`), `admins-routes.tsx`, `components/{create-admin,edit-admin,change-status,
  reset-password}-dialog.tsx`, `components/temporary-password-reveal.tsx`.
  `frontend/src/features/roles/`: `types.ts`, `api.ts` (6 hooks incl. `usePrivileges`),
  `role-schema.ts`, `roles-list-screen.tsx` (+`.test.tsx`), `roles-routes.tsx`,
  `components/{create-role,edit-role}-dialog.tsx`, `components/privilege-picker.tsx`.
  `frontend/e2e/admins.spec.ts` (new spec, this card's AC). **Files edited**: `app/router/paths.ts`
  (+`admins`, `adminDetail(id)`, `roles`), `app/router/app-router.tsx` (mounted both route slices),
  `components/layout/authenticated-shell.tsx` (+nav entries `Admins`/`Roles`, gated
  `admin.view`/`role.view`).

  **Acceptance criteria**:
  - [x] Every screen behind `RequirePrivilege`; nav entries gated the same way (`admin.view` /
        `role.view`), absent rather than disabled for a caller lacking it.
  - [x] All 13 operations exercised against contract-derived MSW handlers (happy path from
        `buildHandlersFromContract`; every 401/409/422 scenario via a per-test `server.use`
        override) — none hand-written as a fixture divorced from the contract.
  - [x] **Temporary password revealed exactly once, absent from every subsequent read.** See
        STATE.md's Decisions entry for the `gcTime: 0` finding and why `reset()` alone was not
        enough. Asserted in both `admins-list-screen.test.tsx` (create flow) and
        `admin-detail-screen.test.tsx` (reset-password flow): DOM absence after "Done", AND
        `queryClient.getQueryCache()`/`getMutationCache()` searched for the literal value.
  - [x] Privilege picker groups by the register's own `groups[].title` (module), round-trips a
        selection through create (`roles-list-screen.test.tsx`'s create case) and edit (the
        unknown-code case, which also proves round-trip on the KNOWN codes). Handles an
        unrecognised code in "Other", still togglable, no crash.
  - [x] Deleting a system role surfaces the 409 `detail` verbatim (`roles-list-screen.test.tsx`).
  - [x] Suspend → reactivate round-trips; the list cache reflects each status from the mutation
        response alone (`useChangeAdminStatus`'s `setQueriesData`), proven by seeding the list
        cache and never calling `GET /admins` in that test at all.
  - [x] Revoking sessions calls `window.confirm` with a message naming the account
        (`expect.stringContaining(staffName)`), and a decline never reaches the endpoint.
  - [x] Every call through `src/api/`'s `apiGet`/`apiPost`/`apiPatch`/`apiDelete` — no raw
        `fetch`/`axios`, confirmed by `npm run check:api-drift`'s check 3 lineage (unchanged) and
        by inspection (every `api.ts` imports only from `@/api/client`).
  - [x] `npm run verify`: typecheck 0 errors, lint 0 warnings, **test 38 files / 270 passed / 0
        failed / Skipped: 0**, build succeeded (521 modules). `npm run test:e2e`: **8 passed,
        Skipped: 0**, including the new `e2e/admins.spec.ts` (create-admin → reveal temp password
        once → suspend, exactly the flow the AC names).

  **Deviations / open items, reported rather than silently resolved**:
  1. **`admin.create` is an unattested privilege code** — see the STATE.md Decisions entry for the
     full reasoning. Used by symmetry with `role.create`/`level.create`/`session.create`; gates
     only the "New admin" button in `admins-list-screen.tsx`. Needs orchestrator confirmation
     against the backend's real `Privileges.Admin.*` registration.
  2. **Self-edit carve-out (spec 6.1.2) not built** — `EditAdminDialog` only covers the
     `admin.update` path. No AC named the narrower self-edit case; flagged rather than omitted
     silently.
  3. Cut line was NOT needed — admin accounts and roles both shipped in full, including the
     privilege picker's module grouping (the piece the cut line offered to simplify further by
     dropping — grouping was already "free" since the contract returns it pre-grouped, so nothing
     was actually cut there beyond the search feature the cut line also permitted dropping, which
     was dropped).
  4. One e2e flake found and fixed (Base UI dialog/select exit-transition timing) — detail in the
     STATE.md Decisions entry — not worked around with a timeout.
- 2026-09-08 **CLOSED by orchestrator.** The one flagged uncertainty is resolved: `admin.create`
  IS attested — `Privileges.Admin.Create = "admin.create"` at
  `backend/src/SchoolManagement.Domain/Security/Privileges.cs:32`, and it is in the register. The
  gate is correct as shipped; no change needed. Deviation 2 (spec 6.1.2's self-edit carve-out, an
  account editing its own `staffName`/`phone` without `admin.update`) is genuinely unbuilt and no
  AC named it — recorded as `## Known drift`, owner a later admins card.
- 2026-09-14 reviewed and CLOSED by orchestrator. Full account: `.agent/decisions/2026-Q3.md`.
