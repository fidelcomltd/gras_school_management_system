# TASK-0041 — implementation log (archived)

Moved out of the card body on 2026-09-14 at close, per `rules/governance.md` section 2.
Card: `.agent/tasks/TASK-0041.md`. Closure account: `.agent/decisions/2026-Q3.md`.

## Log
- 2026-09-08 created by orchestrator on the human's priority ruling that product surface precedes
  further backend modules.
- 2026-09-08 implemented by frontend-dev, status → review. Full account in `STATE.md ## Decisions`
  (same date, "TASK-0041 implemented by frontend-dev"). Summary:

  - **Shell**: `components/layout/authenticated-shell.tsx` wraps `AppShell` with a nav row filtered
    by a new `hasPrivilege(session, privilege)` on `lib/auth/auth-session.ts` — item absent (not
    disabled) when the caller lacks the privilege, asserted both ways in
    `authenticated-shell.test.tsx`. Sign-out moved from a per-screen button into the shell's one
    control (still `useSignOut`'s existing revoke-then-clear ordering); the old
    `features/auth/components/sign-out-button.tsx` deleted as the superseded duplicate.
  - **Routing**: `app/router/protected-layout.tsx` (unauthenticated → sign-in, authenticated →
    shell) and `app/router/require-privilege.tsx` (403 via new `components/feedback/
    forbidden-screen.tsx`, distinct from the also-extracted `not-found-screen.tsx`) are the two
    guards. `app-router.tsx` rewritten per its own comment, split into
    `features/auth/auth-routes.tsx` and `features/settings/settings-routes.tsx`; `paths.settings`
    added.
  - **Settings screen**: `features/settings/` mirrors `features/auth/`'s shape. Read-only view for
    `settings.view`-only callers, react-hook-form + zod edit form for `settings.identity.update`
    callers, 422 field errors mapped case-insensitively, `expectedVersion` round-tripped from the
    last read (never user-edited).
  - **Gates, all green**: `typecheck` 0 errors, `lint` 0 warnings, `test` **29 files / 224 passed /
    0 failed / Skipped: 0** (was 25/205), `build` succeeded (334 modules), `check:api-drift` "No
    drift" (no contract/client change this card). `test:e2e` **5 passed, Skipped: 0** — the two
    pre-existing specs unmodified and still green, plus new `e2e/settings.spec.ts` covering
    sign-in → settings edit → sign-out end to end (the exact flow AC-8 names).
  - **Acceptance criteria**: all eight checked — see `STATE.md` entry for how each was proven
    (order-of-operations for sign-out via a stalled mock response + real `getSession()` state, not
    a spy; nav absence via `queryByRole(...).not.toBeInTheDocument()`; 403-vs-404 via the heading
    text of each).
  - **Deviation on record**: the 422 field-error tolerant-lookup helper is duplicated from
    `sign-in-form.tsx` rather than promoted to `src/shared/`, per CONVENTIONS.md §4's "promote only
    in its own commit" — this dispatch is not that commit.
  - **Flagged, not touched**: `git status` at session start already showed uncommitted TASK-0040
    changes (`schema.d.ts`, two client test files, `src/api/README.md`) plus uncommitted edits to
    `STATE.md`/`TASK-0040.md` themselves, despite `STATE.md` recording TASK-0040 as closed. Left
    alone — a repo-hygiene question for the orchestrator, not this card's to resolve.
  - Nothing committed, per this session's instructions.
- 2026-09-14 reviewed and CLOSED by orchestrator. Full account: `.agent/decisions/2026-Q3.md`.
