# TASK-0024 — full implementation log

Moved out of `TASK-0024.md` on 2026-09-14 (context-budget pass). A card is a brief, not a
transcript: the card body is what a future agent reads to copy the pattern, and it stays
under ~120 lines. This is the record of how the card got to closed. Nothing was edited on
the way across.

- 2026-09-05 created by orchestrator from PR #9's first real backend-ci run.

- 2026-09-05 backend-dev, TASK-0024 done.
  - `global.json`: `rollForward` changed `latestFeature` → `latestPatch`. `latestFeature` let CI
    roll to a NEWER feature band (e.g. 10.0.1xx → 10.0.2xx) than the pinned `10.0.100`, which is
    exactly what pulled in newer analyzers CI saw and local didn't. `latestPatch` still tolerates
    a missing exact patch (so CI isn't fragile to a security-patch bump) but cannot cross a
    feature-band boundary, so CI and local now resolve to the same analyzer set by construction.
    This machine only has 10.0.100 installed, so I could not install CI's newer band locally to
    directly re-trigger the original failures under it — the fix is verified by SDK-resolution
    semantics, not by reproducing the failure and watching it disappear. Flagging this rather than
    claiming a repro I didn't have.
  - CA2025 (`PrivilegeAuthorizationTests.cs:209`, `SendAsAsync`): real bug, not analyzer noise.
    `using var request = new HttpRequestMessage(...)` sat inside a **non-async** method that
    `return`ed the `Task` from `_client.SendAsync(request, ...)` directly. Because the method body
    was synchronous, the `using` block's `Dispose()` ran the instant `SendAsync` returned its
    (still in-flight) `Task` — i.e. `request` could be disposed while `HttpClient` was still
    reading it. Fix: made the method `async` and `await`ed the call, so `Dispose()` is deferred by
    the compiler's state machine until the whole send completes. Before/after in the diff below.
  - CA1873 (`RequestLoggingBehavior.cs:78`, cancellation branch): the log call already goes
    through a `[LoggerMessage]` source-generated method (`BehaviorLog.RequestCancelled`), but that
    only skips the *internal* formatting/allocation when the level is disabled — the caller-side
    argument `(long)Stopwatch.GetElapsedTime(timestamp).TotalMilliseconds` is a plain C# method
    call passed as an argument, so it still runs unconditionally regardless of whether
    Information logging is enabled. (The success path a few lines up isn't flagged because it
    precomputes the same expression into a local `elapsed` variable first — but that only hides
    the eager evaluation from the analyzer, it doesn't skip it, so I didn't copy that pattern.)
    Fix: wrapped the call in `if (logger.IsEnabled(LogLevel.Information))` so the Stopwatch call
    itself is skipped when disabled — the analyzer-recognised, semantically real fix.
  - `dotnet test`: on the pinned 10.0.100 SDK (the only SDK on this box), unit+arch+integration
    all ran clean, `Skipped: 0` throughout — the `argument <dll> is invalid` failure did not occur
    here. I cannot confirm from this machine whether pinning ALONE would have fixed it under CI's
    previously-installed newer band, since I have no way to install that band locally to check;
    what I can say is CI will no longer reach that band at all once this lands, so the question is
    moot for future runs unless `latestPatch` itself needs revisiting.
  - Secret scan: `no leaks found`, gitleaks 8.30.1 (already bumped in the workflow by the
    orchestrator), 21 commits scanned — matches local, passing for the version reason, not a
    silent skip (the gate prints commit/byte counts and a verdict; no "gitleaks not installed"
    warning appeared).
  - Contract: `Generate OpenAPI document` + `OpenAPI contract drift` gates both ran and reported
    "The committed contract matches the code" — `contracts/openapi.json` did NOT move. Not
    promoted (nothing to promote).
  - Gate output (`ci.ps1 -Configuration Release -NoFailFast`), SUMMARY block:
    ```
    SUMMARY
      PASS: Restore
      PASS: Format
      PASS: Build (warnings as errors)
      PASS: Generate OpenAPI document (no database)
      PASS: Unit & architecture tests
      PASS: Integration tests
      PASS: Coverage threshold (60%)
      PASS: Vulnerable dependencies
      PASS: Secret scan
      PASS: OpenAPI contract drift
      Tests: total=245 passed=245 failed=0 skipped=0
      Coverage: line=77.91% branch=58.92%
    ALL GATES PASSED
    ```
  - Requesting (not adding) per the card: a `dotnet --version` print step in
    `.github/workflows/backend-ci.yml` before the gates run, so the CI SDK is never inferred again.
  - Files touched: `backend/global.json`,
    `backend/src/SchoolManagement.Application/Behaviors/RequestLoggingBehavior.cs`,
    `backend/tests/SchoolManagement.IntegrationTests/PrivilegeAuthorizationTests.cs`. Nothing
    outside `backend/**`.
