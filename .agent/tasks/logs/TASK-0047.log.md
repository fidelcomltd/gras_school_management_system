# TASK-0047 — full implementation log

Moved out of `TASK-0047.md` on 2026-09-14 (context-budget pass). A card is a brief, not a
transcript: the card body is what a future agent reads to copy the pattern, and it stays
under ~120 lines. This is the record of how the card got to closed. Nothing was edited on
the way across.

- 2026-09-09 created by orchestrator. Contract staleness verified by running
  `npm run check:api-drift` (RED) and recomputing `sha256sum contracts/openapi.json` against
  `CONTRACT.lock` (match at `c3cb88ff…`).

- 2026-09-09 implemented by frontend-dev. Hash independently recomputed
  (`sha256sum contracts/openapi.json`) and confirmed to match `CONTRACT.lock`
  (`c3cb88ff74b931ba58057a11b781fbad58d72d1778ff91584dbfe712f661a4c9`) before starting.
  `npm run generate:api` rewrote `frontend/src/api/schema.d.ts` (+402/-4 per `git diff --stat`, i.e.
  +398/-4 lines), purely additive: the `ListRoleAssignments`, `CreateRoleAssignment`,
  `RevokeRoleAssignment` operations and their schemas (`RoleAssignmentDto`,
  `CreateRoleAssignmentCommand`, `RoleAssignmentStatus`, `ScopeType`). `check:api-drift` → "No
  drift." Zero new lines in `client.ts`/`client-types.ts` — seventh confirmation of the
  TASK-0033/0037/0040/0044 finding.

  New test file, under CONVENTIONS.md §3's 180-line cap: `client-assignments.test.ts` (126
  lines, all three operations). `src/api/README.md` updated (tests table + zero-new-code
  precedent list, both now naming TASK-0047).

  **Both negative typing directions proven on the card's named pair**: `CreateRoleAssignment`
  rejects an omitted required `Idempotency-Key`; `ListRoleAssignments` rejects a passed
  `idempotencyKey` (it declares `header?: never` — no such header at all).
  `RevokeRoleAssignment` deliberately not used for either, per the card's own instruction (it
  accepts the header optionally). All three operations proven to reject an omitted required `id`
  path parameter. §8 enum tolerance proven for **both** new enums (`RoleAssignmentStatus` and
  `ScopeType`) via one `server.use(...)` override on `GET /admins/:id/assignments` returning
  unrecognised members for both fields at once, asserted to pass through unchanged. Override route
  written as `apiUrl('/api/v1/admins/:id/assignments')`, never the contract's literal `{id}`, per
  the card's explicit warning.

  **The card's named trap, hit exactly as predicted**: `ListRoleAssignments`'s 200 body is an
  inline `array` of `$ref RoleAssignmentDto` with no top-level `example`, so the contract-derived
  default MSW handler answered an empty body instead of an array (confirmed by reading
  `openapi-handlers.ts`'s `exampleFor`, which only resolves an example off a `$ref`'d schema or an
  inline one that names its own — this schema does neither). Fixed with an explicit
  `server.use(...)` override in every test that calls `ListRoleAssignments`, exactly as TASK-0040
  did for `ReorderLevels` and TASK-0047's own notes anticipated. `openapi-handlers.ts` untouched;
  no assertion loosened.

  **All 5 `@ts-expect-error`s verified load-bearing by the removal-probe method**: file copied to
  `.bak`, all five comment lines deleted at once, `npm run typecheck` re-run, reproduced exactly
  five errors (`TS2554` ×2 for the two omitted-required-argument cases, `TS2353` for the
  `idempotencyKey`-rejected case, `TS2345` ×2 for the two omitted-`pathParams` cases on
  `CreateRoleAssignment`/`RevokeRoleAssignment`), then restored from `.bak` and re-confirmed
  `npm run typecheck` clean before deleting the backup.

  **One discrepancy from the card's own notes, flagged not silently corrected**: the card states
  "`RoleAssignmentDto.armIds` is nullable in the contract." Reading the committed schema directly
  (`contracts/openapi.json`'s `RoleAssignmentDto.armIds`) shows it is `required` and typed
  `"type": "array"` — never nullable; only `CreateRoleAssignmentCommand.armIds` (the request body)
  is nullable (`"type": ["null", "array"]`), matching its own description ("must be empty or
  omitted" when school-wide). No client code was built either way (client-seam-only card), so this
  had no effect on what shipped, but it is exactly the kind of thing a later screen card would
  otherwise silently inherit as a wrong assumption — recommend correcting the note if this card's
  text is reused as a template again.

  **Gates, all run directly by this session, in order**: `npm run typecheck` (`tsc -b`) 0 errors;
  `npm run lint` (`oxlint --max-warnings=0`) 0 warnings; `npm run test` (`vitest run`) **44 files,
  317 passed, 0 failed, Skipped: 0** (confirmed against a clean stash: 43/308 immediately before
  this file existed, so +1 file/+9 tests matches the new file exactly); `npm run build`
  (`tsc -b && vite build`) succeeded, 537 modules; `npm run check:api-drift` "No drift"
  (re-confirmed after the gate run). `gitleaks detect --source . --no-git --config
  backend/.gitleaks.toml --redact --no-banner` → "no leaks found" (5.94 MB scanned) — every
  fixture is the same fixed UUID-shaped constant pattern already used throughout
  `src/api/client-*.test.ts`, nothing resembling a real credential.

  **`npm run test:e2e` deliberately SKIPPED** — no screen, route, or auth/session code touched;
  this is a client-seam-only card per its own Out of scope, same call TASK-0040/TASK-0044 made.
  Note: the card's own AC text says "Last known green: 4 passed / Skipped 0 (TASK-0043)", but
  TASK-0043's own Log and `STATE.md`'s Decisions both record **8 passed, Skipped: 0** as the last
  run — flagging the discrepancy rather than silently using either number, since this dispatch did
  not re-run the suite to check.

  **Files changed**: `frontend/src/api/schema.d.ts` (regenerated, +398/-4),
  `frontend/src/api/client-assignments.test.ts` (new, 126 lines), `frontend/src/api/README.md`
  (+9/-4). Nothing under `contracts/**` or `backend/**` touched; hash unmoved at `c3cb88ff…`.

  **Confirmed out of scope, not built, exactly as the card scoped**: no assignments UI, no
  role-grant form, no scope picker, no TanStack Query hooks, no `features/assignments` folder,
  no changes to `client.ts`/`client-types.ts` beyond the (zero) proven gap. Status table and card
  closure left to the orchestrator, as instructed.

- 2026-09-09 **CLOSED by orchestrator.** Diff reviewed directly, not taken on report. All five
  §4.4 checks verified GREEN by contract-guardian: backend→contract byte-identical (38 paths,
  342413 bytes), contract→client "No drift", no raw `fetch`/`axios` outside `src/api/` and
  `src/lib/http/` (`http-boundary.test.ts` 2 passed), `CONTRACT.lock` matched at `c3cb88ff…`,
  `schema.d.ts` header intact with no hand edit.

  **Reviewed, by reading the diff:** the 4 deleted lines in `schema.d.ts` are all
  generator-driven doc-comment reflows carrying TASK-0030's real backend changes — the
  `DeleteRole` description no longer claims "no `role_assignment` table exists yet", and
  `AuthSessionResponse.effectivePrivileges` no longer cites the now-deleted
  `SuperAdminFlagEffectivePrivilegeProvider`. That is evidence the regeneration picked up genuine
  upstream movement rather than only appending, which is the stronger result. Zero new lines in
  `client.ts`/`client-types.ts` — seventh consecutive confirmation. Test file 126 lines, under
  CONVENTIONS.md §3's 180-line cap. Both new enums (`ScopeType`, `RoleAssignmentStatus`) proven
  §8-tolerant in one override; override routes correctly written `:id`; both negative typing
  directions proven on the right pair, and the card's warning that `RevokeRoleAssignment` proves
  neither (optional `Idempotency-Key`) was respected.

  **The implementing agent corrected TWO errors in this card's own text; both verified and fixed
  above.** (1) `RoleAssignmentDto.armIds` is required and non-nullable — only the *command* is
  nullable; I had written the opposite. (2) e2e last-known-green is 8, not 4 — I copied
  TASK-0040's then-current figure without re-checking. Neither affected what shipped, both would
  have propagated into the eighth run of this card. Catching an orchestrator error in the card
  rather than implementing around it is exactly the behaviour §4.5 asks for.
