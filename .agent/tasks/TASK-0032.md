# TASK-0032 — The secret-scan gate is red, and it is structurally one card late

Owner: backend-dev
Depends on: nothing
Contract impact: none
Status: implemented → review

## Goal

`ci.ps1` gate 9 green for the right reason, and able to see the change it is gating.

## Two problems, one card

**1. Six findings, all the same fake string.** `gitleaks detect` reports six `generic-api-key`
hits on the fake `temporaryPassword` example TASK-0027 put in `OpenApiExamples.cs`, echoed into
`contracts/openapi.json` and `frontend/src/api/schema.d.ts`. Verified by the orchestrator
2026-09-06: commits `814b711` and `4170314`, both predating TASK-0028. Not a real credential.

**2. The gate cannot see the diff it is gating — this is the bigger one.** `gitleaks detect`
scans *committed history*. A dispatch runs the gates against an uncommitted working tree, so
gitleaks reads only what is already committed and the change under test is invisible to it. That
is why TASK-0027 closed with gate 9 green and TASK-0028 dispatch 1 found it red having touched
none of the flagged files: **the gate always fires one card late, against the previous card's
author.** This is the TASK-0011/0017 pathology again — a scan reporting PASS without having
examined the thing in question.

## Acceptance criteria

- [x] Gate 9 also scans the working tree (`gitleaks detect --no-git`, or `protect --staged`),
      so a secret introduced by the dispatch under test fails that dispatch and not the next one.
      A test proves it: plant a matching fake string in an uncommitted file, assert the gate fails.
      Done via `--no-git` (chose it over `protect --staged`: staged-only misses an uncommitted,
      unstaged file, the normal shape of a dispatch's own working tree). Proof:
      `backend/scripts/tests/secret-scan-working-tree.tests.ps1`, 3/3 assertions pass.
- [x] The six existing findings cleared **deliberately**, choosing between: a `.gitleaksignore`
      fingerprint entry per finding; changing the example string so it stops matching
      `generic-api-key`; or tightening the rule. Whichever is chosen, write down why in
      `backend/docs/ASSUMPTIONS.md` — TASK-0011 proved that an untested rule and an unexplained
      allowlist entry are the same defect wearing different clothes.
      Chose tightening the rule (a content-anchored `.gitleaks.toml` allowlist entry, Family C).
      Full reasoning, including why the other two are worse for this card: `ASSUMPTIONS.md` §2.19.
- [x] Do NOT blanket-ignore `contracts/openapi.json`, `schema.d.ts` or `OpenApiExamples.cs`. Those
      are exactly the files a real leaked example would land in.
      No path in any of the three is allowlisted anywhere; Family C matches only the one literal
      example string, verified not to swallow an unrelated planted secret in the same tree.
- [x] `ci.ps1`'s own self-test suite covers the new working-tree branch.
      `backend/scripts/tests/secret-scan-working-tree.tests.ps1`, new, same hand-rolled style as
      the two existing self-tests. Not yet wired into `.github/workflows/backend-ci.yml`
      (orchestrator-owned file) — flagged for the orchestrator to add, per the existing precedent.

## Out of scope

The other nine gates. `.gitleaks.toml` rule authorship beyond what criterion 2 needs.

## Log
- 2026-09-06 created by orchestrator. Finding 1 reported by TASK-0028 dispatch 1 and independently
  reproduced; finding 2 is the orchestrator's, from asking why a gate that ran green in TASK-0027
  went red in the next dispatch without the flagged files being touched.
- 2026-09-06 implemented by backend-dev.

  **Problem 1 — six findings cleared.** Chose a content-anchored `.gitleaks.toml` allowlist entry
  (Family C: `targetRules = ["generic-api-key"]`, `regexTarget = "match"`, anchored to the literal
  `temporaryPassword": "aB3xQ9mK2pL7vN4wR8dT"`), not a `.gitleaksignore` fingerprint and not
  changing the example string. Reasoning in full: `backend/docs/ASSUMPTIONS.md` §2.19. Short
  version — a fingerprint only clears the git-mode scan and this card adds a second (`--no-git`)
  scan of the same still-committed files, so a fingerprint would need maintaining twice; changing
  the example string does not clear the two introducing commits at all (git-mode re-evaluates
  every commit's own diff regardless of current content) and would force a contract regeneration
  for zero benefit against a card whose contract impact is declared `none`.

  **Problem 2 — gate 9 now runs gitleaks twice.** `backend/scripts/ci.ps1`'s "Secret scan" gate:
  pass 1 unchanged (`gitleaks detect --source . --config .gitleaks.toml`, git history, whole repo
  via git's own root discovery); pass 2 new (`gitleaks detect --source <repo-root> --no-git
  --config .gitleaks.toml`, working tree, `--source` pointed at the repo root explicitly since
  `--no-git` has no git root to discover from). Chose `--no-git` over `protect --staged`: staged-only
  would miss the normal shape of a dispatch's own working tree before `git add` ever runs. Gate
  fails if either pass fails.

  Turning on a whole-repo `--no-git` pass surfaced three things problem 1 didn't name, all
  pre-existing, all cleared the same way (content-anchored allowlist):
  - `frontend/node_modules/` alone did not finish a plain `du -sh` in two minutes — extended the
    existing "Build output and packages" path allowlist with `node_modules/`, `.git/`,
    `dist|coverage`. Whole-repo `--no-git` now completes in ~1-2s (path allowlist is evaluated
    before content is read).
  - Family D: TASK-0011's two realistic-fake proof connection strings
    (`.agent/tasks/logs/TASK-0011.log.md`, `ASSUMPTIONS.md` §3.9) — TASK-0017's git-mode
    fingerprint doesn't reach the new no-git pass. Two more content-anchored regexes added,
    `targetRules = ["postgres-connection-string-with-password"]`.
  - Family E: TASK-0031's own self-test (`postgres-test-connection.tests.ps1`, uncommitted at the
    time this card was worked, left untouched per this card's own instruction) plants
    `selftest-marker-3fae1c` as both host and password, tripping both rules. One content-anchored
    entry, both rules — not an edit to TASK-0031's file.

  **New self-test**: `backend/scripts/tests/secret-scan-working-tree.tests.ps1` — builds a
  throwaway `git init` repo, plants a fake secret (generated at run time from a fresh GUID, never
  a static literal in the test file's own tracked source) in a file deliberately never committed,
  proves git-mode alone misses it, `--no-git` catches it, and a clean working tree does not
  falsely fail. `PASSED: 3 assertion(s)`, exit 0. Left for the orchestrator: a
  `.github/workflows/backend-ci.yml` step to run it in CI, alongside the two existing self-test
  steps (not this card's file to edit).

  **Verified, not just asserted**: an unrelated realistic fake key
  (`sk-live-<40 hex chars>`, one-line file under `backend/src/`, never committed, deleted
  immediately after) still trips the new `--no-git` pass and is NOT swallowed by any of Families
  C/D/E — the new allowlist entries match only their own specific literal strings.

  **Gates**: full `./backend/scripts/ci.ps1 -NoFailFast` — `ALL GATES PASSED`, exit 0.
  `Tests: total=388 passed=388 failed=0 skipped=0`, `Coverage: line=78.54% branch=62.54%`. Secret
  scan pass 1 (history) `32 commits scanned` / `no leaks found`; pass 2 (working tree) `no leaks
  found`. OpenAPI contract drift clean, contract hash unmoved. Full SUMMARY block in STATE.md.

  **Files changed**: `backend/.gitleaks.toml` (Families C/D/E), `backend/scripts/ci.ps1` (gate 9,
  two-pass), `backend/scripts/tests/secret-scan-working-tree.tests.ps1` (new),
  `backend/docs/ASSUMPTIONS.md` (§2.18 marked resolved, new §2.19). Nothing under `contracts/**`
  touched — contract hash unchanged, confirmed by the "Generate OpenAPI document" gate's own
  "Inspection only — the committed contract was NOT modified" line.

  **Left undone**: the CI wiring step named above (orchestrator-owned file). `TASK-0031`'s and
  `TASK-0028` dispatch 1's own uncommitted work left exactly as found — not reviewed, not touched,
  per the dispatch's own instruction.
