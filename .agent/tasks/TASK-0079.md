# TASK-0079 — Regenerate the typed client against `c5c4d6c6…` and add `apiPut`

Owner: frontend-dev
Depends on: TASK-0076 (done; contract promoted)
Contract impact: **none** — this card consumes a contract already promoted. It must not change one byte of
`contracts/**`.
Status: **done 2026-09-17** — closed on the orchestrator's gate run
Reads: `frontend/src/api/README.md`; `frontend/CONVENTIONS.md` §4; `.agent/rules/wire.md`;
       `.agent/rules/gates.md` §1 (frontend row); `.agent/spec/frontend.md`.

## Goal

`src/api/schema.d.ts` matches the committed contract, and the score-sheet `PUT` is callable, so the
score-entry screen card that follows has a typed seam to build on.

## Contract delta

None. `jq` only what you need: the two new paths are `/api/v1/arms/{armId}/score-sheets` and
`/api/v1/arms/{armId}/score-sheets/void`; the nine new schemas are `ScoreSheetDto`,
`ScoreSheetRowDto`, `ScoreSheetComponentDto`, `ResultSetSummaryDto`, `ResultSetState`,
`SaveScoreSheetCommand`, `SaveScoreSheetRowInput`, `VoidScoreSheetCommand`, `VoidScoreSheetResponse`.

## Acceptance criteria

- [x] `npm run generate:api`, then `npm run check:api-drift` reports `No drift`, exit 0. No hand edit
      to `schema.d.ts` — regeneration must reproduce it byte-for-byte.
- [x] **Add `apiPut` to `src/api/client.ts`, mirroring `apiPatch` exactly** (same CSRF, error and
      `ResponseBodyOf` handling). This closes the drift entry of 2026-09-16: `UpdateAssessment`,
      `UpdateGrading` and `ResetGrading` have been typed but uncallable since TASK-0074, and
      `PUT /arms/{armId}/score-sheets` would be the fourth.
- [x] A test proving `apiPut` sends what `apiPatch` sends, method aside (MSW, alongside the existing
      client tests). Not a snapshot of itself.
- [x] `npm run typecheck` and `npm run lint` exit 0. Pin and lockfile untouched
      (`openapi-typescript@7.13.0`, exact).
- [x] Nothing consumes the score-sheet types yet beyond the `apiPut` test — no screens, no queries.

## Out of scope

The score-entry grid (its own card: §6.7.4, offline resilience, keyboard behaviour). Calling the
grading/assessment PUTs that `apiPut` unblocks. Any `backend/**` or `contracts/**` change.

## Notes for the implementing agent

- **Nearest shape:** TASK-0074 did exactly this regeneration; `src/api/README.md` predicted that the
  zero-new-wrapper-code streak breaks precisely here. `apiPatch` in `src/api/client.ts` is the template.
- `ResultSetState` crosses the wire as a string enum: the client must tolerate an unknown member
  (`rules/wire.md`), so do not switch exhaustively over it without a default.
- The score sheet's `componentMarks` is a map of component id → `number | null`, where **null means a
  blank cell and is never zero**. Keep that distinction in the types; do not default it.
- Do NOT run `npm run verify`, `npm run test:e2e` or the drift gate as a whole-suite run
  (`gates.md` §1): run `typecheck`, `lint`, `check:api-drift` and the tests for the files you touch.
- **Do not write to `.agent/**`.** Do not commit.
- Estimate ≈ 150 lines incl. the test, most of it generated.

## Log

- 2026-09-17 Carded by the orchestrator at TASK-0076's close, which moved the contract to `c5c4d6c6…`
  (64 paths, 132 schemas).
- 2026-09-17 Dispatched to `frontend-dev`. Two document conflicts resolved by orchestrator ruling at
  dispatch: (1) `gates.md` §1 lists `check:api-drift` under what frontend-dev does not run while AC-1
  requires it — agent ran it as a cheap self-check, orchestrator re-ran it authoritatively per the
  TASK-0074 precedent; (2) the agent definition orders an `.agent/STATE.md` append while also
  forbidding writes outside `frontend/**` — agent emitted a `LEDGER ACCOUNT` section instead and the
  orchestrator appended it.
- 2026-09-17 Implemented. `schema.d.ts` regenerated (2,674 insertions / 221 deletions overall);
  `apiPut` + `PutPath` added to `client.ts` (+25/-1); `client-score-sheets.test.ts` added;
  `README.md`'s two standing predictive lines updated now that the streak they predicted has broken.
  `putRequest` already existed in `@/lib/http` and only needed importing — no transport change.
- 2026-09-17 **Closed by the orchestrator.** Gates re-run, none accepted on report: `check:api-drift`
  → `No drift`, exit 0; `npm run verify` → 55 files, 381 tests, 0 failed, **Skipped 0**, build clean,
  exit 0. `CONTRACT.lock` and the live document both `c5c4d6c6…`. Scope verified clean — only
  `frontend/src/api/` touched, pin and lockfile untouched.
- 2026-09-17 **Review findings.** (1) CSRF attachment verified directly at `http-client.ts:81` rather
  than accepted from the report: it is a DENYLIST (`!== 'get' && !== 'head'`), so PUT was covered with
  no interceptor change — an allowlist would have shipped a silent CSRF bypass on every score-sheet
  save. (2) Non-vacuity re-proven with an INDEPENDENT mutation (`putRequest`→`patchRequest`, 3 failed /
  1 passed, 4/4 on revert), different from the agent's dropped-Idempotency-Key injection, per the
  TASK-0075 precedent. (3) The test pins absolute values, not just cross-verb equality, which is what
  stops the TASK-0073 both-null vacuous-pass shape. (4) New drift logged: the `@ts-expect-error`
  type-level tests in `src/api/` fire real unawaited requests — pre-existing since TASK-0029, mirrored
  correctly here, not a fault of this card.
