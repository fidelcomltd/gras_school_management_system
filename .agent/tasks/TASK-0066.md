# TASK-0066 — `GET /admissions/{id}`: make a pending record readable

Owner: backend-dev
Depends on: TASK-0062 (`admission_record`, `AdmissionRecordDto`, `PATCH /admissions/{id}` — closed),
TASK-0051 (approve/decline — closed).
Contract impact: **additive** — one new GET on an existing path. No existing field changes, no
existing response shape changes. `.agent/rules/contract.md` applies; the delta below is APPROVED.
Status: **done 2026-09-15** — closed by the orchestrator on a full green gate (10/10, 993 tests, Skipped 0). Archive: `decisions/2026-Q3.md` 2026-09-15 (TASK-0066).
Reads: `.agent/spec/backend.md`, `.agent/rules/wire.md`, `.agent/rules/contract.md`,
       `.agent/rules/gates.md` (section 1 only);
       `backend/src/SchoolManagement.Application/Admissions/` — the whole slice, especially
       `UpdateAdmissionRecordHandler.cs` (its pending-only 404 rule is the one to copy) and
       `AdmissionRecordMapper.cs`;
       `backend/src/SchoolManagement.Application/Pupils/GetPupilHandler.cs` and
       `PupilAccessGuard.cs` — the nearest existing GET-by-id of this shape;
       `backend/src/SchoolManagement.Api/Endpoints/AdmissionEndpoints.cs`;
       `grep -n "ListAdmissionsQueue" .agent/decisions/2026-Q3.md` for why the queue is
       SCHOOL-WIDE `pupil.view` only

## Goal

A clerk opening a pending applicant to approve them must choose an arm, and the arm selector is
`GET /arms?sessionId=&levelId=`. Neither id is reachable today: the queue row carries
`levelAppliedFor`, a display *name*, and `admission` is `null` on every pupil read path. `PATCH
/admissions/{id}` returns the record but is a write on the wrong privilege. **TASK-0064 is blocked
on exactly this and shipped its approve dialog as a stub** — this card unblocks the only screen
that reaches admission approval.

## Contract delta

- `GET /api/v1/admissions/{id}` — new. `id` is the PUPIL id, the same identity `PATCH
  /admissions/{id}` and the queue already use. Response: `AdmissionRecordDto`, the same shape
  `PATCH` already returns — **reuse it, do not define a second**. Privilege `pupil.view`,
  SCHOOL-WIDE only, for the reason the queue is school-wide only: a pending record has no arm, so
  an arm-scoped grant has no reach. 404 when `id` does not name a **pending** pupil — the same rule
  and the same message `PATCH /admissions/{id}` already applies. No write, no audit-on-read.

## Acceptance criteria

- [x] `GET /api/v1/admissions/{id}` returns `AdmissionRecordDto` for a pending pupil, carrying
      **`sessionId` and `classAdmittedInto`** — the two opaque ids TASK-0064 needs. Asserted by id,
      not by display name.
- [x] 404 for an approved pupil, a declined pupil, and an unknown id — three tests, the same
      rule `UpdateAdmissionRecordHandler` already enforces. **Reuse that rule; do not restate it.**
- [x] 403 for a caller without `pupil.view`; 403 for an **arm-scoped** `pupil.view` grant, matching
      `ListAdmissionsQueue`'s school-wide-only decision rather than returning an empty answer.
- [x] The response is byte-identical to what `PATCH /admissions/{id}` returns for the same record —
      one mapper, one shape. A test asserts the two agree.
- [x] Nothing is written: no audit row, no `updated_at` bump. Asserted, not assumed.
- [x] `openapi.json` regenerated; **additive verified mechanically** — every existing schema's
      `required` array and every property type unchanged. Report the before/after hash.

## Out of scope

- Populating `admission` on `GET /pupils/{id}` — that is the pupil detail-view card and its own
  live drift entry. This card adds a read route; it changes no existing response.
- Any change to `PupilDto`, the queue row, or `ListAdmissionsQueue`.
- Steps 2 to 8 of the admission flow, and any new field on `AdmissionRecordDto`.
- Any `frontend/**` change. TASK-0064 consumes this in a follow-up dispatch.

## Notes for the implementing agent

**This is a read twin of an existing write.** `UpdateAdmissionRecordHandler` already loads the
record, already enforces pending-only-404, and already maps through `AdmissionRecordMapper`. Your
handler is that one with the mutation removed. If you find yourself writing a second mapper or a
second pending check, stop — you are duplicating, and the two copies will diverge.

**Do not widen the privilege.** It is tempting to make this arm-scoped like `GET /pupils`, which
admits an arm-scoped caller to an honest empty page. That reasoning does not transfer: this is a
single-record read, so "empty" would be a 404 that lies about existence. `ListAdmissionsQueue`
chose school-wide-only for this reason and this route follows it.

**Keep the diff small.** This is one query, one handler, one endpoint registration and its tests.
If it exceeds ~250 lines, something has been duplicated.

## Log
- 2026-09-15 created by the orchestrator, mid-flight on TASK-0064. `frontend-dev` correctly refused
  to shim the missing ids — it rejected name-matching against `GET /levels` and assuming "the
  active session" as guesses the contract does not support, and shipped a blocked stub instead.
  That is `rules/contract.md` §5's "backend wins the how, frontend wins the what" working as
  written. The contract delta is approved as additive at creation.
- 2026-09-15 **implemented by backend-dev.** One query, one handler, one endpoint registration, nine
  integration tests — no second mapper, no second pending-only-404 check.
  - `GetAdmissionRecordQuery`/`GetAdmissionRecordQueryHandler`
    (`backend/src/SchoolManagement.Application/Admissions/GetAdmissionRecord.cs`,
    `GetAdmissionRecordHandler.cs`, new files): loads the pupil via
    `IPupilRepository.FindReadOnlyByIdAsync` (ignores the pending-exclusion filter, same as
    `UpdateAdmissionRecordHandler`), 404s unless `Status == Pending`, loads the record via a NEW
    `IAdmissionRecordRepository.FindReadOnlyByPupilIdAsync` (added alongside the existing
    `FindTrackedByPupilIdAsync` — spec 6.6's `AsNoTracking()`-for-reads rule; the tracked method is
    for the PATCH command only), resolves `classAdmittedIntoName` the same way, and maps through the
    existing `AdmissionRecordMapper.ToDto` — no second mapper, no audit call, no `SaveChangesAsync`
    (queries never run through `UnitOfWorkBehavior`, which is `ICommand`-only).
  - `AdmissionEndpoints.cs`: added `MapGetOne` — `GET /{id:guid}`, `RequirePrivilege(Pupil.View,
    ScopeParameterKind.None)`, the exact school-wide-only pattern `MapListQueue` already uses (an
    arm-scoped grant resolves to `ScopeResolution.NotApplicable`, which only a school-wide grant
    satisfies — confirmed by reading `PrivilegeDecision.IsAuthorized` and
    `ScopeParameterKind.None`'s own doc comment before writing the endpoint, rather than assumed).
  - `IAdmissionRecordRepository`/`AdmissionRecordRepository`: added the one read-only method above.
    No DI change needed — the interface is already `AddScoped` and handlers are picked up by
    `ApplicationDependencyInjection`'s assembly scan.
  - Tests added to `PupilEndpointsTests.cs` (already hosts the PATCH /admissions/{id} tests and every
    helper needed — `CreateCommandAsync`, `SeedPupilDirectlyAsync`,
    `SetRegistrationNumberAndStatusAsync`, the `ScopeType.ArmList` 403 pattern, `RecordingSystemAuditSink`
    — reused as-is, nothing duplicated): happy path (asserts `sessionId`/`classAdmittedInto` BY ID,
    not display name), 404×3 (approved, declined/withdrawn, unknown id), 403×2 (no `pupil.view`;
    arm-scoped `pupil.view`), byte-identical-to-PATCH (structural `record` equality on the shared
    DTO), and a no-write proof (swaps `ISystemAuditSink` for `RecordingSystemAuditSink`, asserts zero
    records, and asserts `AdmissionRecord.ModifiedAtUtc` is bit-for-bit unchanged before/after).
  - **Targeted tests**: `PupilEndpointsTests` filter — **Passed 32, Failed 0, Skipped 0** (6m38s,
    real Neon DB, connection resolved via `backend/scripts/lib/postgres-test-connection.ps1`, the
    same shared resolver `ci.ps1` dot-sources — not a new wrapper). `dotnet build -warnaserror` clean
    on Api, IntegrationTests and UnitTests (0 warnings, 0 errors each). Unit-test project build alone
    confirms `PipelineTests`' `Substitute.For<IAdmissionRecordRepository/IPupilRepository/
    IClassLevelRepository>` DI wiring resolves with the new handler with no changes needed there.
  - **Contract**: additive. Hash `4b31521d833b48f37edeb801815c91f70fc9ff1c42c4ab5fbce04b040512ed29` →
    `37f8b4c2d19db1af9c5adc0be1f002840cb667416e9072dc4aa74fc6818a2b53`. Mechanically diffed the
    promoted document against `git show HEAD:contracts/openapi.json` (clean tree at dispatch start,
    so HEAD == the pre-change working tree): zero paths removed/added, only `get` added to the
    already-existing `/api/v1/admissions/{id}` path; zero schemas removed/added (`AdmissionRecordDto`
    reused, not duplicated); every schema present before has an IDENTICAL `required` array and every
    property's `type`/`format`/`$ref`/`enum`/`nullable` unchanged. (One false alarm during review: a
    Python `open()` without `encoding="utf-8"` mis-decoded the description text as CP1252 and
    printed em-dash mojibake — the on-disk bytes were correct UTF-8 throughout; re-checked with
    explicit `encoding="utf-8"` and raw-byte grep before relying on it.)
  - **Left undone**: nothing in scope. Out-of-scope items (populating `admission` on `GET
    /pupils/{id}`, steps 2–8, any `frontend/**` change) untouched, as directed.
  - No process still running: integration-test run was backgrounded mid-turn and its completion was
    awaited before any further DB-touching command; verified idle (`testhost`/`dotnet test`/`dotnet
    build` absent, only reusable MSBuild worker nodes and the VS Code build host) before the contract
    regeneration build and again before ending the turn.
