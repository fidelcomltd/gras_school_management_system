# TASK-0052 — Regenerate the frontend client for reg-number settings and pupils

Owner: frontend-dev
Depends on: TASK-0005c and TASK-0050 — **two** contract moves, consumed in ONE regeneration
Contract impact: none. Consumes a committed contract; must not change one.
Status: queued

> **Seventh run of one recurring card, and the first to consume two moves at once.** TASK-0029, 0033, 0037, 0040, 0044, 0047 were the same work
> against earlier contract moves. **[TASK-0047.md](TASK-0047.md) is the template — follow it
> exactly**; this card records only what differs. Every prior run wrote **zero** new lines in
> `client.ts`/`client-types.ts`; expect the same.

## Goal

`src/api/schema.d.ts` matches the committed contract, §4.4 check 2 green, and all **nine** new
operations — three reg-number settings, six pupils — callable through the existing typed wrapper.

## Contract delta

**None.** Already committed at sha256
`53820aa5feb23ef8d34b4962b250a74ef202faa3cbc3f066873a6a2c38f0da9b` (**45 paths**, was 38 when this
card was first written). Recompute it yourself and confirm against `CONTRACT.lock` before starting.

**This card consumes TWO contract moves in one pass** — TASK-0005c's reg-number settings and
TASK-0050's pupils. Deliberate: `npm run generate:api` rewrites the whole of `schema.d.ts` from the
committed document, so two sequential cards would regenerate the same file twice and the first
would be dead work.

**From TASK-0050** — four new paths and six schemas: `ListPupils`, `CreatePupil`, `GetPupil`,
`UpdatePupilBiographical`, `FindPupilDuplicates`, `ListAdmissionsQueue`; `PupilDto`,
`CursorPageOfPupilDto`, `CreatePupilCommand`, `UpdatePupilBiographicalCommand`, `PupilSex`,
`PupilStatus`. Prove §8 tolerance for **`PupilSex` and `PupilStatus`** as well as the two
reg-number enums. `PupilStatus` carries five members (`pending`/`active`/`transferred`/
`withdrawn`/`graduated`) of which only `pending` is currently reachable — do not build client logic
that assumes the others are absent.

**From TASK-0005c** — three operations, plus two additive groups on the existing `SettingsDto`:

| Operation | Route | Headers |
|---|---|---|
| `UpdateRegNumber` | `PATCH /api/v1/settings/reg-number` | `X-CSRF-Token` required, `Idempotency-Key` **accepted, not required** |
| `GetRegNumberPreview` | `GET /api/v1/settings/reg-number/preview` | none — read |
| `UpdateAbbreviation` | `PATCH /api/v1/settings/abbreviation` | `X-CSRF-Token` required, `Idempotency-Key` **accepted, not required** |

## Acceptance criteria

- [ ] `npm run generate:api`; `check:api-drift` → "No drift." Report `git diff --stat`.
- [ ] All **nine** operations reachable through the existing generic wrapper. Expect **zero** new
      lines in `client.ts`/`client-types.ts`; justify any you add.
- [ ] **Pupils: the omitted-required-path-parameter direction IS available here** — `GetPupil` and
      `UpdatePupilBiographical` both take `{id}`. Prove both reject an omitted `id`. This is the
      new instance the reg-number half cannot supply.
- [ ] `ListPupils`' `status` filter typechecks as optional, and `FindPupilDuplicates`' four query
      parameters (`surname`, `firstName`, `dob`, `contactPhone`) typecheck. An unknown query key
      is rejected on both.
- [ ] **`registrationNumber` is nullable on `PupilDto` and `null` is the norm, not an edge case** —
      every pupil this backend can currently create is `pending` and so carries `null`. Assert a
      `null` passes through unchanged. Do not coerce it to `""` and do not add a `?? ""`.
- [ ] **Negative typing direction — read this carefully, it differs from every prior run.**
      `Idempotency-Key` is **optional** on both `PATCH`es, so neither proves the
      omitted-required-header direction and neither should be used for it. Prove instead:
      an `idempotencyKey` **rejected** on `GetRegNumberPreview`, which declares no such header.
      For the omitted-required direction, reuse an existing operation that does require it
      (`CreateLevel`, `POST /arms`) rather than inventing a case — or state plainly in your report
      that this contract move offers no new instance of it. **Do not fabricate a proof.**
- [ ] Both `PATCH`es proven to accept `idempotencyKey` optionally — passing it typechecks, omitting
      it typechecks. That is the actual new typing shape here.
- [ ] `GetRegNumberPreview`'s query parameters (`separator`, `serialWidth` — unsaved, per 6.2.4)
      typecheck as optional, and an unknown query key is rejected.
- [ ] §8 enum tolerance proven for the new `RegNumberSerialReset` enum (`PerYear` / `Continuous`)
      and for `YearSource` (`AdmissionYear`) via `server.use(...)` overrides returning an
      unrecognised member. **Route templates written `:id`-style where any path parameter exists**
      — none of these three has one, so note that in the test comment for the next copier.
- [ ] **`issuedCount` is nullable and `null` is meaningful** — it means "no register exists to
      count", never `0` (TASK-0005c amendment 2). Add a test asserting a `null` passes through
      unchanged. Do **not** coerce it to `0` anywhere, and do not add a `?? 0`.
- [ ] Every `@ts-expect-error` verified load-bearing by TASK-0044's removal-probe method.
- [ ] `npm run verify` green with counts, `Skipped: 0`.
- [ ] **Run the secret scan** from the repo root:
      `gitleaks detect --source . --no-git --config backend/.gitleaks.toml --redact --no-banner`.
      `npm run verify` does not cover it.
- [ ] `npm run test:e2e` — re-run only if something e2e-relevant changed; if you skip it say why.
      Last known green: 8 passed / Skipped 0 (TASK-0043).

## Out of scope

- **Every screen.** No reg-number settings UI, no abbreviation dialogue, no preview widget, no
  TanStack Query hooks. Client seam only.
- **The 6.2.4 abbreviation dialogue's copy and the `CHANGE` token flow** — that is a screen card.
  Note for whoever writes it: it must **suppress the count sentence entirely when `issuedCount` is
  `null`** rather than printing `0`, per TASK-0005c amendment 2.
- `client.ts`/`client-types.ts` changes beyond a proven gap.
- Anything under `backend/**` or `contracts/**`.

## Notes for the implementing agent

Read `.agent/STATE.md` first (§4.1), then `frontend/src/api/README.md` and `CONVENTIONS.md`.

Keep new test files under `CONVENTIONS.md` §3's 180-line cap; split by route group if needed.

Watch for the inline-array/no-example trap TASK-0040 and TASK-0047 both hit: if any 200 response is
an inline array or lacks a top-level `example`, the contract-derived MSW default handler answers
empty. Fix it with an explicit `server.use(...)` override in that one test — never by touching
`openapi-handlers.ts` or loosening the assertion.

## Log
- 2026-09-09 created by orchestrator on TASK-0005c moving the contract to `e86e1b18…`, leaving
  §4.4 check 2 RED.
- 2026-09-09 **widened** on TASK-0050 closing and moving the contract again to `53820aa5…`
  (45 paths). One card, not two: `generate:api` rewrites the whole of `schema.d.ts` from the
  committed document, so a separate pupils-client card would have regenerated the same file a
  second time and made the first run's output dead. Pupil criteria added above; the pupils half
  also supplies the omitted-required-path-parameter proof the reg-number half structurally cannot.
