# TASK-0064 — Admissions queue screen: approve and decline

Owner: frontend-dev
Depends on: **TASK-0051** (the two endpoints — CLOSED 2026-09-15), TASK-0062 (`GET /admissions`,
`AdmissionRecordDto` — closed), TASK-0053 (arms screens, the nearest existing feature — closed).
Contract impact: **none** — this card CONSUMES `4b31521d833b…`. It regenerates the client from the
committed document; it must not change a byte of `contracts/openapi.json`.
Status: **done 2026-09-15** — closed by the orchestrator. Frontend gates green (typecheck, lint, 27 tests, check:api-drift); the backend gate that carried its unblocking route was 10/10. Archive: `decisions/2026-Q3.md` 2026-09-15 (TASK-0064).
Reads: `.agent/spec/frontend.md`, `frontend/CONVENTIONS.md` sections 4 (directory map), 6 (HTTP
       layer), 8 (design system), 11 (accessibility), 12 (testing);
       `frontend/src/api/README.md`;
       `frontend/src/features/arms/` — the nearest existing feature of this shape, in full
       (`arms-list-screen.tsx`, `arm-detail-screen.tsx`, `arms-routes.tsx`, `api.ts`,
       `arm-schema.ts`, and both test files);
       `frontend/src/api/client-admissions.test.ts` — the existing typed seam for this area;
       `jq '.paths."/api/v1/admissions/{id}/approve"' contracts/openapi.json` and the same for
       `/decline` and `/api/v1/admissions`;
       `jq '.components.schemas.ApproveAdmissionCommand' contracts/openapi.json`

## Goal

The office opens the admissions queue, sees every pending applicant with what is still missing from
each, and finishes the decision: approve one into a named arm — which issues the registration
number and turns the applicant into a real pupil — or decline it with a reason. Today the backend
can do both and no screen exists to reach either, so the feature is unshippable.

## Contract delta

**None.** Two operations already in the document become reachable:

- `ListAdmissionsQueue` — `GET /api/v1/admissions`, cursor-paginated. Rows carry
  `levelAppliedFor`, `dateApplicationReceived` and `missing`. Privilege `pupil.view`, SCHOOL-WIDE.
- `ApproveAdmission` — `POST /api/v1/admissions/{id}/approve`. **`Idempotency-Key` header is
  REQUIRED.** Privilege `pupil.admission.approve`. 409 when already decided.
- `DeclineAdmission` — `POST /api/v1/admissions/{id}/decline`, body `reason`. Same privilege.
- `ListArms` — `GET /api/v1/arms?sessionId=&levelId=` populates the arm selector.

## Acceptance criteria

- [x] `npm run generate:api` re-run; `schema.d.ts` carries `ApproveAdmission` and
      `DeclineAdmission`; **`npm run check:api-drift` is GREEN** (drift check 2 is RED today and
      this criterion is the whole reason this card is urgent). No hand edit to any generated file.
- [x] Typed client wrappers for both operations beside the existing ones in `src/api/`, each with a
      test in the shape of `client-admissions.test.ts`.
- [x] `/admissions` route behind `RequirePrivilege privilege="pupil.view"`, listing the queue:
      name, `levelAppliedFor`, `dateApplicationReceived`, and `missing` rendered as what a clerk
      must still collect — not a raw array.
- [x] Approve dialog: arm selector sourced from `GET /arms?sessionId={record.sessionId}
      &levelId={record.classAdmittedInto}` — never a free-text id; assessment remarks;
      head-of-school confirmation tick; optional head-of-school name.
- [x] **The tick gates the submit button.** `headOfSchoolConfirmed: false` is rejected by the
      server (TASK-0051), so the form must not let it be sent at all.
- [x] **`Idempotency-Key` is generated once per dialog opening, not per click** — a double-click or
      a retry after a dropped response must reuse the SAME key. Test it: two submits, one key, one
      request shape asserted.
- [x] On success the issued `registrationNumber` is shown back to the user — that number is the
      product of the whole flow and must be visible without navigating away.
- [x] Decline dialog: `reason` required, non-empty, and the row leaves the queue on success.
- [x] Every server error surfaces its message: 409 already-decided, 422 blocking conditions (no
      arm, no active term, missing assessment, unsigned declaration). Tested with at least the 409
      and one 422 — the blocking conditions are the ones a real clerk will actually hit.
- [x] Approve and decline actions are hidden — not merely disabled — without
      `pupil.admission.approve`, in the shape `arm-detail-screen.test.tsx` already uses.
- [x] `npm run lint`, `typecheck`, `test` and `check:api-drift` all green. Report counts.

## Out of scope

- The nine-step resumable admission flow, steps 2 to 8, and any section-editing screen.
- The pupil register, pupil detail, and the duplicate-candidates screen.
- Registration-number correction (TASK-0063, backend-side and not yet shipped).
- The printed admission slip.
- Any backend change whatsoever. If the screen needs a shape the contract lacks, **stop and say
  so** — do not shim it in the client (`rules/contract.md` §5).

## Notes for the implementing agent

**`frontend/src/features/arms/` is your template.** Same folder layout, same `api.ts` seam, same
`*-routes.tsx` with `RequirePrivilege`, same test style. Read it before writing anything.

**The generated request type will demand keys you think are optional.** `ApproveAdmissionCommand`
lists `assessmentResultRemarks` and `headOfSchoolName` in its `required` array while typing both
as nullable. Send them as explicit `null` when the user leaves them blank — omitting the key will
fail validation even though the value is optional in product terms. This is the single most likely
place this card goes wrong.

**`headOfSchoolName` left blank defaults server-side** from `settings.head_teacher_name`. Say so in
the field's help text rather than pre-filling it — the default is the backend's ruling to hold, and
pre-filling would silently freeze today's setting into the record.

**`missing` covers only what sections A and I can check today** (assessment result, declaration
signature). Steps 2 to 8 have no entity, so a gap there never appears in the array. Do not render
it as an exhaustive checklist — it is "what we can see is missing", and the copy should not
overclaim.

**Idempotency:** one key per dialog opening, held in component state, regenerated only when the
dialog is re-opened for a new decision. `crypto.randomUUID()`. Do not key off the pupil id.

## Log
- 2026-09-15 created by the orchestrator at TASK-0051's close, replacing the placeholder
  "regenerate the API client" row. Regeneration alone produces no product; `rules/contract.md` §3
  pairs regeneration and consumption in ONE frontend step, so the screen is in the same card.
- 2026-09-15 **implemented by frontend-dev, one AC blocked and reported rather than shimmed.**

  **Done, fully:** `npm run generate:api` re-run against `4b31521d833b…` (`ApproveAdmission`,
  `DeclineAdmission` now in `schema.d.ts`; `check:api-drift` GREEN — the AC this card called
  urgent). Typed client tests for both operations added to `client-admissions.test.ts` (list's own
  client test already existed from TASK-0052). `/admissions` route behind `RequirePrivilege
  privilege="pupil.view"`, queue list (name, `levelAppliedFor`, `dateApplicationReceived`,
  `missing` rendered as prose, captioned as partial per the card's own note — never an exhaustive
  checklist). Approve/Decline hidden — not disabled — without `pupil.admission.approve`, in
  `arm-detail-screen.test.tsx`'s shape. Decline dialog: `reason` required non-empty
  (`declineAdmissionSchema`), row leaves the queue on success, 409 and 422 both surfaced verbatim
  and tested. `useApproveAdmission`/`useDeclineAdmission` hooks in `features/admissions/api.ts`.

  **Blocked: the Approve dialog's arm selector.** AC reads "arm selector sourced from `GET
  /arms?sessionId={record.sessionId}&levelId={record.classAdmittedInto}`". Neither field is
  reachable from any read path this screen can call. Proof, from the contract itself, not
  inference: `CursorPageOfPupilDto.items[].example` (the literal example bound to
  `ListAdmissionsQueue`'s own response schema) shows `"admission": null` on a queue-shaped row —
  the row carries only `levelAppliedFor` (a display NAME) and `dateApplicationReceived`, never
  `sessionId` or the level's opaque id. `PupilDto.admission`'s own property description says the
  same thing in words: populated only by `CreatePupilHandler`'s response, "null on every other read
  path ... (list, single-get, duplicates)" — so `GET /pupils/{id}` doesn't help either (this is the
  SAME root cause STATE.md's existing drift line about `GET /pupils/{id}` already names, just hit
  from the queue side instead of the detail-view side). There is no `GET /admissions/{id}` — only
  `PATCH`, a write endpoint, gated `pupil.update` rather than `pupil.admission.approve`, and using
  it to "read" would both misuse the route and silently require a privilege the product doesn't
  otherwise ask of an approver.

  Two shims were considered and rejected rather than shipped: matching `levelAppliedFor` by name
  against `GET /levels` (guesses an identity relationship the contract never states, and is not
  even unique in general); assuming "the currently active session" (contradicts spec 6.5.11's own
  distinction between a record's target session and whatever happens to be active — the 409 for
  "no term is currently active" is explicitly a SEPARATE blocking condition from session choice).
  Both are exactly the guess-across-the-boundary `CLAUDE.md` §3 and this card's own out-of-scope
  note rule out.

  **What shipped instead:** `ApproveAdmissionDialog` opens, names the applicant, states the gap
  plainly, and closes — no request is ever made, no field is guessed. `useApproveAdmission` (the
  mutation hook: required `Idempotency-Key`, explicit `null` for `assessmentResultRemarks`/
  `headOfSchoolName` per the card's own most-likely-failure note) is implemented and tested at the
  client-wrapper level (`client-admissions.test.ts`) so the moment the contract gains the missing
  field(s), only the dialog's form needs writing — the transport and the nullable-required-field
  trap are already handled. **Acceptance criteria NOT met, and why:** the arm-selector AC itself;
  the tick-gates-submit AC (no submit exists); the idempotency-key-per-dialog-opening AC (nothing
  to key); the registration-number-shown-on-success AC; the 409/422 surfacing AC for APPROVE
  specifically (DECLINE's own 409/422 surfacing is fully implemented and tested).

  **Recommended next step (for the orchestrator):** a small additive contract delta — either
  `sessionId`/`classAdmittedInto` added to the `ListAdmissionsQueue` row, or a dedicated `GET
  /admissions/{id}` returning `AdmissionRecordDto` gated `pupil.view` — then a follow-up card
  swaps `ApproveAdmissionDialog`'s body for a real form; `useApproveAdmission` and its tests do not
  change.

  **Files changed** (frontend/ only; `contracts/openapi.json` untouched):
  - `frontend/src/api/schema.d.ts` — regenerated only, `npm run generate:api` against the
    committed contract.
  - `frontend/src/api/client-admissions.test.ts` — approve/decline client tests added (+96 lines).
  - `frontend/src/app/router/paths.ts`, `frontend/src/app/router/app-router.tsx` — `admissions`
    route wired in.
  - `frontend/src/features/admissions/` — NEW: `types.ts`, `api.ts`, `admission-schema.ts`,
    `admissions-queue-screen.tsx`, `admissions-routes.tsx`,
    `components/{admission-queue-list,decline-admission-dialog,approve-admission-dialog}.tsx`
    plus their colocated tests and a shared `components/test-fixtures.ts` (645 lines total, split
    to keep every file under CONVENTIONS.md §3's 180-line cap).

  **Gates run** (targeted, per `.agent/rules/gates.md` §1 — full gate is the orchestrator's):
  `npm run typecheck` clean. `npm run lint` clean (oxlint, zero warnings). `npm run test` over the
  touched paths: 4 files, 19 tests, all passing. `npm run check:api-drift`: GREEN, "schema.d.ts
  matches contracts/openapi.json. No drift."

  **Diff size:** ~744 hand-written lines (schema.d.ts's +300 is generated, excluded) against the
  card's own ~400-line guidance — flagged here rather than trimmed, since the overage is
  concentrated in test files split to respect the 180-line cap and in the blocked-dialog's own
  file-plus-test pair, both a direct consequence of the contract gap above, not scope creep.

  **For `## Known drift`:** one new line added to STATE.md pointing here — the same
  null-`admission` root cause STATE.md already tracks against `GET /pupils/{id}`'s detail view now
  also blocks this card's Approve arm-selector, which is a new and more immediate trigger than the
  existing line names.

- 2026-09-15 **Approve dialog finished, unblocked by TASK-0066's `GET /admissions/{id}`.** The
  coordinator shipped `GetAdmissionRecord` (returns `AdmissionRecordDto`, carrying the `sessionId`/
  `classAdmittedInto` ids the queue row cannot) and moved the contract to
  `37f8b4c2d19db1af9c5adc0be1f002840cb667416e9072dc4aa74fc6818a2b53` — verified additive by the
  coordinator (one `get` added to an existing path, zero schema changes). Client regenerated;
  `check:api-drift` GREEN against the new hash.

  Replaced the blocked stub with the real flow: `ApproveAdmissionDialog` now fetches the record
  first (four states: loading/error/unauthorized/loaded, mirroring `ArmDetailScreen`) and hands it
  to the new `ApproveAdmissionForm`, which sources the arm selector from `GET
  /arms?sessionId={record.sessionId}&levelId={record.classAdmittedInto}` — the record's own ids,
  never a name-match or an "active session" guess. New: `ArmSelect` (a genuinely new picker — arms'
  own `arm-pickers.tsx` selects a session/level to scope arms BY, never an arm itself, so this
  isn't a duplicate), `useAdmissionRecord` in `api.ts`, `approveAdmissionSchema` in
  `admission-schema.ts` (the tick refined to `true`, belt to the disabled-submit-button braces).

  Every previously-blocked AC now met: the tick disables Approve until checked (tested); one
  `Idempotency-Key` generated per mount via `useState(() => crypto.randomUUID())` — the form only
  mounts while its dialog is open, so it survives a re-render but not a close/reopen — proven with
  a test asserting a dropped-response retry sends the SAME header value twice, never a fresh one;
  the issued `registrationNumber` is shown in-dialog on success (`Done` button, no auto-navigate);
  409 (already decided) and one 422 (no active term) both surfaced verbatim and tested. Decline,
  the queue, privilege gating and `useApproveAdmission`'s explicit-null handling were NOT
  reworked, per the coordinator's instruction — only the dialog's body changed.

  **Files changed this pass** (frontend/ only): `frontend/src/api/schema.d.ts` (regenerated),
  `frontend/src/features/admissions/types.ts` (`AdmissionRecordDto`, `Record` key),
  `frontend/src/features/admissions/api.ts` (`useAdmissionRecord`),
  `frontend/src/features/admissions/admission-schema.ts` (`approveAdmissionSchema`),
  `frontend/src/features/admissions/components/approve-admission-dialog.tsx` (rewritten: fetch +
  four states, delegates to the form), NEW `approve-admission-form.tsx`, NEW `arm-select.tsx`, NEW
  `approve-admission-form.test.tsx`, `approve-admission-dialog.test.tsx` (rewritten for the real
  fetch states), `test-fixtures.ts` (+`admissionRecord()`), `admission-queue-list.test.tsx` (one
  test name corrected — the dialog opens the real form now, not a notice).

  **Gates:** `npm run typecheck` clean. `npm run lint` clean (one `jsx-a11y(prefer-tag-over-role)`
  finding fixed during the pass — success message moved from `<p role="status">` to `<output>`).
  `npm run test` over `src/features/admissions` + `src/api/client-admissions.test.ts`: **5 files,
  27 tests, all passing.** `npm run check:api-drift`: GREEN against
  `37f8b4c2d19db1af9c5adc0be1f002840cb667416e9072dc4aa74fc6818a2b53`.

  **Card is now fully implemented** — every acceptance criterion has a passing test. Closure
  itself (and the drift line this card's Log points at from STATE.md) is the orchestrator's call.
