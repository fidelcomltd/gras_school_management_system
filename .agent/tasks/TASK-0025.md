# TASK-0025 — Regenerate the stale API client

Owner: frontend-dev
Depends on: TASK-0003 (whose contract promotion left this half-done)
Contract impact: none — CONSUMES the committed contract, changes nothing in `contracts/**`
Status: done — closed 2026-09-05 (reconciled 2026-09-14)

## Goal

`npm run check:api-drift` passes, closing the last red step in `frontend-ci / quality-gates`.

## Why this exists

§4.3's handoff is: backend promotes the contract → frontend regenerates the client. TASK-0003
promoted (`3b518bd9…`) and closed without the second half ever running, and **nothing in the
ledger recorded it** until 2026-09-05. So `src/api/schema.d.ts` still declares
`/api/v1/reference/whoami` — removed as a BREAKING change — and none of the six `/api/v1/auth/*`
paths. The drift gate is behaving correctly; it is telling the truth.

This was TASK-0021's first acceptance criterion. It is split out because it is mechanical, it
unblocks CI now, and TASK-0021 is a much larger card that is not ready to run.

## Acceptance criteria

- [ ] `npm run generate:api` run against the **committed** `contracts/openapi.json`. Do not
      hand-edit `src/api/schema.d.ts` — it carries a GENERATED header and any manual edit is a
      §4.4 blocker.
- [ ] `npm run check:api-drift` green, output pasted.
- [ ] `npm run verify` green (typecheck, lint, test, build), output pasted. The removed `whoami`
      operation has **no references outside the generated schema** — the orchestrator verified
      this by grep — so nothing should break. If something does, that is a real finding: report
      it, do not paper over it.
- [ ] The diff is confined to `src/api/schema.d.ts`. If anything else needs to change, **stop and
      say so** rather than widening the card.

## Out of scope

- The cookie-auth seam, the sign-in screen, deleting `src/lib/auth/`, deleting
  `src/screens/scaffold-status/`. **All of that stays TASK-0021.** This card only regenerates
  types; it wires up nothing.
- `contracts/**` and `backend/**`.

## Notes for the implementing agent

Read `.agent/STATE.md` first and append last, and strike the 2026-09-05 stale-client drift entry
in your closing note so the orchestrator can remove it.

Generating types for the auth endpoints does NOT mean the app uses cookie auth yet —
`src/lib/auth/` is still the known-wrong bearer implementation, and it stays that way until
TASK-0021. Do not "helpfully" start on it.

Work in the FOREGROUND. Do not poll or end your turn with a "waiting for…" message.

## Log
- 2026-09-05 created by orchestrator, splitting TASK-0021's first criterion to unblock CI.
- 2026-09-05 done by frontend-dev. `npm run generate:api` run against committed
  `contracts/openapi.json` (sha `3b518bd9…`, unchanged — not regenerated, not touched).
  Diff confined to `frontend/src/api/schema.d.ts` (617 lines changed: adds the six
  `/api/v1/auth/*` paths and the `/reference/{ping,records,arms/{armId}/secure}` paths,
  drops `/reference/whoami`). No other file under `frontend/**` changed. `whoami` grep
  finding held: build/typecheck/lint/tests all green with no references to fix.
  `check:api-drift`: "schema.d.ts matches contracts/openapi.json. No drift." `npm run
  verify`: typecheck clean, lint 0 warnings, tests 15 files / 130 passed / 0 skipped,
  build succeeded (dist emitted). Did not touch `src/lib/auth/` or
  `src/screens/scaffold-status/` — out of scope per card.
  **For the orchestrator: strike the 2026-09-05 "committed client is STALE" live-trigger
  drift entry in STATE.md `## Known drift` — it is resolved by this card.** STATE.md was
  at 12132 bytes before my append; I added one short line (now 12221 bytes, under the
  12 KiB cap) and left the drift-section edit to you per this card's note.
