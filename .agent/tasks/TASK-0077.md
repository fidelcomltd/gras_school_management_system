# TASK-0077 — Result rules settings (§6.2.8)

Owner: backend-dev
Depends on: TASK-0076 (makes `IPublishedResultsGate` a real query, which this card's locks read)
Contract impact: **additive** — two operations on one new path, delta below, approved by the orchestrator 2026-09-17
Status: **done 2026-09-18** — closed on the orchestrator's full gate run
Reads: `product-specification/04-module-school-settings.md` §6.2.8 and §6.2.9 (grep the headings, read those two);
       `.agent/spec/backend.md`; `backend/AGENTS.md` §4; `.agent/rules/wire.md`;
       `.agent/rules/contract.md` §2; `.agent/rules/gates.md` §1.

## Goal

An administrator can read and change the result rules the computation engine obeys: tie-break,
position scope, level position, pass mark and minimum subjects for position. The annual and
promotion fields are stored too, so the annual and promotion cards find them. A fresh database has
§6.2.8's defaults.

## Contract delta (approved)

| Op | Path | Privilege | Success | Errors |
|---|---|---|---|---|
| GetResultRules | `GET /api/v1/settings/result-rules` | `settings.view` | 200 `ResultRulesDto` | — |
| UpdateResultRules | `PUT /api/v1/settings/result-rules` | `settings.resultrules.update` | 200 `ResultRulesDto` | 422 validation; 409 `result_rules_locked` naming the locked field |

`ResultRulesDto` = request body: `annualMethod` (`simple_average`/`weighted`), `weightFirst`,
`weightSecond`, `weightThird` (int|null), `primaryPositionScope` (`arm`/`level`),
`showLevelPosition`, `tieBreakRule` (`shared_position`/`exam_then_ca`/`exam_then_alphabetical`),
`passMark`, `promotionThreshold`, `requireCorePass`, `coreSubjectIds` (uuid[]),
`minSubjectsForPosition`. The PUT carries the reason field exactly as `PUT /settings/assessment` does
under §6.2.9, if that handler requires one. Match it, don't invent.

## Acceptance criteria

- [x] One persisted row, seeded to §6.2.8's defaults. `coreSubjectIds` is seeded **empty**: §6.2.8
      says "set by the administrator once subjects exist". The seeded subjects are named "English
      Language", not §8.4.9's "English Studies", so seeding by name would guess.
- [x] Every §6.2.8 validation, with the spec's own message where it gives one ("The three term
      weights total 90. They must total 100."). Weights required only when `weighted`.
      `coreSubjectIds` required non-empty on PUT when `requireCorePass` is true, and each id must
      be an existing active subject.
- [x] Locks: `primaryPositionScope` and `tieBreakRule` → 409 once any result set in the active
      session is Published (`IPublishedResultsGate`). `annualMethod` and weights → 409 once any
      Third Term set is Published (add the port method; real query). Changing a locked field to its
      current value is not a change and is not refused.
- [x] A `ConfigVersionGroup.ResultRules` member and config-version write on save, following grading
      and assessment exactly (§6.2.9).
- [x] Audit event with `before_json` / `after_json` populated.
- [x] Fresh-database test proves the seeded defaults. Add the table to `ApiTestFixture`'s reset.
- [x] A port the engine reads (`IResultRulesReader` or the repository), unit-testable with a fake.
- [x] Contract promoted additive; drift gate green; gates via `-IntegrationFilter`.

## Out of scope

"Editable until promotion is run" for `passMark`/`promotionThreshold`/`requireCorePass`/`coreSubjectIds`:
no promotion exists, so those fields stay editable (logged as drift, trigger TASK-0036).
`GET /settings/impact` (§6.2.12, already drift). Any frontend.

## Notes for the implementing agent

- **Nearest shape:** `SettingsEndpoints.cs:241` (`PUT /assessment`) and `Application/Settings/UpdateAssessmentHandler.cs`,
  including its `PublishedResultsReasonGate` use. Seed via `HasData` like `AssessmentStructureSeed.cs`.
- **Drift that bites:** "production seed path for `HasData` exercised by nothing" (2026-09-16). Your
  seed inherits that gap, so don't claim to close it. "Validation is a pipeline behaviour" (2.2):
  follow the existing validator shape.
- **Do not write to `.agent/**`.**
- Estimate ≈ 700 lines incl. tests.

## Log

- 2026-09-17 Carded by the orchestrator. Found missing while rewriting TASK-0071: the engine reads
  five of these fields and no result-rules entity existed.
- 2026-09-17 Dispatched to `backend-dev` with six pre-resolved facts (reason gate applies; `ConfigVersionGroup`
  member is code-only; privilege already exists; `needs_recompute` out of scope per live drift; reason gate vs
  hard locks are distinct; §6.2.13 does not amend §6.2.8) and the `LEDGER ACCOUNT` hand-back in place of
  `.agent/**` writes. The agent wrote nothing outside `backend/**`.
- 2026-09-18 Implemented. Three judgment calls surfaced in the hand-back. **Delta corrected by the
  orchestrator before promotion:** enums stay PascalCase (agent right, card wrong); error code renamed
  `result_rules_locked` → `settings.resultrules.locked` (card wrong); optimistic concurrency ADDED —
  `SchoolProfile.ResultRulesVersionNumber`, `versionNumber`/`expectedVersion`, `settings.resultrules.stale_version`,
  stale check first — folded into the single migration `20260917232247_AddResultRulesSettings`.
- 2026-09-18 **Promoted by the orchestrator:** `c5c4d6c6…` → `84b46211…`, 65 paths, 137 schemas. Additive
  verified by `jq` diff against a pre-promotion copy — zero removals, zero changed paths, zero new required,
  removed or retyped properties on any existing schema.
- 2026-09-18 **Closed.** First full gate: 1267/1267 Skipped 0, but Format (one import ordering, fixed by the
  agent) and Contract ledger drift (orchestrator bookkeeping) failed. Re-run: ALL 11 GATES PASSED,
  1267/1267, Skipped 0, coverage 80.60%, local container. Review verified: handler check order, the
  same-value-is-not-a-change lock comparison, the Third Term port as a real query, all eight seeded defaults
  against §6.2.8, `ApiTestFixture` reseeding `result_rules`, before/after audit metadata. New drift: the
  seeded defaults are not round-trippable through the PUT validator.
