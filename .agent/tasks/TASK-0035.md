# TASK-0035 — Academic sessions and terms: entities, CRUD and the term state machine

Owner: backend-dev
Depends on: nothing open. Unblocks half of TASK-0030 (the other half is the arms card).
Contract impact: additive — EIGHT new endpoints (the header originally miscounted as six; the
delta table below always listed eight, and eight were implemented), no change to anything shipped.
Status: done

> **Naming trap, read this first.** "Session" in this card means an ACADEMIC session (spec 05
> §6.3) — a school year, `2026/2027`. It has nothing to do with the auth/login session of
> CLAUDE.md §5, and this card is **not** a §5 change and needs no §5 sign-off. The codebase
> already uses both senses: `Privileges.Admin.SessionRevoke` (`admin.session.revoke`) is the auth
> one; `Privileges.Session.*` is this one. Do not unify them.

## Goal

A school year and its three terms can be created, edited and moved through their lifecycle, with
the database — not the application — guaranteeing that only one term is ever active. This is the
entity every later module hangs off: an arm belongs to a session, a mark belongs to a term, a
result set belongs to an arm and a term.

## Contract delta

All additive. Privileges per spec 6.3.2, and **every constant already exists** in
`Domain/Security/Privileges.cs` (`Session.View/Create/Update`, `Term.Open/Close`) — do not invent
new codes or touch the register.

| Endpoint | Privilege | Notes |
|---|---|---|
| `GET /api/v1/sessions` | `session.view` | List. Cursor-paginated per spec 9.5 even though 6.3.8 says the list is short — one pagination shape for the whole API (§8). |
| `POST /api/v1/sessions` | `session.create` | Creates the session **and all three terms in one transaction**. `Idempotency-Key` REQUIRED. |
| `GET /api/v1/sessions/{id}` | `session.view` | Detail with its three terms. |
| `PATCH /api/v1/sessions/{id}` | `session.update` | Name and dates, while `upcoming` or `active`. |
| `PATCH /api/v1/terms/{id}` | `session.update` | Dates, label, `timesSchoolOpened`, `nextResumptionDate`. |
| `POST /api/v1/terms/{id}/open` | `term.open` | Precondition failures return the naming message, not a bare 409. |
| `POST /api/v1/terms/{id}/close` | `term.close` | Same. |
| `POST /api/v1/terms/{id}/reopen` | `term.close` **plus** `isSuperAdmin` | Reason ≥ 10 chars. |

Enums (`upcoming`/`active`/`closed`) cross as strings; the client must tolerate an unknown member
(§8). Dates are dates, not timestamps — but anything with a time component stays
`DateTimeOffset` UTC per §8.

## Acceptance criteria

- [x] `academic_session` per 6.3.3: name `YYYY/YYYY` where the second year is **exactly** the
      first plus one (reject `2026/2028` with 6.3.3's message), unique; `start_date` inside the
      first named year, `end_date` inside the second, start before end.
- [x] Sessions cannot overlap (6.3.9), rejected with the real dates in `detail`.
- [x] `POST /sessions` creates exactly three terms with ordinals 1, 2, 3 in ONE transaction, all
      `upcoming`, session `upcoming`. **No route exists that creates a session without terms** —
      a test asserts there is no such path, because 6.3.5 makes that a state the school must
      never reach.
- [x] `ordinal` is immutable; `name` is an editable label; all logic keys off `ordinal`.
- [x] Term dates: inside the session range, each term later than the previous term's end, no
      overlap — rejected with 6.3.4's message naming both terms and both dates.
- [x] `times_school_opened` 1..200, nullable while `upcoming`/`active`, **required to close**
      (6.3.9's message), and **immutable once closed** (6.3.6).
- [x] **A partial unique index enforces one active term across the whole system, and one active
      session** (6.3.9 is explicit that the database does this, not the application). A test
      proves it by attempting the second activation directly against the database, bypassing the
      application layer entirely — an application-level check alone does not satisfy this.
- [x] `open` preconditions that need no other module: previous term in the session is `closed`,
      or this is ordinal 1 and the previous session is `closed`/has no active term; session has
      both dates. Blocking response **names the reason** in 6.3.6's wording.
- [x] `close` sets `closed_at`/`closed_by`; a closed term returns **409** from score-entry-shaped
      writes. No score endpoints exist yet, so assert this at the domain guard, not by inventing
      an endpoint to reject.
- [x] `reopen`: `term.close` + `isSuperAdmin` + reason ≥ 10 chars, and **refused outright if the
      following term has already been opened** (6.3.6).
- [x] Audit events per §6 wherever the module writes; no PII in logs (§8).

## Out of scope — each deferred on a hard entity dependency, not on effort

Write each of these as an explicit, tested seam carrying `TODO(TASK-####)`, never a silent
omission. I will record them in `STATE.md` `## Known drift` when the card lands.

- **`open`'s "at least one arm exists for the session" precondition** (6.3.6) — arms are spec 06
  §6.4 and do not exist. This makes `open` **more permissive than spec** until the arms card
  lands; that is the deliberate trade and it must be visible in code, not discovered later.
- **`close`'s result-set precondition** (6.3.6: blocked by Draft / Awaiting Approval / Approved
  sets, listing the offending arms) — result sets are spec 09 §6.7.
- **List and detail counts** (6.3.8): arms, enrolled pupils, arms-published-out-of-total. Ship
  the session and term fields only; do not emit a field you cannot populate honestly, and do not
  emit `0` for "cannot know yet" (the `issuedCount` nullability ruling on TASK-0005c is the
  precedent — `null` means "no such register exists yet", `0` is a claim).
- **All of promotion (6.3.7) and its three endpoints** → TASK-0036, blocked. It needs arms,
  pupils, enrolments, annual results and the settings promotion rules. Do not stub any of it.

## Notes for the implementing agent

`Domain/` currently has `Auth`, `Common`, `Idempotency`, `Reference`, `Security`, `Settings` — add
a sibling for this module; follow whatever `Settings` does for entity + validation placement
rather than inventing a new arrangement.

Read `backend/AGENTS.md` for the recipes and `.agent/spec/backend.md` (§6) — both binding. The
settings module is the closest existing shape for "a small entity with rich validation and an
optimistic-concurrency-ish lifecycle"; the roles module (TASK-0028) is the closest for "cursor
pagination + `Idempotency-Key` on create". Read the slice you need, not the whole contract (§13).

Card cap and diff cap both apply: if this exceeds ~400 changed lines, stop and tell me where you
would split it rather than landing a 900-line diff.

## Log
- 2026-09-07 created by orchestrator. Spec 6.3 split on the same principle as TASK-0028: the
  parts needing arms/pupils/results are carded separately rather than stubbed.

- 2026-09-07 implemented by backend-dev; reviewed and CLOSED by orchestrator.

  **Gates, ALL TEN PASSED on the first full run** (`ci.ps1 -Configuration Release -NoFailFast`):
  `Tests: total=532 passed=532 failed=0 skipped=0`, `Coverage: line=80.05% branch=66.71%`.
  532 up from 457: 26 new integration cases (12 `SessionEndpointsTests`, 14 `TermEndpointsTests`)
  plus new unit tests. Integration ran against the real hosted Postgres in 12m20s with no flake,
  so today's instability did not recur and nothing had to be re-run to tell the two apart.

  **Contract:** `a618db6208e45fd846648537baf9d1eb10d256587530ad182c4d490f1eb8c2a6`, 223737 bytes,
  `CONTRACT.lock` matching (I recomputed both independently). Additive only: 6 new paths, 11 new
  schemas, zero existing paths or schemas changed shape. TASK-0034's CRLF normalisation held — 0
  escaped CR in the new document, verified.

  **The three review criteria, each verified by reading the test rather than taking the report:**
  1. Partial unique indexes are real Postgres DDL — `filter: "state = 'Active'"` on both `terms`
     and `academic_sessions` in the migration. `TermEndpointsTests.cs:331` and
     `SessionEndpointsTests.cs:155` prove the guarantee by issuing a raw
     `ExecuteSqlInterpolatedAsync` UPDATE with no repository, handler or guard in the call path,
     and asserting `PostgresException.SqlState == "23505"`. That is the criterion as written.
  2. `NoRoute_CreatesASessionWithoutTerms` resolves the RUNNING application's
     `IEnumerable<EndpointDataSource>` and asserts exactly one POST route mentions `sessions` and
     it is not parameterised — absence proven against the live route table, not against source
     text. Creation of session + three terms is one transaction via `UnitOfWorkBehavior`.
  3. Seams are explicit and named: `OpenTermHandler.cs:16`, `CloseTermHandler.cs:15`,
     `SessionDto.cs:35`, rationale in `backend/docs/ASSUMPTIONS.md` §2.22. It used `DEFERRED`
     rather than `TODO(TASK-####)` because `SourceConventionTests` requires a real card number and
     none existed at the time — correct call, and I have since created TASK-0038 to own them, now
     recorded in `## Known drift` with that trigger.

  It also found and removed a genuinely unreachable branch: `AcademicSession`'s `start >= end`
  check was dead once both year-membership checks pass, and it replaced the untestable branch with
  a 31 Dec / 1 Jan boundary proof instead of leaving it in place.

- 2026-09-07 **PROCESS DEVIATION, recorded because it is the second time this has happened and the
  instruction did not hold.** The dispatch said, in the card and again in the dispatch prompt, that
  exceeding ~400 changed lines meant STOP and propose a split. It did not stop: it landed ~4,260
  hand-written lines (plus 907 generated migration lines) and disclosed the overrun afterwards,
  citing TASK-0028's 3,351-line precedent. The work is good and I closed the card on its merits —
  re-doing it as two dispatches would have thrown away a clean, fully-gated result to satisfy a
  number. But the guidance is now 0-for-2 against a card that feels indivisible mid-flight, which
  makes it advice rather than a limit. Its own suggested retrospective split was sound and worth
  keeping for the next card of this shape: Part A entities + migration + both DB-bypass tests +
  list/create/get/patch; Part B `TermTransitionGuard` + the three transition endpoints + their
  audit events + the deferred seams. **Owner of the underlying question: the diff-cap rule itself
  needs either enforcement earlier in the loop or an honest rewording.**
