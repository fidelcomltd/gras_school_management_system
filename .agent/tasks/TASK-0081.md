# TASK-0081 — Regenerate the typed client against `0ebca075…`

Owner: frontend-dev
Depends on: TASK-0071 (done; contract promoted)
Contract impact: **none** — this card consumes a contract already promoted. It must not change one byte of
`contracts/**`.
Status: **done 2026-09-18** — closed on the orchestrator's run: `check:api-drift` No drift; `npm run verify` 55 files / 381 tests / build clean
Reads: `frontend/src/api/README.md`; `.agent/rules/wire.md`; `.agent/rules/gates.md` §1 (frontend row);
       `.agent/spec/frontend.md`.

## Goal

`src/api/schema.d.ts` matches the committed contract, so frontend CI's drift gate goes green on PR #5 and the
future broadsheet / result screens have a typed compute operation to call.

## Contract delta

None. `jq` only what you need: the one new path is `/api/v1/result-sets/{resultSetId}/compute` (`POST`, no body,
optional `Idempotency-Key`, CSRF); the two new schemas are `ComputeResultSetResponse` and `ComputeResultSetFlagDto`.

## Acceptance criteria

- [x] `npm run generate:api`, then `npm run check:api-drift` reports `No drift`, exit 0. No hand edit
      to `schema.d.ts` — regeneration must reproduce it byte-for-byte.
- [x] `npm run typecheck` and `npm run lint` exit 0. Pin and lockfile untouched
      (`openapi-typescript@7.13.0`, exact).
- [x] **No new wrapper code.** A bodiless `POST` is already reachable through the existing helpers. If you find
      yourself needing to add a verb helper, STOP and report why.
- [x] Nothing consumes the compute types yet — no screens, no queries.

## Out of scope

Any screen, query or mutation hook calling compute (the broadsheet and result-set cards). Any `backend/**` or
`contracts/**` change.

## Notes for the implementing agent

- **Nearest shape:** TASK-0080 did exactly this regeneration one contract ago; one-commit precedent.
- `flags[].code` values (`no_examination_sat`, `absent_all_examinations`) are response-body data, not problem
  codes. Tolerate an unknown value per `rules/wire.md`.
- **Known drift in the test family you are touching:** the `@ts-expect-error` type-level tests in
  `src/api/` fire real unawaited requests. If you add one, prefer a non-executing form. → `drift/2026-Q3.md` 2026-09-18.
- Do NOT run `npm run verify` or `npm run test:e2e` (`gates.md` §1).
- **Do not write to `.agent/**`.** Emit a `LEDGER ACCOUNT` section in your report instead. Do not commit.
- Estimate ≈ 80 lines, essentially all generated.

## Log

- 2026-09-18 Carded and dispatched by the orchestrator after PR #5's frontend CI failed only on `check:api-drift`
  (typecheck, lint, test, build and e2e all green).
- 2026-09-18 **Implemented by frontend-dev**: `schema.d.ts` +178 lines, generated only; drift, typecheck, lint exit 0;
  no wrapper code (bodiless POST reachable via `apiPost`); pin and lockfile untouched.
- 2026-09-18 **Closed by the orchestrator** on its own run: `check:api-drift` No drift, exit 0; `npm run verify`
  55 files / 381 tests / build clean, exit 0.
