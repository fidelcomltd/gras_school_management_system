# TASK-0091 — Regenerate the typed client against `e7113c87…`

Owner: frontend-dev
Depends on: TASK-0090 (contract promoted 2026-09-21, commit `77390d9`)
Contract impact: **none**. This card consumes a contract that is already promoted, and must not change one byte of `contracts/**`.
Status: **done 2026-09-21**. Orchestrator: `No drift`; verify 55/381, build clean.
Reads: `frontend/src/api/README.md`; `.agent/rules/wire.md`; `.agent/rules/gates.md` §1 (frontend row); `.agent/spec/frontend.md`.

## Goal

Make `src/api/schema.d.ts` match the committed contract, so frontend CI stays green on TASK-0090's PR.

## Contract delta

None. `jq` only what you need:
- 2 new paths: `POST /api/v1/result-sets/{resultSetId}/approve` and `/return`; 3 new schemas.
- **ONE existing schema changed:** `ResultSetSummaryDto` gains required nullable `returnReason`. Response-only, but any test fixture or MSW
  mock that builds a `resultSet` block will now fail typecheck. Add `returnReason: null` to each, and nothing else. Report the count.

## Acceptance criteria

- [x] `npm run generate:api`, then `npm run check:api-drift` reports `No drift` with exit 0. Do not hand-edit `schema.d.ts`.
- [x] `npm run typecheck` and `npm run lint` exit 0. Leave the pin and the lockfile untouched (`openapi-typescript@7.13.0`).
- [x] **No new wrapper code.** `apiPost` already exists.
- [x] No screens, queries or hooks consume the new types yet.

## Out of scope

The approval screen, any `backend/**` or `contracts/**` change.

## Notes for the implementing agent

- **Nearest shape:** TASK-0089 did exactly this one contract ago; TASK-0085 is the precedent for fixtures rippling.
- Do NOT run `npm run verify` or e2e (`gates.md` §1). Never git push, pull, merge or rebase.
- **Do not write to `.agent/**`.** Put a `LEDGER ACCOUNT` in your report instead. Commit `schema.d.ts` (and any fixtures) on `task-0090` as
  `feat(client): regenerate against e7113c87` with `Refs: TASK-0091`.

## Log

- 2026-09-21 carded and dispatched by the orchestrator.
- 2026-09-21 frontend-dev `be208a0`, `schema.d.ts` +305/-9, zero fixtures needed. **Closed.**
