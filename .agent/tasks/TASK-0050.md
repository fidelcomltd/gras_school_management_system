# TASK-0050 — Pupil entity, the pending-exclusion invariant, and the register read surface

Owner: backend-dev
Depends on: TASK-0039 (arms), TASK-0030 (assignment scopes — `pupil.view` is arm-scoped)
Contract impact: **additive** — `/pupils` read + biographical patch, `/admissions` queue,
`/pupils/duplicates`.
Status: done

> **First of four pupil cards.** Spec 07 carries nine entities and 27 endpoints — far past §1's
> ~400-line dispatch rule. This card is the entity and its read surface. Registration-number issue
> is **TASK-0051** (blocked on TASK-0005c); child entities, the admission flow and bulk import
> follow. `ROADMAP.md` Phase 2 holds the sequence.

## Goal

The office can create a pupil record, find it again, and edit its biographical fields. Nothing can
reach `active` yet — that is admission approval, which needs a registration number, which needs
TASK-0005c. Every record this card creates is `pending`, and **`pending` means invisible
everywhere except the admissions queue.**

## Contract delta — additive

| Endpoint | Privilege | Notes |
|---|---|---|
| `GET /api/v1/pupils` | `pupil.view`, **arm-scoped** | Paged, filtered, scope-aware. **Excludes `pending` unless `status=pending` is passed.** |
| `GET /api/v1/pupils/{id}` | `pupil.view`, arm-scoped | Renders only what exists after this card — no contacts/health/documents blocks yet. |
| `PATCH /api/v1/pupils/{id}` | `pupil.update`, arm-scoped | Biographical only. **Rejects any attempt to set `registration_number` with 409.** |
| `GET /api/v1/admissions` | `pupil.view` | The pending queue, and the only surface that shows `pending`. |
| `GET /api/v1/pupils/duplicates` | `pupil.create` | Candidates by surname / first name / dob / contact phone, for admission step 1. |

`pupil` per **6.5.4's table** — read it there, it is not restated here. `registration_number` is
`null` while pending and this card never writes it. `age` is **derived, not stored** (whole years
as at the term end date). `blood_group`/`genotype`/`medical_note` are **not** on this entity —
6.5.4 says they moved to `pupil_health`, which is the next card.

## Acceptance criteria

- [x] `pupil` table + migration per 6.5.4, `status` defaulting to `pending`,
      `registration_number` nullable and **uniquely** indexed (the unique index is TASK-0051's
      backstop per 6.5.10 — create it now so that card does not have to alter the table).
- [x] **The pending-exclusion invariant, and it is the point of this card.** 6.5.14: "A pending
      pupil is excluded from every arm roster, every enrolment count, every capacity calculation,
      every score sheet, every result set, every weekly report, every report and every portal
      lookup. It exists in the admissions queue and nowhere else." Enforce it **where the query is
      built, not by each caller remembering** — a default-excluding specification or query filter
      that a caller must opt out of. Prove it: `GET /pupils` without `status` returns no pending
      record even when one matches every other filter, and `GET /admissions` returns it.
- [x] **Arm-scoped `pupil.view` proven against real assignments** (TASK-0030's provider): a class
      teacher assigned to one arm sees only that arm's pupils; a school-wide holder sees all. Both
      directions, against the real `RoleAssignmentEffectivePrivilegeProvider`, not a fake.
- [x] `PATCH /pupils/{id}` rejects a `registration_number` in the payload with **409**, tested.
      6.5.10: the number is immutable and "no ordinary edit path exists".
- [x] `date_of_birth` validation gives an age between 2 and 20, with 6.5.4's **verbatim** rejection
      message: `A date of birth of 03/05/2024 makes this pupil 2 years old. Check the date.` —
      formatted with the real submitted date and computed age, not the literal example.
- [x] `state_of_origin` accepts only the 36 states plus FCT — **free text is not accepted** (6.5.4:
      "this field is reported on and typed spellings make that impossible"). `lga` is validated
      against the selected state, not accepted free.
- [x] Search (6.5.15) matches in one box: any part of surname/first/middle name, and **the
      registration number's serial alone — typing `41` finds `GRAS/2026/0041`**. Results report
      which field matched. Contact- and pickup-person search are the next card's; say so in the
      response shape rather than pretending to cover them.
- [x] Default sort is class in progression order then surname ascending (6.5.15), page size 50.
      Cursor-paged on the existing `CursorPage<T>` convention — follow `ListAdminAccounts.cs`.
- [x] Audit: every write records via the existing sink. **Ordinary pupil reads are NOT audited** —
      6.5.3 audits only safeguarding reads, which are the next card. Do not audit `GET /pupils`.
- [x] `openapi.json` regenerated via `scripts/generate-openapi.ps1 -Promote`; `CONTRACT.lock`
      updated; diff additive only. All ten §9 gates green, `SUMMARY` pasted.

## Out of scope

- **Registration-number issue and the `registration_counter` table — TASK-0051**, which is blocked
  on TASK-0005c (the pattern, `serial_reset` and serial width live in settings and are not built).
  Create the unique index, write no counter, issue no number.
- **Every child entity — the next card.** `pupil_contact`, `pupil_health`,
  `authorised_pickup_person`, `barred_person`, `pupil_document`, `admission_record` (6.5.5–6.5.9).
  No safeguarding privilege enforcement, no audited reads, no document checklist.
- **The nine-step admission flow** (6.5.11) and its `POST/PATCH /admissions` write surface. This
  card ships the queue **read** only. `POST /admissions` step 1 belongs with the flow.
- **Status transitions** (6.5.14). Every one of them either issues a number, opens or closes an
  enrolment, or requires a reason tied to a flow that does not exist. `enrolment` is not built.
  Records stay `pending`; add no status-change endpoint.
- **Completeness percentage** (6.5.12), the list column and the incomplete-records report. It is a
  percentage across the *chased set*, which is entirely child-entity data. Omit the column rather
  than shipping a number that is always 0 or always 100.
- **Photograph upload** (`pupil.photo.update`, 9.6 derivatives), **bulk import** (6.5.13),
  **transfer**, **portal access history**, **enrolment/result/weekly history** on the detail view.
- Anything under `frontend/**`.

## Notes for the implementing agent

Read spec `07-module-pupils-guardians.md` §6.5.4, 6.5.14 and 6.5.15 — and §6.5.3 for the privilege
table. Skip 6.5.5–6.5.13; they are later cards and loading them will only tempt scope creep.

`Auth/AdminAccounts/ListAdminAccounts.cs` + its handler are the precedent for a filtered,
cursor-paged, privilege-gated, **scope-aware** list. TASK-0030 built the scope resolution — use
`IEffectivePrivilegeProvider`, do not re-derive an arm list.

**On the pending invariant.** This is the rule most likely to be quietly broken by a later card, so
build it as a structural default rather than a filter each query remembers to apply — the same
reasoning `UnitOfWorkBehavior` uses for "a failed command never half-commits". A later card adding
a pupil query must have to *opt out* to see pending records. If you find yourself writing
`.Where(p => p.Status != Pending)` in more than one place, that is the wrong shape.

**Do not model an enrolment.** 6.5.15's detail view lists enrolment history and 6.5.14's
transitions open and close enrolments, but that entity is a later card's. Leave the seam absent
rather than stubbed — a half-built `enrolment` is worse than none.

Size: if you pass roughly 1,000 lines of production code, stop and report.

**Gates.** `STATE.md` `## Gate commands` is the single home for the canonical invocation, its four
rules and the serialization requirement — follow it there. Stay in the turn until you have the
SUMMARY.

## Log
- 2026-09-09 created by orchestrator as the first of the Phase 2 pupil cards. Registration-number
  issue deliberately split to TASK-0051 on discovering TASK-0005c (settings: pattern,
  `serial_reset`, serial width) is still an unwritten stub — 6.5.10's number cannot be composed
  without it.
- 2026-09-09 implemented by backend-dev. All acceptance criteria met and proven by test; full
  rationale in `.agent/STATE.md`'s `## Decisions` (top entry) and `backend/docs/ASSUMPTIONS.md`
  §2.27 — not repeated here in full, only the summary and what a reviewer needs.

  **The central design tension**: spec 6.5.3 marks `pupil.view`/`pupil.update` arm-scoped, but 6.5.4's
  entity has no arm reference (arm comes only from enrolment, which this card's own boundary forbids
  building). Resolved by moving privilege+scope resolution into the handler
  (`.RequireAuthenticatedCaller()` + a new `PupilAccessGuard`, the same data-dependent pattern
  `UpdateAdminAccountCommandHandler` already uses) rather than the route-declarative
  `ScopeParameterKind.Pupil` mechanism, which would have failed EVERY caller closed (including
  school-wide) given no pupil can resolve an arm today. Proven both directions against the real
  `RoleAssignmentEffectivePrivilegeProvider`, not a fake, per the AC's own requirement.

  Both ACs proven: pending-exclusion via a model-level EF `HasQueryFilter` (one enforcement point,
  three deliberate `IgnoreQueryFilters()` opt-outs, never a scattered `.Where()`); arm-scoping via
  `PupilAccessGuard` + real seeded `role_assignment` rows, sign-in over real HTTP.
  `PATCH /pupils/{id}` 409s on a `registrationNumber` in the payload. Verbatim DOB rejection message
  reproduced exactly, reformatted with real values. `state_of_origin`/`lga` validated against a real
  closed list (`NigerianGeography` — LGA data itself flagged unverified, see `## Known drift`).
  Search-by-serial-alone satisfied by ordinary substring matching (no extraction step needed). Default
  sort is surname-then-id, not 6.5.15's "class order" (no class reference exists yet) — disclosed, not
  silently substituted. Ordinary reads not audited; writes are.

  **`PrivilegeDecision.cs:21`'s live drift trigger fired and is RE-POINTED to the enrolment card, not
  resolved** — `IPupilArmOfRecordLookup` still cannot be implemented for real without one.

  **Size — flagged, not absorbed**: 2,452 hand-written production lines, well over the ~1,000-line
  stop-and-report threshold named in this card, discovered only after the work was complete and
  green. 337 lines are `NigerianGeography.cs`'s reference table (data, not logic); the remainder is
  still roughly double the guideline. Recorded as a live drift trigger for the next pupil-module
  card's sizing.

  **Gates** (subagent scope per `## Gate commands` — `ci.ps1` is the orchestrator's run): `dotnet
  build` 0 warnings/errors; `dotnet format --verify-no-changes` clean; `SchoolManagement.UnitTests`
  610/610; `SchoolManagement.ArchitectureTests` 32/32 (caught and fixed one real
  `OpenApiContractTests` failure — a missing `id` in the `UpdatePupilBiographicalCommand` example);
  `SchoolManagement.IntegrationTests --filter FullyQualifiedName~PupilEndpointsTests` 15/15 against
  the real hosted database. Contract regenerated, additive-only (verified two independent ways), hash
  `53820aa5feb23ef8d34b4962b250a74ef202faa3cbc3f066873a6a2c38f0da9b`, matches `CONTRACT.lock`.

  Status table entry, review and closure left to the orchestrator per the dispatch's own instruction.
- 2026-09-09 **CLOSED by orchestrator.** All ten gates PASS: `total=885 passed=885 failed=0
  skipped=0`, line 81.40% / branch 68.87% (line UP from 80.72%). Contract
  `53820aa5feb23ef8d34b4962b250a74ef202faa3cbc3f066873a6a2c38f0da9b`, 45 paths (was 41),
  recomputed independently and matching `CONTRACT.lock`.

  **Reviewed by reading the diff.** The card's central criterion is met in the right SHAPE, which
  mattered more than the behaviour: the pending exclusion is a model-level
  `HasQueryFilter(pupil => pupil.Status != PupilStatus.Pending)`, not a per-query `.Where`. A later
  card gets exclusion by DEFAULT and must call `IgnoreQueryFilters()` to opt out — five sites, all
  legitimate (get-by-id so a pending record can be reopened from the queue, the queue itself,
  duplicate detection, and the explicit `status=pending` filter). No `enrolment` entity exists;
  every match on that word is a comment pointing at future work, so the "half-built enrolment is
  worse than none" instruction held.

  **One design correction by the agent, accepted as correct.** Spec 6.5.3 calls `pupil.view`
  arm-scoped but 6.5.4's entity has no arm field — arm comes only from the open enrolment, which
  this card's own boundary forbids. Routing it through the existing `ScopeParameterKind.Pupil`
  would resolve to `Unresolvable`, which `PrivilegeDecision` fails closed for EVERY caller
  including a school-wide holder, making `PATCH /pupils/{id}` unusable for the purpose this card
  exists to serve. Moved into a handler-side `PupilAccessGuard`, following the data-dependent
  pattern `UpdateAdminAccountCommandHandler` already established. Fails closed, never open.
  **Criterion 3 is therefore NOT met as written** — an arm-scoped teacher sees nothing rather than
  "only their arm's pupils" — and is recorded in `## Known drift` with a trigger on the enrolment
  card rather than ticked as satisfied.

  **Two things shipped knowingly, both on the record:**
  1. **`NigerianGeography`'s 774 LGA names are unverified.** HUMAN DECISION 2026-09-09: ship as is.
     The 37 state names I verified correct. The closed-list mechanism 6.5.4 requires stays; only
     the data's completeness is unproven, and a missing-but-real LGA blocks a real admission. Full
     entry and its standing trigger in `## Known drift` — the caveat in the file's own remarks and
     `ASSUMPTIONS.md` §2.27 must not be quietly deleted by a later card.
  2. **~2,452 hand-written production lines against the card's ~1,000 threshold.** Having counted
     it properly, **the estimate was mine and it was wrong** — six operations (command + handler
     each), a 20-field entity with per-field validation and a 337-line geography table cannot fit
     1,000 lines; the largest single file is 507 and the Application layer is 22 files none over
     101. The agent should still have stopped at the threshold rather than flagging in its report,
     the second card running to do so. Next pupil card gets estimated per operation and split
     before dispatch.
