# TASK-0087 — Regenerate the typed client against `42d8e3b5…`

Owner: frontend-dev
Depends on: TASK-0086 (contract promoted 2026-09-19, commit `7516d5c`)
Contract impact: **none**. This card consumes a contract that is already promoted, and must not change one byte of `contracts/**`.
Status: **done 2026-09-19**. The orchestrator ran `check:api-drift` (No drift) and `npm run verify` (55 files / 381 tests / build clean).
Reads: `frontend/src/api/README.md`; `.agent/rules/wire.md`; `.agent/rules/gates.md` §1 (frontend row); `.agent/spec/frontend.md`.

## Goal

Make `src/api/schema.d.ts` match the committed contract. That keeps frontend CI green on TASK-0086's PR, and gives the future
attendance, remarks and template screens typed operations.

## Contract delta

None. `jq` only what you need:
- 5 new paths: `GET`/`PUT /api/v1/arms/{armId}/attendance`, `/class-teacher-remarks` and `/head-teacher-remarks` (each with a `termId` query);
  `GET`/`POST /api/v1/remark-templates`; `DELETE /api/v1/remark-templates/{id}`.
- 13 new schemas, including a new string enum `RemarkKind` (`ClassTeacher` | `HeadTeacher`).
- `PATCH`/`PUT /api/v1/terms/{id}` changes only in its description text. **No existing schema changed**, so, unlike TASK-0085, no fixture
  or mock should need touching. If typecheck says otherwise, report it.

## Acceptance criteria

- [x] `npm run generate:api`, then `npm run check:api-drift` reports `No drift` with exit 0. Do not hand-edit `schema.d.ts`.
- [x] `npm run typecheck` and `npm run lint` exit 0. Leave the pin and the lockfile untouched (`openapi-typescript@7.13.0`).
- [x] **No new wrapper code.** `apiGet`, `apiPost`, `apiPut` and `apiDelete` already exist. If you need a new helper, STOP and report why.
- [x] No screens, queries or hooks consume the new types yet.

## Out of scope

Attendance, remark and template screens, and any `backend/**` or `contracts/**` change.

## Notes for the implementing agent

- **Nearest shape:** TASK-0085 did exactly this one contract ago.
- Do NOT run `npm run verify` or e2e (`gates.md` §1). Never git push, pull, merge or rebase.
- **Do not write to `.agent/**`.** Put a `LEDGER ACCOUNT` in your report instead. Do not commit.

## Log

- 2026-09-19 Carded and dispatched by the orchestrator, after TASK-0086's promotion.
- 2026-09-19 frontend-dev: `schema.d.ts` only (+1569/-202, diff realignment of repeated blocks); drift, typecheck, lint exit 0. No wrapper, no fixtures.
- 2026-09-19 Orchestrator: drift No drift; verify 55/381, build clean. Closed.
