# TASK-0016 — Make the gate suites fail fast and fail honestly

Owner: backend-dev, then frontend-dev (sequenced, disjoint files, no contract impact)
Depends on: none
Contract impact: none
Status: done

## Goal

A gate run tells the truth as cheaply as possible. Two defects today: the suites run expensive
checks before cheap ones, so an agent that broke a typecheck waits for a database round trip to
find out; and `ci.ps1`'s coverage gate recognises skipped tests but not **failed** ones, so it
has printed `PASS` on a run with failures. The second one has already cost this project real
money — TASK-0002 was reported done twice on runs whose integration suite silently skipped, each
false close costing a full re-dispatch of a Sonnet session.

## Contract delta

None. No file under `backend/src/**`, `frontend/src/**` or `contracts/**` changes.

## Acceptance criteria

- [x] `backend/scripts/ci.ps1` runs its gates cheapest-first and stops at the first failure:
      restore → format → build `-warnaserror` → unit tests → integration tests → coverage floor
      → vulnerable-package scan → gitleaks → OpenAPI drift. A `-NoFailFast` switch preserves the
      current run-everything behaviour for CI, which wants the full picture.
- [x] `ci.ps1` exits non-zero when the test run reports `Failed > 0`, proven by a test that feeds
      the parser a fixture summary line with failures and asserts a non-zero exit. This closes
      the `## Known drift` entry of 2026-08-27.
- [x] `ci.ps1` exits non-zero when `Skipped > 0` unless `-AllowSkipped` is passed explicitly, and
      prints which suite skipped. A skipped suite is not a passing suite (CLAUDE.md §13).
- [x] The final summary is a fixed block: one line per gate with its verdict, plus the test and
      coverage numbers. An agent can paste that block and nothing else and have satisfied §9.
- [x] `frontend/package.json` gains a `verify` ordering with the same property — typecheck → lint
      → test → build, stopping at the first failure — and `check:api-drift` stays separate.
- [x] Both suites' happy path and both failure modes are exercised by a test, not by inspection.

## Out of scope

- Changing any gate's threshold, including the coverage floor.
- Adding or removing a gate. `Docker.DotNet.Enhanced` verification stays with its drift entry.
- CI workflow restructuring beyond passing `-NoFailFast`.
- Anything under `backend/src/**` or `frontend/src/**`.

## Notes for the implementing agent

Read `backend/scripts/ci.ps1` first and find the existing summary-parsing code — the coverage
gate already parses `Skipped`, so the failure case is an extension of code that exists, not a new
parser. `.agent/drift/2026-Q3.md`, entry dated 2026-08-27, records exactly what it printed on the
run where it got this wrong; reproduce that run as your fixture.

The `-LiteralPath` fix noted in `decisions/2026-Q3.md` touched this same gate — read that entry
before editing, so the two fixes do not collide.

Do not treat this as licence to tidy `ci.ps1` generally. It is a working script with eight gates
wired into two CI workflows; the diff should be legible as "ordering, plus two exit conditions,
plus a summary block".

## Log
- 2026-09-04 created by orchestrator alongside the §13 context-budget work. Not yet dispatched.
- 2026-09-04 dispatched to backend-dev and frontend-dev in parallel (disjoint files: `backend/scripts/**` + `.github/workflows/backend-ci.yml` vs `frontend/package.json` + a frontend test; no contract impact, so §4.3 permits it despite the card header saying "sequenced").
- 2026-09-04 backend-dev returned; orchestrator review found three gaps and sent them back: Format
  still ran after Build, `.github/workflows/backend-ci.yml` was untouched (no `-NoFailFast`, no
  self-test step), and the trx glob had gained an unforced `-Recurse` that risked double-counting.
  All three fixed; `-Recurse` reverted with the evidence recorded in the comment.
- 2026-09-04 Second review found the skip line named the trx file (`HP_DESKTOP-..._14_12_56.trx`),
  not the suite, so criterion 3 was unmet — and the self-test passed anyway because the fixtures
  were named `IntegrationTests.trx`, a filename VSTest never produces. Suite name now derived from
  `TestRun.TestDefinitions.UnitTest[0].storage`; fixtures renamed to realistic
  `<user>_<machine>_<timestamp>` form; assertions added that the line names the project and not the
  file. A fixture that made a failing criterion look met is the same defect class this card closes.
- 2026-09-04 backend-dev found, while fixing that, a second bug the fixtures had also hidden:
  `ci.ps1 -AllowSkipped` never worked. `gate-summary.ps1` declared its own `param([switch]$AllowSkipped)`
  for script mode, and dot-sourcing a script binds its `param()` block in the CALLER's scope, so
  `. lib/gate-summary.ps1` silently reset `ci.ps1`'s own `-AllowSkipped` to `$false` on every run.
  Fixed by removing the `param()` block entirely and hand-parsing `$args` in script mode only.
- 2026-09-04 CLOSED. Orchestrator verified independently, not on report: full run with real Postgres
  `total=180 passed=180 failed=0 skipped=0`, line 75.57% / branch 57.60%, ten gates PASS, exit 0 --
  re-run after the `param()` removal since that touched the dot-source path. No-database run exits 1
  at the Coverage threshold gate with Vulnerable dependencies, Secret scan and OpenAPI drift never
  running (fail-fast and the skip exit condition proven in `ci.ps1` itself, not just the helper); the
  same run with `-AllowSkipped` exits 0 and names `schoolmanagement.integrationtests`. Self-test 19
  assertions green. Frontend: 129/129 tests, 0 skipped, build and `check:api-drift` green,
  `package.json` untouched (`verify` already held the property, so the deliverable was the
  enforcement test). Deviations recorded in `## Known drift`: the Generate-OpenAPI gate kept its
  position before the test gates (the card's order omitted it; ArchitectureTests read the artefact),
  and Format measured SLOWER than Build (~30s vs 4.2s incremental / 19.0s cold), so the card's
  cheapest-first premise is contradicted by the order it specifies.
