# TASK-0001 — Re-audit the backend and frontend scaffolds against §6 and §7

Owner: reviewer (read-only — writes nothing)
Depends on: —
Contract impact: none
Status: done

## Goal

`.agent/AUDIT.md` was written on 2026-07-27 against two empty directories and says so. Both
scaffolds have since landed with no task card owning them (`STATE.md` Known drift, entries
2026-08-08 and 2026-08-26). Before any feature work is dispatched on top of them, produce a
real, prioritised list of deviations from CLAUDE.md §6 (backend) and §7 (frontend), so that
"the scaffold is accepted" is an evidence-based statement rather than an assumption. A wrong
`.csproj` property or `tsconfig` flag is cheapest to fix now and most expensive after twenty
features sit on it.

## Contract delta

None. This card produces a report and changes no code.

## Acceptance criteria

- [x] Every rule in §6 is checked against `backend/` and recorded as pass, fail or N/A with the
      file and line that evidences it. Give particular attention to: `TreatWarningsAsErrors`,
      `Nullable`, the `Api → Application → Domain` dependency direction, `TypedResults` usage,
      RFC 9457 ProblemDetails shape, `CancellationToken` propagation, absence of `.Result` /
      `.Wait()` / `async void`, structured logging with message templates rather than
      interpolation, and options validated with `ValidateOnStart()`.
- [x] Every rule in §7 is checked against `frontend/` the same way. Particular attention to:
      `strict` and `noUncheckedIndexedAccess`, absence of `any` and `@ts-ignore`, no raw
      axios/fetch to the API host outside `src/lib/http/`, TanStack Query defaults and
      `staleTime`, error boundaries per route, and the a11y rules oxlint is configured with.
- [x] Findings are ranked blocker / should-fix / nice-to-have, each naming the §6 or §7 rule it
      violates and the file it lives in.
- [x] Confirm whether `backend/src/**/obj/` and `frontend/dist/` are correctly gitignored — both
      currently appear in the working tree.
- [x] Confirm the two scaffolds' claimed gate results reproduce: `./backend/scripts/ci.ps1` and
      `cd frontend && npm run verify`. Paste the output.
- [x] `.agent/AUDIT.md` is **not** written by you — return the report; the orchestrator writes it.

## Out of scope

- Fixing anything. This card only reports.
- Auditing against `product-specification/` — the scaffolds predate it and implement no product
  behaviour, so a spec audit would find only "not built yet".
- The auth mechanism question. It is Open question 8 and belongs to TASK-0003.

## Notes for the implementing agent

Read `.agent/STATE.md` first (§4.1). Then `frontend/HANDOFF.md`, `backend/AGENTS.md`,
`backend/CONTRIBUTING.md` and `backend/docs/ASSUMPTIONS.md` — the two scaffolds documented
their own decisions and several apparent deviations are deliberate and already justified there.
Do not report a documented, justified deviation as a fresh finding; report it as accepted drift
so the orchestrator can decide whether to keep accepting it.

`frontend/src/screens/scaffold-status/` is a demo, not a feature, and is already slated for
deletion. Do not audit it as product code.

## Log
- 2026-08-26 created
- 2026-08-27 dispatched by orchestrator as TWO parallel read-only audits — backend §6 and
  frontend §7 — because the halves are disjoint, neither writes, and they use different
  toolchains so there is no `obj/bin` or lockfile contention. Note for the record: CLAUDE.md §11
  lists `reviewer` as an optional subagent and it is NOT configured in this session's roster, so
  both halves run as `general-purpose` under an explicit write-nothing brief. Backend gate
  evidence supplied from the orchestrator's own 2026-08-27 `ci.ps1` run (ALL GATES PASSED,
  integration suite 30/30, coverage 77.18%/58.61%) rather than re-run; the frontend half runs
  `npm run verify` for real, which is the half not yet executed this session.
- 2026-08-27 **CLOSED by orchestrator.** Deliverable is `.agent/AUDIT.md`, rewritten from scratch
  and superseding the 2026-07-27 version. Both halves returned rule-by-rule verdicts with
  `file:line` evidence; the orchestrator independently re-verified every finding ranked blocker or
  should-fix before carding it — the ProblemDetails schema and its invalid example, the generated
  client's five-member type, the absence of `Idempotency-Key`, the two README passages prescribing a
  client bypass, the bare `oxlint` script, the `httpClient` barrel export, the unwrapped catch-all
  route, `obj/` not being tracked, and the four zero-coverage OpenApi classes. Findings ranked and
  owned: **B1 -> TASK-0012**, **B2 -> TASK-0013**, **S4-S8 -> TASK-0014**, **S1-S3 -> TASK-0015**;
  nice-to-haves recorded in AUDIT.md and deliberately not carded.
  Both auditors honoured the write-nothing brief — verified: the only modified files were the
  orchestrator's own concurrent `.agent/` edits.
  Gate criterion met from two sources: the backend suite from the orchestrator's 2026-08-27 live run
  (ALL GATES PASSED, integration 30/30), and `npm run verify` executed for real during the audit
  (exit 0, 124 tests, zero lint diagnostics, build clean).
  Three ledger claims corrected as factually wrong rather than carried forward: `obj/` tracked (it
  is not), `frontend/dist/` tracked (it is not), and `## Contract` listing the health endpoints in
  the document surface (they are `.ExcludeFromDescription()`). The coverage question I asked was
  answered with arithmetic, not a guess: 194 lines in four OpenApi classes, 1696/1946 = 87.15%
  against the 87.00% recorded — attribution lost, checking retained, no card.
