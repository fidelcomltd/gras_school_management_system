# TASK-0026 — Make analyzer and test-runner behaviour independent of the installed SDK

Owner: backend-dev
Depends on: TASK-0024 (which pinned `rollForward` and proved it insufficient)
Contract impact: none expected — verify, and STOP if the hash moves
Status: queued

## Goal

The backend produces the same gate result on any machine, because what the gates DO is determined
by committed, pinned versions rather than by whichever SDK happens to be installed.

## Why TASK-0024 was not enough

TASK-0024 set `rollForward: latestFeature` → `latestPatch` and the next CI run still failed two
SDK-coupled behaviours that pass locally on 10.0.100:

- `CA1873` at `RequestLoggingBehavior.cs(84,21)` — **inside** the `IsEnabled` guard TASK-0024 added.
- `dotnet test` still reports `The argument <dll> is invalid. Please use the /help option`.

The pin was verified by SDK-resolution semantics, not by reproduction, and that was too weak.
`AnalysisLevel` and the test bridge can both change **within** the 10.0.1xx patch band, so no
`rollForward` value closes this.

**The actual root cause is in the repo, not in `global.json`:** `Directory.Build.props:19` sets
`<AnalysisLevel>latest-All</AnalysisLevel>`. `latest` means "whatever this SDK ships". The build's
rule set is therefore a property of the machine, which is exactly what a lockfile exists to prevent.

## Acceptance criteria

- [x] **Fix CA1873 version-independently** by hoisting inside the existing guard:
      `if (logger.IsEnabled(LogLevel.Information)) { var elapsedMs = (long)Stopwatch…; BehaviorLog.RequestCancelled(logger, requestName, elapsedMs); }`
      This keeps the skip semantics AND matches the success path four lines up, which passes
      precisely because it hands over a local. The guard alone does not satisfy the analyzer:
      `BehaviorLog.RequestCancelled` is a static `[LoggerMessage]` method taking `logger` as a
      PARAMETER, so the analyzer cannot tie `logger.IsEnabled(...)` to it.
- [x] **Analyzer behaviour pinned to a committed version, not the SDK.** Reference
      `Microsoft.CodeAnalysis.NetAnalyzers` at an exact version through Central Package Management
      and replace `latest-All` with a pinned equivalent. State what you chose and confirm the
      SDK's built-in analyzers no longer decide the rule set. **Do not weaken the rule set to pass**
      — `-All` severity coverage must be preserved; only its VERSION becomes fixed.
- [x] **The test-runner bridge is DECLARED, not inherited.** `dotnet test` is passed VSTest-style
      arguments (`--settings coverlet.runsettings`, `--logger trx`, `--results-directory`) while
      `xunit.v3` is Microsoft.Testing.Platform-native and `xunit.runner.visualstudio` provides the
      VSTest bridge. Which path runs currently depends on the SDK. Make it explicit
      (e.g. `TestingPlatformDotnetTestSupport`) so the invocation and the arguments agree on ANY
      SDK. Say which mode you chose and why. **`.trx` output and coverlet coverage must both keep
      working** — the §9 gates and CI's not-skipped assert depend on the `.trx`.
- [x] `./backend/scripts/ci.ps1` green locally, `SUMMARY` pasted, `Skipped: 0`.
- [x] Contract hash unchanged. If it moves, **STOP and report the diff** — do not promote.

## The verification problem — read this before claiming done

**None of these three failures reproduces on this machine.** Local passes all ten gates today,
including the code CI rejects. So "it passes locally" is NOT evidence, and TASK-0024 already
closed on exactly that reasoning and was wrong.

- [x] For each of the three fixes, state explicitly what you verified and what you could not.
- [x] Prefer a change whose correctness is **argued from the mechanism** over one that merely
      happens to pass here. A fix that only works because this SDK is lenient is not a fix.
- [x] If you can construct any local check that would have caught the CI failure — a targeted
      analyzer invocation, a second SDK, an MSBuild property dump — do it and show it.

## Out of scope

- `.github/workflows/**` (orchestrator's). A `dotnet --version` step already exists; its output is
  still outstanding and may change the diagnosis. If it turns out CI is NOT on 10.0.100, report it.
- Suppressing CA1873, lowering `AnalysisLevel` coverage, or removing `warnaserror`.
- `frontend/**`, `contracts/**`.
- TASK-0019's rate-limiter ordering fix.

## Notes for the implementing agent

Read `.agent/STATE.md` first and append last. It is at ~12.2 KB against a 12 KB cap — archive to
`decisions/` or `drift/` and leave an index line rather than appending prose.

`local-env.ps1` here is stale (drift): set `POSTGRES_TEST_CONNECTION` from `$HOME/.gras/pg-test.txt`
and call `./scripts/ci.ps1 -Configuration Release -NoFailFast` directly.

Work in the FOREGROUND. Do not background, poll, or end your turn with a "waiting for…" message —
a dispatch in this session failed five times that way. Your final message must be the report.

## Log
- 2026-09-05 created by orchestrator after TASK-0024's pin proved insufficient on CI.
- 2026-09-05 closed by backend-dev. Summary below; full mechanism, verified/unverified breakdown,
  and a red-herring XML bug found along the way are in `.agent/decisions/2026-Q3.md`
  ("TASK-0026: analyzer/test-bridge behaviour made independent of the installed SDK").

  **Fix 1 — CA1873.** Reproduced locally by forcing `-p:AnalysisLevel=10.0-All` on this same SDK
  (no second SDK needed): this surfaced the card's cited site
  (`RequestLoggingBehavior.cs(84,21)`, `RequestCancelled`) AND three more the card didn't
  mention — `RequestLoggingBehavior.cs(56-60)` (`RequestFailed`), and, once every project built at
  the real pin, `GlobalExceptionHandler.cs` ×3 and `EndpointModuleExtensions.cs` ×1. All six fixed
  by guarding with `logger.IsEnabled(...)` and hoisting the computed argument to a local INSIDE the
  guard (the pattern the success path already used) — confirmed clean at the forced level, then
  again under the real committed pin and under `ci.ps1`.

  **Fix 2 — analyzer version pin.** `AnalysisLevel` → `10.0-All` (Directory.Build.props) +
  `Microsoft.CodeAnalysis.NetAnalyzers` `PackageVersion` `10.0.100` (Directory.Packages.props). A
  bare `PackageReference` to that package does NOT supersede the SDK-bundled analyzer on this SDK —
  verified via `dotnet build -v:diag`, grepping the literal `csc.exe /analyzer:` switches: only SDK
  paths appeared. New `backend/Directory.Build.targets` removes the SDK's implicit `Analyzer` items
  by metadata match and re-adds the pinned package's DLLs in a `Target BeforeTargets="CoreCompile"`
  (a static ItemGroup can't batch-Remove by metadata — MSB4191). Re-verified via the same grep:
  SDK paths gone, pinned paths present, full solution builds 0 warnings/0 errors.

  **Fix 3 — test bridge.** `TestingPlatformDotnetTestSupport=false` declared explicitly in
  `backend/tests/Directory.Build.props` (its default lives in `Microsoft.Testing.Platform.MSBuild`,
  a transitive, unpinned-floor dependency of xunit.v3). Verified: `dotnet test` with `ci.ps1`'s
  exact arguments produces classic VSTest output (`.trx` + coverlet) both before and after — this
  machine already defaulted to `false`, so CI's failure with a newer resolved package version could
  not be reproduced, only argued from the mechanism. Attempted, then reverted: pinning
  `Microsoft.Testing.Platform.MSBuild`'s own version via CPM broke restore (`NETSDK1013`, empty
  `TargetFramework` — its `buildMultiTargeting` assets import unconditionally once centrally
  pinned). Recorded as new drift, not closed.

  **Verified / unverified, per fix:** see the decision entry — short version: CA1873 fully
  reproduced-fixed-reproved locally; the analyzer-version-pin mechanism verified by direct
  `/analyzer:` inspection but NOT by a second SDK (none available); the test-bridge default
  verified as `false` on this machine but CI's flip to a different default not reproduced.

  **Gates:** `./backend/scripts/ci.ps1 -Configuration Release -NoFailFast` — ALL GATES PASSED,
  `Tests: total=245 passed=245 failed=0 skipped=0`, `Coverage: line=77.85% branch=58.80%`.
  Contract hash unchanged: `3b518bd951b386b270ca35b1398c515a3c9bad3fe0eba4f6c89382ede0565185`.

  **Left undone / disclosed:** `Microsoft.Testing.Platform.MSBuild`'s version stays an unpinned
  floor (see above); no NuGet lock file anywhere in `backend/` (found along the way, out of scope —
  new drift entry); `.agent/STATE.md` is 12540 bytes against the 12 KB cap after archiving this
  card's own content as tightly as reasonable — closing the remaining ~250-byte gap would require
  compressing OTHER agents' standing-lesson entries, deliberately not done here.
