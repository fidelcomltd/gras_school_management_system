# TASK-0082 — Regenerate the typed client against `8e3087d9…`

Owner: frontend-dev
Depends on: TASK-0072 (contract promoted 2026-09-18)
Contract impact: **none**. This card only consumes a contract that is already promoted, and must not change one
byte of `contracts/**`.
Status: **done 2026-09-18**. Closed on the orchestrator's own run: `check:api-drift` No drift; `npm run verify` 55 files / 381 tests / build clean.
Reads: `frontend/src/api/README.md`; `.agent/rules/wire.md`; `.agent/rules/gates.md` §1 (frontend row);
       `.agent/spec/frontend.md`.

## Goal

Make `src/api/schema.d.ts` match the committed contract. That keeps frontend CI green on TASK-0072's PR, and gives
the future settings screens typed rating-scale, development-domain and trait operations.

## Contract delta

None. `jq` only what you need:
- New paths:
  - `PUT /api/v1/settings/rating-scales`
  - `PUT /api/v1/settings/development-domains`
  - `PUT /api/v1/settings/traits`
  - Each takes CSRF and an optional `Idempotency-Key`.
- 20 new schemas, the `Update*Command`s, `*Input`s and `*Dto`s among them.
- `SettingsDto` gains the always-present properties `ratingScales`, `developmentDomains` and `traits`.

## Acceptance criteria

- [x] `npm run generate:api`, then `npm run check:api-drift` reports `No drift` with exit 0. Do not hand-edit
      `schema.d.ts`: regeneration must reproduce it byte-for-byte.
- [x] `npm run typecheck` and `npm run lint` exit 0. Leave the pin and the lockfile untouched
      (`openapi-typescript@7.13.0`, exact).
- [x] **No new wrapper code.** `apiPut` already exists. If you find you need a new helper, STOP and report why.
- [x] If a test fixture or mock builds a whole `SettingsDto`, typecheck will demand the three new properties. Add
      them there with minimal valid values, and list every such file in your report. *(None existed: only a type re-export.)*
- [x] No screens, queries or hooks consume the new types yet.

## Out of scope

Any settings screen for scales, domains or traits. Any `backend/**` or `contracts/**` change.

## Notes for the implementing agent

- **Nearest shape:** TASK-0081 did exactly this one contract ago.
- `status` (`active`/`archived`) and `TraitDomain` are enums. Tolerate unknown values per `rules/wire.md`.
- Do NOT run `npm run verify` or `npm run test:e2e` (`gates.md` §1).
- **Do not write to `.agent/**`.** Put a `LEDGER ACCOUNT` section in your report instead. Do not commit.
- Estimate: a few hundred lines, essentially all generated.

## Log

- 2026-09-18 Carded and dispatched by the orchestrator at TASK-0072's promotion, so the regeneration rides in the same PR
  (the PR #5 lesson in `decisions/2026-Q3.md`, TASK-0081).
- 2026-09-18 **Implemented by frontend-dev**: `schema.d.ts` +1386/-55, generated only; drift, typecheck and lint exit 0; no wrapper code; no fixtures touched; pin and lockfile untouched.
- 2026-09-18 **Closed by the orchestrator** on its own run: `check:api-drift` No drift, exit 0; `npm run verify` 55 files / 381 tests / build clean, exit 0.
