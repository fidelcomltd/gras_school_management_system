# TASK-0070 — Subjects, level mappings and per-arm exceptions

Owner: backend-dev
Depends on: TASK-0038/0039 (closed) — levels, sections and arms exist. TASK-0035 (closed) — terms.
Contract impact: **additive.** New paths under `/subjects`. Propose the delta and get it approved
BEFORE you implement — `.agent/rules/contract.md`.
Status: **queued 2026-09-16** — behind TASK-0069, which is dispatched first. Independent of it in
substance; sequenced only because both write `backend/**` and two concurrent gate runs on one Neon
endpoint have already cost this project a run (`decisions/2026-Q3.md`, 2026-09-15 and 2026-09-16).
Reads: `.agent/rules/contract.md`, `.agent/rules/wire.md`, `.agent/rules/gates.md` (sections 0, 1);
       `product-specification/08-module-subjects.md` §6.6 in full — it is short;
       `product-specification/13-result-computation-rules.md` §8.1, the "Subjects in effect" row,
         which is the one consumer that matters and fixes the resolution semantics.

## Goal

A subject exists, is mapped to class levels for a term, and an arm may include or exclude one by
exception. The computation engine resolves "the subjects in effect for this arm this term" through
exactly one query, and that resolution is this card's real deliverable.

## The resolution rule, stated once

§8.1 defines it: **level mappings for the term, plus the arm's include exceptions, minus its
exclude exceptions.** Build that as a single resolver the engine calls. Two arms of one level may
legitimately end up with different subject sets — that is why §8.4.7 ranks level position on
average rather than on total, and the engine depends on this card getting it right.

## Acceptance criteria

- [ ] `subject`, `subject_mapping` and `subject_mapping_exception` persist per §6.6.2-6.6.4.
- [ ] Seeds load the school's real lists: **14 nursery subjects, 19 primary** (ROADMAP Phase 2).
- [ ] One resolver returns the subjects in effect for (arm, term) implementing mappings + includes
      - excludes. It is the only place that rule is expressed; no caller reimplements it.
- [ ] An include exception and an exclude exception on the same (arm, subject, term) is rejected,
      not silently resolved by ordering.
- [ ] §6.6.6 — unmapping a subject after marks exist behaves exactly as specified. Read it before
      designing the delete path; it is the one place this card can lose data.
- [ ] §6.6.8 error and edge cases covered, each with the spec's own message.
- [ ] List views per §6.6.7; endpoints per §6.6.9.
- [ ] Authorization follows the seeded roles: School Administrator holds `subject.*` except delete.
      Do not invent a privilege; if §6.6.9 needs one that is not in `01-actors-and-privileges.md`
      §4, **stop and raise it** rather than adding it.
- [ ] Contract delta approved before implementation; `contracts/openapi.json` regenerated; drift
      gate green.
- [ ] Gates green via `./backend/scripts/ci.ps1 -IntegrationFilter` scoped to your new classes.
      Never the full gate — `.agent/rules/gates.md` §1.

## Out of scope

Frontend screens. Score entry, which needs the resolver but is TASK-0071's problem.

## Log

- 2026-09-16 Carded by the orchestrator alongside TASK-0069 and TASK-0071 when the human
  authorised the module roll-out.
