# TASK-0085 — Regenerate the typed client against `9c2f8d55…`

Owner: frontend-dev
Depends on: TASK-0083 (contract promoted 2026-09-19)
Contract impact: **none**. This card consumes a contract that is already promoted, and must not change one byte of
`contracts/**`.
Status: **done 2026-09-19**. The orchestrator ran `check:api-drift` (No drift) and `npm run verify` (55 files / 381 tests / build clean).
Reads: `frontend/src/api/README.md`; `.agent/rules/wire.md`; `.agent/rules/gates.md` §1 (frontend row);
       `.agent/spec/frontend.md`.

## Goal

Make `src/api/schema.d.ts` match the committed contract. That keeps frontend CI green on TASK-0083's PR, and gives the
future rating-entry screens typed operations.

## Contract delta

None. `jq` only what you need:
- New paths: `GET`/`PUT /api/v1/arms/{armId}/trait-ratings` and `/api/v1/arms/{armId}/development-ratings`, each with a
  `termId` query, CSRF, and an optional `Idempotency-Key`.
- 11 new schemas: the `*RatingSheetDto`, `*RowDto`, `Save*Command` and `Save*RowInput` shapes.
- `SectionDto` gains a required `ratesTraits`; `CreateSectionCommand` and `UpdateSectionCommand` gain it as OPTIONAL.
- The three settings PUTs change only in their description text.

## Acceptance criteria

- [x] `npm run generate:api`, then `npm run check:api-drift` reports `No drift` with exit 0. Do not hand-edit
      `schema.d.ts`.
- [x] `npm run typecheck` and `npm run lint` exit 0. Leave the pin and the lockfile untouched (`openapi-typescript@7.13.0`).
- [x] **No new wrapper code.** `apiGet` and `apiPut` already exist. If you need a new helper, STOP and report why.
- [x] **`SectionDto` is consumed by existing code** (the sections/levels screens). Typecheck will demand `ratesTraits`
      wherever a `SectionDto` literal is built, for example in fixtures, mocks and MSW handlers. Add it with a realistic
      value there (Primary true, Nursery false) and list every file you touch. Do NOT add UI for the flag; that is a
      later card.
- [x] No screens, queries or hooks consume the new rating types yet.

## Out of scope

Rating-entry screens, a UI for `ratesTraits`, and any `backend/**` or `contracts/**` change.

## Notes for the implementing agent

- **Nearest shape:** TASK-0082 did exactly this one contract ago.
- Do NOT run `npm run verify` or e2e (`gates.md` §1). Never git push, pull, merge or rebase.
- **Do not write to `.agent/**`.** Put a `LEDGER ACCOUNT` in your report instead. Do not commit.

## Log

- 2026-09-19 Carded and dispatched by the orchestrator at TASK-0083's promotion, so it ships in the same PR.
- 2026-09-19 **Implemented by frontend-dev**: `schema.d.ts` regenerated (+1306/-78 with the fixtures). Typecheck and lint 0. No `SectionDto` literal is type-asserted, so tsc flagged nothing; the agent found the MSW mocks by grep and added `ratesTraits` in `create-level-dialog.test.tsx` (Primary true) and `section-list.test.tsx` (Secondary false, Primary true). No wrapper code.
- 2026-09-19 **Closed by the orchestrator**: `check:api-drift` No drift, exit 0; `npm run verify` 55 files / 381 tests / build clean, exit 0.
