# TASK-0006 — Regenerate the frontend client against the TASK-0002 contract

Owner: frontend-dev
Depends on: TASK-0002 (contract promoted), TASK-0004 (pipeline that makes this one command)
Contract impact: none — consumes the committed contract, changes nothing on the wire
Status: done

## Goal

TASK-0002 promoted a new contract. `frontend/src/api/schema.d.ts` is now stale, and
`npm run check:api-drift` fails — correctly. This card is the frontend half of §4.3's handoff
sequence for that delta: backend implemented and regenerated, orchestrator reviewed, frontend
now regenerates and adapts.

This is the first real exercise of the pipeline TASK-0004 built, so it doubles as proof that
the pipeline works on a live contract change rather than only on the document it was written
against.

## Contract delta

None of your own. You are consuming:

- **Hash moved** `40e14997…` → `d229f8042c8c826e0b561deb31139e1fb332217e72e7c6d79d2eb66ecd658a79`
- **New path** `GET /api/v1/reference/arms/{armId}/secure` — a reference-slice demonstration of
  privilege enforcement, returning `SecureArmResponse`. It is scaffold, not product; do not
  build anything durable on it.
- **`GET /api/v1/reference/whoami` is now explicitly anonymous.** Response shape unchanged.
- Protected routes now document a `403` ProblemDetails response.

## Acceptance criteria

- [x] `npm run generate:api` re-run; `src/api/schema.d.ts` regenerated and committed.
- [x] `npm run check:api-drift` passes.
- [x] The contract-derived MSW handlers in `src/test/msw/openapi-handlers.ts` pick up the new
      operation without a hand edit. If they do not, that is a defect in the derivation — fix
      the derivation, never hand-write the handler.
- [x] `npm run verify` green — typecheck, lint zero warnings, tests, build. Paste the output.
- [x] Confirm the new `403` response documentation does not break `ApiError` normalisation in
      `src/lib/http/`. If the generated types now express an error shape the transport does not
      model, report it — do not paper over it in the client.
- [x] Append to `.agent/STATE.md`: the contract hash the client is now generated against.

## Out of scope

- Any UI, feature, screen or route. Nothing product-facing.
- Building anything on `/reference/arms/{armId}/secure` beyond what the drift check needs.
- Touching `src/lib/auth/` — still TASK-0003's.
- Any change to the backend or to `contracts/**`.

## Notes for the implementing agent

Read `.agent/STATE.md` first and append to it last (§4.1).

If regeneration produces a diff larger than the contract change justifies, stop and report —
that would mean the generator version or its settings moved, which is a different problem from
a contract change and must not be absorbed silently.

Do not repeat TASK-0004's process error: this card adds no dependencies and must not regenerate
`package-lock.json`. If npm wants to rewrite the lockfile, stop and say so.

## Log
- 2026-08-26 created after TASK-0002 promoted contract `d229f804`
- 2026-08-26 frontend-dev: confirmed drift first (`npm run check:api-drift` failed, diff showed
  exactly the documented delta — new `/api/v1/reference/arms/{armId}/secure` path,
  `SecureArmResponse` schema, `GetSecureArm` operation with 401/403/429, and a new 429 on
  `whoami`). Ran `npm run generate:api`; `src/api/schema.d.ts` regenerated from the committed
  `contracts/openapi.json` (sha256 `d229f8042c8c826e0b561deb31139e1fb332217e72e7c6d79d2eb66ecd658a79`,
  matches `contracts/CONTRACT.lock`). `npm run check:api-drift` now passes. No hand edits to
  `schema.d.ts`.
  MSW derivation (`src/test/msw/openapi-handlers.ts`) picked up `GetSecureArm` automatically —
  it walks `contracts/openapi.json` directly and needs no per-operation code, so no derivation
  change was needed. One thing worth flagging, not a derivation defect: the new
  `SecureArmResponse` schema has no `example` in the contract (every other schema in
  `components.schemas` — `PingResponse`, `WhoAmIResponse`, `ProblemDetails`, etc. — does), so the
  derived default handler for `GET /api/v1/reference/arms/{armId}/secure` answers 200 with an
  empty body instead of `{ "armId": "..." }`. The derivation is doing the honest thing (no
  example in, no fabricated body out) rather than inventing data. No test exercises this
  endpoint today, so nothing is broken, but a future card that writes tests against it will need
  either a `server.use()` override or a `SecureArmResponse.example` added on the backend side —
  this card cannot add one, out of scope (`contracts/**` is not mine to touch) and the endpoint
  is explicitly reference scaffolding.
  Checked `ApiError` normalisation in `src/lib/http/http-error.ts` against the new documented
  `403`: `KIND_BY_STATUS` already maps `403 -> 'forbidden'` and `isProblemDetails`/`normalizeError`
  handle any object-shaped error body generically — this was already correct before today, the
  contract just now documents explicitly what the transport already handled. No gap found, no
  change needed in `src/lib/http/`.
  Added no dependencies; did not touch `package.json` or `package-lock.json` (their existing
  diff in the working tree predates this card — TASK-0004's dependency refresh, already recorded
  in STATE.md Decisions — and generate:api never invokes npm install).
  Gates: `npm run check:api-drift` clean, `npm run verify` green (typecheck, oxlint zero
  warnings, 124/124 tests, build). Did not touch `src/lib/auth/`, `contracts/**`, or
  `backend/**`. Shipped no UI/feature/screen. Status set to `review` for orchestrator sign-off
  per this repo's convention (TASK-0004 was closed by the orchestrator, not self-closed).
- 2026-08-26 orchestrator review: verified independently — drift clean, `npm run verify` green (124/124, zero lint warnings, clean build), and the only frontend file this card touched is the regenerated `schema.d.ts`; `package.json`/`package-lock.json` carry only TASK-0004's diff, so the no-lockfile-churn constraint held. The `403` finding is correct: `KIND_BY_STATUS` already mapped it and the contract merely documents what the transport handled. CLOSED.
- 2026-08-26 **Reopened.** Contract re-promoted (backend added the missing `SecureArmResponse`
  example after `OpenApiContractTests.EverySchema_HasAnExample` failed on a real database run —
  the same gap this card's frontend-dev flagged independently from the MSW handler's empty
  body). Hash moved `d229f8042c8c826e0b561deb31139e1fb332217e72e7c6d79d2eb66ecd658a79` ->
  `228ae57fd80c22f7e0511a665efce5d4722bb3280c66f5e3f4c96fff53795a65`.
  Repeated the same sequence. `npm run check:api-drift` confirmed red first; the diff was
  exactly the fix implied and nothing else: `SecureArmResponse` gained an object-level
  `@example {"armId": "0192f0c4-7c3e-7a1b-9f2d-3b8e5a6c1d40"}` and `armId` gained a
  property-level `@example` of the same UUID. Ran `npm run generate:api`; `schema.d.ts`
  regenerated, not hand-edited. `npm run check:api-drift` now passes.
  `npm run verify`: typecheck clean, oxlint zero warnings, 124/124 tests, `vite build` clean
  (dist unchanged shape — same chunk set as before).
  Re-checked the specific finding from the first pass, as asked: traced `exampleFor()` in
  `src/test/msw/openapi-handlers.ts` against the freshly-fetched `contracts/openapi.json`
  directly (`$ref` resolution to `components.schemas.SecureArmResponse.example`) and confirmed
  it now yields `{"armId":"0192f0c4-7c3e-7a1b-9f2d-3b8e5a6c1d40"}`. So the derived MSW handler
  for `GET /api/v1/reference/arms/{armId}/secure` now answers a real body, not an empty one. No
  derivation defect — it was already reading the correct location; the example simply did not
  exist there until the backend fix landed.
  Same constraints held: no dependency added, no lockfile touched, `src/lib/auth/` untouched,
  `contracts/**`/`backend/**` untouched, no UI shipped. Status set back to `review`, not
  self-closed, per the reopen instruction.
- 2026-08-26 **CLOSED by orchestrator** (second pass, after TASK-0002's reopen moved the contract to
  `228ae57f`). Verified independently: drift clean, `npm run verify` green (124/124, zero lint
  warnings, clean build), and the contract now carries both a property-level and an object-level
  example for `SecureArmResponse`. Checked the whole document: NO schema lacks an example.
  The agent's earlier observation is fully resolved and was not a derivation defect — `exampleFor()`
  was reading the right location all along; the example simply did not exist there. That
  observation, made from the frontend side with no sight of the backend test, independently
  corroborated a real backend defect. Worth remembering as evidence the contract-derived mocks earn
  their keep beyond convenience.
