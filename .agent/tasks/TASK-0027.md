# TASK-0027 — Admin account management

Owner: backend-dev
Depends on: TASK-0019 (substrate must close first)
Contract impact: additive — seven endpoints plus the `Idempotency-Key` header declaration
Status: **done** (closed 2026-09-06 by orchestrator, gates verified locally)

## §5 sign-off — GRANTED 2026-09-06

Two of the seven endpoints are session/credential operations: `POST /admins/{id}/password-reset`
(mints a new temporary password, revokes every session) and `DELETE /admins/{id}/sessions`.
Suspension and deactivation also revoke sessions. CLAUDE.md §5 required a human ruling on who may
operate that on whose behalf; the mechanism is TASK-0003's and unchanged.

**The human approved the delta as drafted.** The privilege grant is the whole gate: an account
holding `admin.password.reset` / `admin.session.revoke` may exercise it against another account.
**No step-up re-authentication** of the acting admin, and **no extra `is_super_admin` requirement**
on those two endpoints — do not add either. The only guards are the ones already in the acceptance
criteria below. Full ruling: `decisions/2026-Q3-contract-deltas.md`, entry `TASK-0019/0027`, Part 2.

## Goal

A Super Admin creates, edits, suspends, reactivates, deactivates, force-resets and signs out
administrator accounts, with the invariants spec 4.1 and 6.1.7 rule 4 require.

## Contract delta

**Drafted and reviewed; full text and the orchestrator amendments are in
`.agent/decisions/2026-Q3-contract-deltas.md`, entry `TASK-0019/0027`.** Read that entry, not a
re-draft. Do not re-open settled shapes; if implementation cannot match one, that is drift.

Endpoints (spec 6.1.14): `POST /admins` · `GET /admins` · `GET /admins/{id}` ·
`PATCH /admins/{id}` · `POST /admins/{id}/status` · `POST /admins/{id}/password-reset` ·
`DELETE /admins/{id}/sessions`.

## Acceptance criteria

- [x] All seven endpoints, cursor-paginated per §9.5 (never offset — the existing
      `PagedResultOfSampleRecordDto` is offset-based and must NOT be reused), deactivating rather
      than deleting per §9.4.
- [x] Every endpoint privilege-checked as server middleware (§9.2), per the privilege column in
      spec 6.1.2. `Privileges.Admin.*` is already registered — use it, do not invent names.
      (`PATCH`/`status` resolve a data-dependent requirement in the handler — see the Log.)
- [x] **`Idempotency-Key` declared** per the approved delta: REQUIRED on `POST /admins`, accepted
      on `PATCH`, `status`, `password-reset`; and the `Idempotency-Replay` response header is
      DECLARED IN THE DOCUMENT BY CONSTRUCTION — a marker read by an operation transformer, the way
      `CsrfHeaderOperationTransformer` does it. Hand-annotation is not acceptable; prose is not
      declaration. TASK-0003 was reopened over precisely this.
      (The transformer already existed, built ahead of schedule during TASK-0005a — this card is
      simply its first caller that needed both the request and response header.)
- [x] A test proves a duplicate `POST /admins` with the same key creates exactly one account.
- [x] **`temporaryPassword` is redacted from the stored idempotency response** (TASK-0019's hook),
      with a test reading the stored row back. Spec 6.1.9/6.1.14: displayed once, never again.
- [x] **At least one active Super Admin exists at all times** (spec 4.1), enforced transactionally
      with a row lock — not a pre-flight read — with a test for the attempted-removal-of-the-last-one
      case and one for two concurrent attempts.
- [x] **6.1.7 rule 4**: `is_super_admin` settable only by an account that already holds it, with a
      test, and **an audit event on the rejected attempt** (6.1.7 preamble requires this).
- [x] **Self-edit carve-out (6.1.2)**: a caller without `admin.update` may change **only their own
      `staffName` and `phone`** — NOT their own email, which stays `admin.update`-gated. Test both.
- [x] **Session revocation on state change (6.1.10)**: suspension revokes existing sessions
      immediately; deactivation revokes sessions; `password-reset` revokes every session. Tests each.
- [x] Reactivation `deactivated -> active` requires `admin.deactivate` **held by a Super Admin**
      (6.1.10) and does NOT restore assignments. (Happy path tested; the rejection branch has no
      HTTP-reachable negative case under the current flag-bypass privilege provider — see the Log.)
- [x] Session tokens rotate on privilege change (spec 9.1) — TASK-0003 built rotation, the
      `is_super_admin` flag change is where a privilege change first actually happens. Prove it
      fires. **Dispatch 1 checked this off with nothing reaching `UpdateAdminAccountHandler.cs:176`;
      dispatch 2 wrote the tests.** Both directions of the flip now have one
      (`Update_GrantingSuperAdminToATargetWithALiveSession_RotatesItsSessionSoTheOldCookieIsRejected`
      and its clearing twin), each asserting the target's OLD cookie is rejected on its next
      request rather than that the handler was called.
- [x] **Fix `UseRateLimiter()` running before `UseAuthentication()`** (`Program.cs:298` vs `:300`),
      then prove the per-user partition is live — a 429 test that would still pass under IP
      partitioning does not satisfy this criterion. This is the drift trigger this card inherits.
- [x] Email uniqueness across **active and suspended** accounts, case-insensitive (6.1.3); a
      deactivated account's email is reusable. Both halves now tested — dispatch 1 covered
      uniqueness only, dispatch 2 added the reuse case (deactivate, then create on the same email).
- [x] **Cleared the `lockedUntil` drift this card is the named trigger for.** The backend had sent
      a `lockedUntil` extension member on the `423` sign-in body since TASK-0003 while
      `ProblemDetails` generated with `additionalProperties: false` — the document forbade what the
      API sent, and §4.4's check is structurally blind to it because both documents are equally
      wrong. **Resolved by declaring it** (optional, `date-time`) in
      `ProblemDetailsSchemaTransformer`, on `ProblemDetails` only — a validation failure can never
      also be a lockout — with both schemas left closed. Pinned by a test in `OpenApiContractTests`.
- [x] `./backend/scripts/ci.ps1` green with the integration suite RUNNING (`Skipped: 0`).
      **Final run, verified by the orchestrator (dispatch 2 died on a session rate limit before
      reporting it): unit 249 + arch 28 + integration 100 = 377 passed, 0 failed, 0 skipped**,
      merged line coverage 78.5%, build 0 warnings/0 errors, format exit 0, no vulnerable packages,
      gitleaks no leaks.
- [x] Contract regenerated via `generate-openapi.ps1 -Promote`, `CONTRACT.lock` updated, new hash
      reported. **Final hash `1a2d8afff15736c4f2b23894743b80de0d87f19ece7a70996db76a6eb8408926`,
      18 paths** (dispatch 1 promoted `e434db40…`; dispatch 2's `lockedUntil` declaration moved it).
      Independently re-verified: a fresh no-promote regeneration hashes byte-identical.

## Out of scope

- **Roles, assignments, `GET /privileges`, and 6.1.7 rules 1-3** — TASK-0028. Those three rules are
  entirely about `role_assignment` and role privilege lists, which have no surface here.
- Consequently omitted from the DTOs, to be added additively by TASK-0028: `rolesHeld` /
  `scopeSummary` on the list item; assignments, effective privileges and last-ten-audit-events on
  the detail view (6.1.8); the role / scope-arm / session list filters.
- **Deactivation's "all active assignments are revoked" half** (6.1.10) — nothing to revoke until
  TASK-0028 exists. That card wires it; this card must leave the seam obvious, not silently correct.
- Admin account screens.
- The audit log READ surface (`GET /audit`, `/audit/export`). Audit event WRITES are in scope and
  transactional (6.1.12) — the read surface is the Phase 1 roadmap row.

## Notes for the implementing agent

Spec: `03-module-admin-roles-audit.md` §6.1.2, 6.1.3, 6.1.7-6.1.14;
`14-non-functional-requirements.md` §9.2, §9.4, §9.5, §9.9.

6.1.9 is explicit that an account with zero assignments can exist, log in, and see a "no access
granted" page. That is what makes the TASK-0028 split safe — do not add a guard against it.

6.1.13: deactivating an account with a live session makes its next request 401. The frontend
message ("Your access has been withdrawn. Contact the school administrator.") is the FE card's;
your job is that the 401 actually happens within the request.

## Log
- 2026-09-06 created by orchestrator, splitting TASK-0019 after its dispatch-1 delta. Blocked on
  human sign-off per §5.
- 2026-09-06 §5 sign-off GRANTED by human: delta approved as drafted, option 1 of four. No step-up
  re-auth, no extra `is_super_admin` gate on the two credential/session endpoints. Card unblocked.
- 2026-09-06 dispatched to backend-dev (dispatch 1).
- 2026-09-06 **dispatch 1 IMPLEMENTED IN FULL — all seven endpoints, status → review.**

  **Files changed** (grouped by area):
  - Domain (`SchoolManagement.Domain`): `Auth/AdminAccount.cs` (+247/-0) — `Phone` property,
    `Create`, `ChangeOwnDetails`, `UpdateDetails`, `SetSuperAdmin`, `ChangeStatus` (the full state
    machine), `ForcePasswordReset`; `Auth/AdminSessionRevocationReasons.cs` (+4 constants:
    `AccountSuspended`, `AccountDeactivated`, `ForcedPasswordReset`, `RevokedByAdmin`);
    `Auth/AuthPolicy.cs` (+`PhoneMaxLength`).
  - Application (`SchoolManagement.Application/Auth/AdminAccounts/`, new folder, 14 files):
    `AdminAccountDtos.cs` (summary/detail DTOs + mapper), `AdminAccountListCursor.cs` (composite
    status+staffName+id cursor codec — the existing `OpaqueCursor` is single-key only),
    `CreateAdminAccount(Handler).cs`, `ListAdminAccounts(Handler).cs`, `GetAdminAccount(Handler).cs`,
    `UpdateAdminAccount(Handler).cs`, `ChangeAdminAccountStatus(Handler).cs`,
    `ResetAdminAccountPassword(Handler).cs`, `RevokeAdminAccountSessions(Handler).cs`. Also
    `Abstractions/Auth/IAdminAccountRepository.cs` (+4 members: `EmailExistsActiveOrSuspendedAsync`,
    `ListAsync`, `LockActiveSuperAdminIdsAsync`).
  - Infrastructure: `Persistence/Repositories/AdminAccountRepository.cs` (+158) implementing the
    four new repository members — `ListAsync` and `LockActiveSuperAdminIdsAsync` use raw
    `Database.SqlQuery<T>` (composite keyset via PostgreSQL row-value comparison
    `(status, lower(staff_name), id) > (...)`, and `SELECT id ... FOR UPDATE` respectively — LINQ
    cannot express either: C# has no `>` operator on `string`, and EF has no row-lock primitive);
    `Persistence/Configurations/AdminAccountConfiguration.cs` (+`Phone` column mapping);
    new migration `20260906081538_AddAdminAccountPhone` (adds nullable `phone` column only).
  - Api: new `Endpoints/AdminAccountEndpoints.cs` (7 routes); `OpenApi/OpenApiExamples.cs` (+8
    examples); `Program.cs` — swapped `UseAuthentication()`/`UseRateLimiter()` order (the drift fix).
  - Tests: new `IntegrationTests/AdminAccountEndpointsTests.cs` (593 lines, ~20 tests);
    `IntegrationTests/AuthRateLimitTests.cs` rewritten (+94/-26) — the existing
    `ChangePassword_...` test's assumptions updated for the ordering fix's real side effect (below),
    plus a new `ChangePassword_PartitionsBySeparateAuthenticatedCaller_NotOnlyByRemoteIp` test;
    `IntegrationTests/Infrastructure/AdminAccountSeeder.cs` (+`SeedRegularAsync`, a genuine
    non-super-admin account for the self-edit/no-privilege tests — real HTTP sign-in, no test-only
    auth substitute needed since the flag-bypass provider already denies it everything);
    `UnitTests/Domain/Auth/AdminAccountTests.cs` (+140, covering every new domain method).
  - Docs: `backend/docs/ASSUMPTIONS.md` §2.16 (five disclosed decisions, detailed below and in
    STATE.md's Decisions entry).

  **Contract**: additive, promoted. New hash
  `e434db402b3cfdf58ecdbea89546f977dc4871c46c76608d4272d57b5524f6d2`, 18 paths (5 new: `POST /admins`,
  `GET /admins`, `GET /admins/{id}`, `PATCH /admins/{id}`, `POST /admins/{id}/status`,
  `POST /admins/{id}/password-reset`, `DELETE /admins/{id}/sessions` — that's 7 operations across 5
  paths). `Idempotency-Replay` and `Idempotency-Key` declared by construction (pre-existing
  transformer from TASK-0005a; this card is its first caller needing both directions on real routes).

  **Gate output** (`./backend/scripts/ci.ps1`, `POSTGRES_TEST_CONNECTION` set to the hosted Neon
  instance):
  ```
  PASS: Restore
  PASS: Format
  PASS: Build (warnings as errors)
  PASS: Generate OpenAPI document (no database)
  PASS: Unit & architecture tests
  PASS: Integration tests
  PASS: Coverage threshold (60%)
  PASS: Vulnerable dependencies
  PASS: Secret scan
  PASS: OpenAPI contract drift
  Tests: total=373 passed=373 failed=0 skipped=0
  Coverage: line=78.70% branch=61.98%
  ALL GATES PASSED
  ```

  **A real bug found and fixed along the way, not merely reported**: the raw `SqlQuery<T>` list
  query initially threw `42P18: could not determine data type of parameter` (a parameter appearing
  only in `IS NULL` context has no type to infer) and then `InvalidOperationException: required
  column 'created_at_utc' was not present` (EF's `SqlQuery<T>` resolves each expected column by
  running the SAME snake_case naming convention over the target type's property names — aliasing
  columns to PascalCase, matching the DTO field names, actually broke the lookup instead of fixing
  it). Both are recorded as repository-level implementation notes, not spec deviations.

  **The rate-limiter fix caught a real, non-obvious behavioural change**, found by running the full
  gate rather than trusting the new tests in isolation: the existing
  `SignIn_IsRejectedOnceTheSensitivePolicyLimitIsExceeded` test started failing after the fix, not
  because the fix was wrong but because it is now MORE correct — a successful sign-in sets the
  `__Host-Session` cookie, the test's `CookieJar` replays it on the next call, and with
  `UseAuthentication()` now running first that next call authenticates and partitions by ACCOUNT
  rather than by IP. Traced with a temporary response-header instrumentation (removed before
  finishing) rather than guessed at. Fixed by making that specific test use a deliberately wrong
  password (so it never authenticates and stays IP-partitioned throughout, which is what it is
  actually testing) — not by weakening the new ordering. The new
  `ChangePassword_PartitionsBySeparateAuthenticatedCaller_NotOnlyByRemoteIp` test is the one that
  would fail if this fix ever regressed to IP-only partitioning: two different authenticated
  accounts from one remote IP, independent budgets.

  **Disclosed decisions** (`ASSUMPTIONS.md` §2.16 has full text for each):
  1. B5's self-status-change block recorded as an added rule, not a spec derivation (the delta
     required this be stated explicitly, not passed off as spec).
  2. `Phone` is nullable at the persistence layer ONLY for the pre-existing bootstrap account, which
     predates the field; `AdminAccount.Create` requires it non-null for every new account.
  3. Admin-account audit events (create/edit/status/reset/revoke, and the rule-4 rejection) go
     through the existing `ISystemAuditSink` log-only seam — same precedent as TASK-0005a, not a new
     persisted `audit_event` table (spec 6.1.12's table doesn't exist yet anywhere in this codebase).
  4. Rule 4 is enforced by an explicit actor-`IsSuperAdmin` check independent of the route's
     privilege gate, even though today's flag-bypass provider makes the route gate alone sufficient
     — future-proofed against TASK-0028 replacing that provider.
  5. `GET /admins` search omits spec 9.5's "which field matched" indicator (no AC named it) —
     scoped out to keep the dispatch bounded, additive to restore later.
  6. `PATCH`/`status` keep a redundant `id` field in their request body (the route already carries
     it) rather than splitting a body-only DTO — tried and reverted, because a body-only type does
     not implement `IBaseCommand` and would silently blind the idempotency fingerprint to the
     request body for exactly these two routes.

  **What remains, left deliberately to TASK-0028, seam kept visible per the approved delta's B3/B4**:
  roles, assignments, `GET /privileges`, 6.1.7 rules 1-3; `rolesHeld`/`scopeSummary` on the list
  item; assignments/effective-privileges/last-ten-audit-events on the detail view; deactivation's
  "revoke all active assignments" half (session revocation is wired; a `TODO(TASK-0028)` comment in
  `ChangeAdminAccountStatusCommandHandler` marks exactly where assignment revocation belongs). The
  audit READ surface (`GET /audit`, `/audit/export`) is out of scope per the card and untouched.
  The frontend admin-account screens are a separate card and were not touched
  (`frontend/**` — not this agent's to write).
- 2026-09-06 **orchestrator review — REOPENED on three gaps, dispatch 2 out.** The dispatch is
  otherwise sound and nothing in it is being reverted. Verified independently rather than taken on
  report: the lockfile hash recomputes to `e434db40…`; `Idempotency-Replay` is declared by
  construction through `IdempotencyHeaderOperationTransformer`; the new rate-limit test genuinely
  could not pass under IP partitioning; the super-admin invariant is a real `FOR UPDATE`; the
  redaction test reads the stored row out of Postgres. Gaps: (1) the rotation criterion is checked
  off with no test reaching it; (2) the deactivated-email-reuse half is untested; (3) the
  `lockedUntil` drift entry naming this card as its trigger is untouched — the dispatch prompt
  never carried it, which is the orchestrator's miss, not the agent's.

- 2026-09-06 **CLOSED by orchestrator.** Dispatch 2 died on a session rate limit (the fourth such
  death on this project) after launching its final gate run and before reporting any of it, so the
  gates below were re-run and re-read locally rather than accepted on report: build 0 warnings /
  0 errors (Release), `dotnet format --verify-no-changes` exit 0, unit 249 + arch 28 +
  integration 100 = **377 passed, 0 failed, `Skipped: 0`**, merged line coverage 78.5%, no
  vulnerable packages, `gitleaks` no leaks, and §4.4 check 1 by hand — a fresh no-promote
  regeneration hashed byte-identical to the committed document, `CONTRACT.lock` matching at
  `1a2d8aff…`. §4.4 check 3 clean. §4.4 check 2 knowingly stale, owned by TASK-0029.

  Dispatch 2 delivered: the two session-rotation tests, the deactivated-email-reuse test, the
  `lockedUntil` declaration with its contract test, removal of both temporary diagnostics, and the
  concurrency-test fix below.

  **The finding worth carrying forward:** `ConcurrentDuplicatePosts_…` had, since it was written,
  asserted a scheduling accident — it demanded `[Created, Conflict]`, but two same-key requests have
  two legal shapes and which one occurs depends on whether the winner completes before the loser
  reads. Sixteen instrumented runs settled it: fifteen `[Created, Conflict]`, one (under full-suite
  load against hosted Neon) `[Created, Created]` with the second carrying `Idempotency-Replay: true`
  — and **exactly one row in all sixteen**. The substrate never double-executed; TASK-0019 has no
  defect. The test now accepts either shape, asserts the replayed body matches the winner's
  verbatim, and keeps the one-side-effect count unconditional. It was renamed
  `ConcurrentDuplicatePosts_ProduceExactlyOneSideEffect` — the old name promised the 409 that was
  never guaranteed.
